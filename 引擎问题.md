# 引擎问题（本项目遇到的）

> 由执行者随手记、主 agent 汇总去重。**不影响本轮交付**：能绕就绕，绕不过的登记进 `策划/验收表.md`。
> ⚠️ 本轮用户已明确「可以改引擎」⇒ 「我们的绕法」列对已动过的项改写为「**本轮已改引擎**（落点 + 行号）」。

## 引擎缺口

| 现象 | 最小复现 | 影响 | 我们的绕法 | 建议 |
|---|---|---|---|---|
| 引擎浮层 `PointerFloatLayer` 只有「跟随指针」一种定位口径（`Tick()` 每帧按 `Game.Input.MousePosition` 重摆、贴边时关于指针镜像），**摆不到**「锚控件矩形顶边中点」 | 想按原版口径给 UI 控件做 hover 提示（参考实现 `Tooltip.cs`：`pos = new Vector2(rect.center.x, rect.yMax)` → `ShowScreenLabel`） | 项目侧无法复用引擎浮层做**控件级**提示 | 项目侧自建 `client/Assets/Scripts/UI/ControlTip.cs`（黑底 `Image` + `D2Label`，尺寸/底色/字号/文字色全部复用本项目既有常量），只接 `UI/HoverTarget` 的 `OnEnter/OnExit` | 若后续项目普遍要「控件顶边提示」，再考虑给引擎浮层加「锚到控件 rect」的定位口径；⛔ 本项目本轮未改引擎 |
| 同一个 `Image` 上存在**两条着色通道**：引擎 `UiImageLoader.SetTint`（贴图未到时**记录**、到位后套用）与调用方直接写 `Image.color`；后者会被异步回调**覆写**，且**没有任何警告** | `UI/CharacterPanel.cs` 的 `SetPlusEnabled` 在 `points == 0` 时把 `img.color` 置 `alpha 0.40`，实机读到 `color=(1,1,1,1)`（贴图到位回调晚于该赋值） | 「写了但看不到」的静默失效 —— 代码与交付态不一致，后续维护者会据此做错判断 | 该处按「写不出出处的量不进工程」**删掉**置灰（保留 `Button.interactable` 的行为口径）；需要灰度时必须走引擎 tint 记录通道、或放在回调之后 | 引擎侧把「色调 / 置灰」收敛成一条**可后置**的契约，或在直接写 `color` 被覆盖时打一条可关的 Warn |
| 引擎音效播放闸门只有「单帧起播上限」「同 clip 真并发上限」两维，**没有**「同一路径最小重播间隔（时间窗）」 | `SoundRepeatGate.MinRepeatSecondsPerClip` 保持默认 `0`（不限），对同一 clip 连发 > 32 次 ⇒ 32 个音源被该 clip 占满，其余音效被静默丢弃（日志 `音效池（32 个音源）已全部占用，本次播放被丢弃`） | 高热度键（实测 `portal` ~52 次/秒，`client/Logs/2026-09-23.log`）会把音源池占满 ⇒ 同一时刻的 hit / monster_attack / miss 全被丢 | **本轮已改引擎**：`clover-client-unity-engine/Runtime/Presentation/Sound.cs` 新增 `public static class SoundRepeatGate`（`MinRepeatSecondsPerClip`（**默认 `0` = 不限**）/ 可注入 `Clock` / `DropCount` / `ShouldDrop(key, intervalSeconds, out sinceSeconds)` / `Reset()`，第 642 行起），`SoundManager.AllowPlay` 第 244-252 行接入，告警走 `LogThrottle.WarnThrottled("Sound", "playcap.repeat:<path>")` | 保持现状；后续项目直接置 `MinRepeatSecondsPerClip`，⛔ 不要再自写一份同键节流（diablo2 的 `Module/Audio/SfxThrottle.cs` 本轮已改薄转发） |

## 引擎 bug

| 现象 | 最小复现 | 影响 | 我们的绕法 | 建议 |
|---|---|---|---|---|
| （本轮无） | | | | |

## skill 问题

| 位置（文件 / 条款） | 问题 | 当时的场景 | 建议 |
|---|---|---|---|
| `scaffold/probe-harness.md`（探针驱动写法） | 未写明 `ScreenCapture.CaptureScreenshot` **写的是「调用之后」那一帧**；驱动若「Snap 完立刻 Close/Open 下一态」，截出来的是**已切走**的画面 | 实机采 18 面板时，第一版驱动把 5 张游戏内面板全拍成空画面 | 写明：Snap 之后必须**中断本步并保持 ~0.5s（或等 ≥2 帧）**再切下一态；并加一句「图里没有预期内容时，第一嫌疑是快门时机，不是面板没画」 |
| `reference/rules-full.md` §4（进 Play / 读实机读数之前） | 缺一条判断「**运行期程序集比源码旧**」的机械方法 —— 编辑器开着时 DLL 可能比源码早数小时，此时一切实机读数描述的都是**旧构建** | 同一根因废掉两批读数：U32 锚点（反射出的字段数 = 0）、关闭钮（驱动引用新常量**编译不过**、而源码里明明有） | 在驱动里引用**本轮新增的常量 / 成员**编一次：**编译不过、而源码里有** ⇒ 构建陈旧（成本 = 一次编译哨兵）。它比「比 mtime」硬，也比「感觉不对」硬 |
| `patterns/engine-fix.md` §4.5 | 要求「**同时**改 ③ 站点文档 `clover-doc/`」，与本片任务书的「⛔ 不许改 clover-doc」直接打架 | 改 `Runtime/Presentation/Sound.cs` 之后按 §4.5 本该同步 `clover-doc/client/**` 的 API 表 | §4.5 补一句「以**本轮任务书的授权清单**为准；未授权的副本只登记不改」 |

## 附：离线自检宿主与引擎源码的耦合（本轮**未改**，仅登记）

| 现象 | 最小复现 | 影响 | 我们的绕法 | 建议 |
|---|---|---|---|---|
| `tools/probes/hosts/audiocheck`（纯 .NET、不启编辑器的自检宿主）用 `<Compile Include="Assets/Scripts/Module/Audio/*.cs" />` 直编项目源码，但**不含**引擎 `Runtime/Presentation/Sound.cs` ⇒ 项目 `SfxThrottle` 一旦转发到 `CloverEngine.SoundRepeatGate`，宿主必然 `CS0103` | `dotnet build tools/probes/hosts/audiocheck/AudioCheck.csproj` | 宿主编译红（与 `d2-audio` 报的同一处）；host 全量闸门会红 | 本片**未动宿主**（不在授权写清单）⇒ 最小修法见右列，留给主 agent 统一处理 | ① `AudioCheck.csproj` 增 1 行 `<Compile Include="..\..\..\..\..\clover-client-unity-engine\Runtime\Presentation\Sound.cs" />`；② `shim/EngineShim.cs` 的 `IResourceManager` 补 `void Release(string path);`、补 `CloverEvents`（`App.Pause` / `App.Resume` 两个常量）；③ `Program.cs` 的 `FakeRes` 补 `Release` 空实现 |
