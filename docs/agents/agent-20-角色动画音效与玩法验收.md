# agent-20：**角色/怪物动画（DCC）+ 音效 + 玩法端到端验收**（三个任务包）

> 背景：原版**地形/UI/图标**已接入（Tiles 514 / Objects 423 / UI 681 / Items 348 张原版 PNG），
> 城镇已"一眼是暗黑2"。**剩下三块**：① 角色/怪物**动画**（现在是黄色方块）；② **音效/BGM**；③ **玩法端到端验收**（42 行验收表）。
> 规则：skill `SKILL.md::★★ 1:1 复刻硬标准`。通用前置按 `docs/agents/_common.md`（尤其 §3.5）。

## 0. 已就绪的工具与原料（**不要重复下载**）

| 东西 | 路径 |
| --- | --- |
| 解包器（已验证可解加密 mpq） | `_assets_src\storm.py`：`python storm.py list/extract <mpq> <out> <ext...>` |
| 数据包 | `_assets_src\d2mpq\{d2data,d2char,d2sfx,d2music,patch_d2}.mpq` |
| 已导出 | `_assets_src\d2raw\`（`.dt1`/`.TBL`/`.PL2`） |
| **格式参考实现（有依据）** | `_assets_tmp\d2src\Diablerie\Assets\Scripts\**`（DCC/COF/DC6 读取器）；另可查 `OpenDiablo2` 的 Go 实现 |

---

## §A 交给 `agent-07`：**DCC 解码 → 角色/怪物动画**（★ 最后一块"像不像"）

**只许改**：`Module/View/**`、`Module/Monster/**`；**允许新增** `tools/d2codec/dcc.py`、`tools/d2codec/cof.py`；
**允许写入** `client/Assets/Resources/Clover/D2/{Chars,Monsters}/**`。

**要做**：
1. **写 DCC 解码器**：`.dcc` = 头 + 方向数 + 每方向帧数 + 每帧头（含**等距 x/y 偏移**）+ 每帧多层 RLE 数据 + 调色板。
   **必须保留每帧的 offset**（决定动画对齐，去掉就会"角色浮空/错位"）。
   **格式依据**：`Diablerie/Assets/Scripts/` 的 DCC 读取器 + `OpenDiablo2` 的 `dcc` 包。
2. **解出并接上**：
   - 5 个职业的 **idle / walk / attack / cast / hit / death**（8 方向）；**先做亚马逊**（当前默认职业），再做其余 4 个；
   - Act1 的 8 种怪物（堕落者 / 堕落萨满 / 尖刺鼠 / 僵尸 / 堕落罗格 / 血鹰 / 巨兽 / 幽灵）的 idle/walk/attack/hit/death；
   - 帧落地命名 = **现有约定** `{动作}_{方向}_{帧号}.png`（`Module/View/SpriteFrames.cs` 里的 `FrameCounts` 要同步改）；
   - **NPC**（阿卡拉/卡夏/恰西/基德/瓦瑞夫）：用对应的原版 NPC 动画（找不到就登记欠图，**不许用黄方块**）。
3. **`SpriteFrames` 的帧键/帧数改成真实值**（现在是占位常量）。
4. **顺手修**：城镇截图 `Assets/Screenshots/v3_town_tiles.png` 里有 **4 个绿色菱形占位块** —— 查清是什么
   （NPC 占位？某类 tile？），**换成真素材或正确逻辑**，并在回报里说明根因。

**验收**：
- [ ] 解码统计（职业/怪物 × 动作 × 方向 × 帧数）
- [ ] 进 Play 截图 `Assets/Screenshots/v4_chars.png`：**能看到原版亚马逊（不是黄方块）**，走一步能看到行走帧变化
- [ ] 截图 `v4_monster.png`：血腥荒野里能看到原版怪物
- [ ] 4 个绿色菱形**消失**（或说明为什么该保留）
- [ ] 10 宿主全绿

---

## §B 交给 `agent-11`：**音效 / BGM 接入**（原版 `.wav`）

**只许改**：`Module/Audio/**`；**允许写入** `client/Assets/Resources/Clover/Sound/{SFX,BGM}/**`。

**要做**：
1. 用 `storm.py extract` 从 **`d2sfx.mpq`** 导出 `.wav`（**只导 `SfxRegistry` 里登记过的键对应的文件**，
   不要全导）；BGM 从 `d2music.mpq` 导出 `.wav/.mp3`（城镇一首 + 野外一首）。
2. **文件名对齐 `Module/Audio/SfxRegistry.cs`**（键 → 文件的唯一登记表）。原版文件名不同就**改名或改登记表**，
   **必须留下一张对照表**（键 ↔ 原版文件名 ↔ 触发点）。
3. 音量/静音持久化已实现，**不要重写**；只补"文件到位"。
4. **验收**：Play 里**真的出声**（`Game.Sound.PlaySFX` 有调用 + 无"文件缺失"Warn）；
   贴出 `SfxRegistry` 全部键的"文件已就位"清单（逐条 `Test-Path`）。

**验收**：
- [ ] 导出清单（键 ↔ 文件 ↔ 大小）
- [ ] Play 里 Console 无 `[Audio] 音效文件缺失`（贴日志）
- [ ] 挥砍/命中/怪物叫/拾取/升级/UI 点击/进图 至少 7 个触发点**实际响过**（贴日志）

---

## §C 交给 `agent-12`：**玩法端到端验收 + 补完 `策划/验收表.md`**（★ 交付合同）

**只许改**：`策划/验收表.md`、`策划/自审对比/**`；**发现 bug 写进回报**，不要自己改别人的模块
（**例外**：`App/**` 归你，可在其中做必要的输出/打印修正）。

**要做**（**逐项真做，不许读代码推断**）：
1. 按 `策划/验收表.md` 的 **42 行**逐项在 Play 里**演一遍**，每格填：**≥2 条独立证据（日志行 + 截图路径）+ 结论**。
2. 重点验（之前从未跑过的）：**点击移动 + A* 绕障**、**交互光标**、**攻击与命中三件套**（飘字+音效+目标血条）、
   **怪物 AI**（追击/远程/萨满复活/逃跑）、**精英怪词缀**、**掉落/拾取/背包/装备/腰带**、**商店买卖修理**、
   **主线任务「邪恶洞穴」完整链**（接取→清光→交付→+1 技能点）、**技能树与施法**、**投射物**、
   **小地图**、**暂停/设置**、**存档往返**、**回主菜单再进一次**。
3. 机制验证用探针：给玩家 100 万经验再杀 1 只小怪验升级（不要真刷半小时）。
4. **截图必须自己用多模态读一遍**，并把图上的数字与日志数字**对一遍**（对不上就是 bug）。
5. 拍到的坏图**不许删**，写清"坏在哪 + 改了什么 + 重拍"。
6. 完成后写 `策划/自审对比/README.md`：与 A 的同角度对照表，每项只写 `一致` / `不一致（差在哪）`。

**验收**：
- [ ] `策划/验收表.md` **42 行全部填满**（结论列无空、无"未验/待验"）
- [ ] 每格 ≥2 条证据（日志行 + 截图路径）
- [ ] 发现的 bug 列成清单（现象/证据/归口模块）回报给主 agent
- [ ] **不许**自己改 `Module/**`、`UI/**`

---

## 共用
- 驱动编辑器前先跑 `client/_dev/p_runbg.cs`（失焦不 tick）；截图前等 ≥6 秒否则拿过期帧。
- 面板真值用 `Game.UI.IsOpen<T>()`；截图自己读图核对。
- ⛔ 不许读工作区里其它 `clover-project-*`。
- ⛔ 产出必须是**原版像素**，缺什么登记 `client/资源欠缺清单.md`，**不许重绘/AI 补图/用色块顶替**。
