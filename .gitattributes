# 由 skill §3 清单要求生成（tools/verify.ps1 收尾自检口径）
# text=auto：交给 git 自动判定文本换行（源码统一 LF，避免 CRLF 抖动）
* text=auto

# 二进制类型标记：这些文件一律不做换行/编码转换
*.png binary
*.jpg binary
*.wav binary
*.unity binary
*.prefab binary
*.asset binary
*.xlsx binary
