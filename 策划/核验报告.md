# 暗黑破坏神 II 复刻项目 · 完整核验报告

- **被核验对象**：`clover-project-diablo2`
- **核验时间**：2026-09-20
- **核验口径**：`clover-engine` skill（三条铁律 / 成本闸门 / 交付闸门）+ 项目自带一键闸门 `tools/verify.ps1` + 独立复核
- **编辑器环境**：Unity 6000.6.0f1，端口 7802 = `ready`，工程路径正确（`unity status` 实跑）

---

## 一、总判定

> **机械闸门全绿，但文档层未闭环 —— 当前不可按"已完成 / 47 行全通过"交付。**

| 维度 | 结论 |
|---|---|
| `tools/verify.ps1` 机械闸门 | **FAIL=0**（25 PASS / 7 HUMAN-ONLY）✅ |
| 编译 / 控制台 | `recompile_status = up_to_date`、`consoleErrors = 0` ✅ |
| 离线自检宿主 | `TOTAL_HOSTS=11 FAILED=0` ✅ |
| d2codec 素材自证 | 4 个校验脚本 + 素材 SHA256 逐帧全等 ✅ |
| 验收表本体 | 47 行，全"通过"、0"不一致"、类别齐全、汇总自洽 ✅ |
| **验收表 ↔ 当前代码一致性** | ❌ **滞后**（见问题 P1） |
| **轮次登记完整性** | ❌ **有未登记轮次**（见问题 P2） |
| 证据自身自洽 | ⚠️ 联络图与索引不一致（见问题 P3） |

---

## 二、机械闸门逐项（`tools/verify.ps1` 实跑）

```
PASS  stray-temp-files / hard-rules / hard-rules:Resources.Load / direction-key-move
PASS  table-rows(47) / table-category / table-summary(47 pass / 0 mismatch)
PASS  allow-diff-registry(25 例外行齐全) / path-reachability(82 图全在) / reference-tables(9)
PASS  no-handoff-docs / no-escaped-artifacts / sampler-selfcheck / impl-by-executor(64)
PASS  compile(up_to_date) / console-errors(0) / offline-hosts(11/FAILED=0)
PASS  d2codec ×4 / d2codec:panels-pixels(A 条像素 0 不一致) / original-asset-md5(6 帧逐字节全等)
PASS  evidence-economy
HUMAN-ONLY ×7
===== summary: FAIL=0  HUMAN-ONLY=7 =====
```

7 项 HUMAN-ONLY：`freshness:u2-rows`、`freshness:u1-rows`、`freshness-global`、`engine-selfname`、`skill-family-consistency`、`play-budget`、`freeze-before-capture`

---

## 三、独立复核（我另行取证的结论）

| 复核项 | 方法 | 结果 |
|---|---|---|
| 形态 = 单机 | `Test-Path server`；grep `CloverNet.Init` | 无 `server/` ✅；`CloverNet.Init` 仅 1 处命中，且在 `App/Bootstrap.cs` 注释里写"**不调** CloverNet.Init" ✅ |
| 引擎自称 | 对源码统计 `clover-engine/CloverEngine/CloverNet` | 仅 `CloverEngine` 61 处，无自造别名 ✅ |
| 硬规则 | 闸门第 02 项 | `Debug.Log`/`PlayerPrefs`/`GameObject.Find`/`FindObjectOfType`/`Instantiate(`/裸 `Input.` = **0** ✅ |
| 方向键（U-1） | 闸门第 03 项 | 7 个模式 **0 命中** ✅（原版只有鼠标点地移动） |
| 素材为原版 | 闸门第 19b/20 项 | panels 组 A 条逐帧 RGBA **24/24 不一致 0**；EndGame 帧 SHA256 与独立 DC6 解码**全等** ✅ |
| 联络图（U-2） | 我**多模态读图** `p8_contact_combat.png` | 图上确实有怪物、玩家、红色伤害飘字（2/1/3/3），**内容真实** ✅（但格数与索引不符，见 P3） |
| 三屏对照 | 读 `自审对比/启动链路对照.md` | prefab 矩形/字号/色值逐值为原版 ×1.8，差异 0；字体行 IoU 0.974 ✅ |

**已达标的交付要素**：启动与菜单链路、首场景完整版、游戏内流程（暂停/设置/回主菜单/EXIT 按钮 → `Events.QuitRequest`）三段齐备；814 张实机证据 + 联络图 + 离线宿主 + 一键闸门。

---

## 四、发现的问题

### P1【严重 · 验收表滞后于代码】文档与现状矛盾

- 验收表最后修改 **09-19 19:43**；但源码改到 **09-20 00:21**。闸门 `freshness-global` 已自报：**388/388 张图早于最新源码 00:21:15**（被判 HUMAN-ONLY，未 FAIL）。
- **确凿矛盾（第 2 行 主菜单）**：验收表写「原版 **4 项** SINGLE PLAYER / MULTIPLAYER / CINEMATICS / EXIT … **4 项文案逐字正确**」；而当前 `UI/MainMenuPanel.cs`（22:48 改）**只建 2 项**（`SINGLE PLAYER` / `EXIT`），中间两项**按用户回合点名删除**。新证据 `Screenshots/p40_menu_2items.png`（22:56）与现状一致，与验收表不一致。`自审对比/启动链路对照.md` §① 同错。
- **可能同样滞后**：第 43/44 行（营地 56×40 / 荒野 72×72）的证据早于 `MapGenTownLayout.cs`(00:21)、`MapGenTown.cs`(00:12)、`MapGenWilderness.cs`(23:35)、`MapView.cs`(00:17) 的本轮改动。
- 另：第 2 行**未登记**"主菜单底部 `by clover-engine` 署名"（skill §1.6 硬要求 + 用户点名项），该实现已在 `MainMenuPanel.cs` 落地（用户原话「1.首界面 by clover-engine 呢？」）。

### P2【严重 · 未登记轮次】有轮次绕过了验收流程

`play-log.tsv` / `dispatch-log.tsv` / `.ai-tmp` 显示存在**晚于验收表、且任何文档都没记录**的轮次：

- `agent-38`（move-flash-placeholder）
- `agent-40a`（品牌署名 + 删 2 个菜单按钮）
- `agent-41`（`p41_*` 联络图，09-20 01:08）
- `pass5-final-play-evidence`（"**9 项用户投诉**"的最终取证）

`docs/agents/` 只到 `agent-37`；验收表在"片 23"后即止。⇒ 这几轮的改动无验收行、无对照更新、无 bug 登记。

### P3【一般 · 证据自身不自洽】U-2 联络图与其索引不一致

| 项 | 实际 | 出处矛盾 |
|---|---|---|
| 联络图 `p8_contact_combat.png` | 1880×1677 = **2 列 × 3 行，5 格（A1–A5）** | 片 15 段写"5 格 A1~A5"✅；**自审对比表写"2 列 × 4 行 = 8 格"**❌ |
| 索引 `p8_contact_combat.index.tsv` | **8 行（cell 01–08）**，mtime **07:18** | 联络图 mtime **11:47**（更新）⇒ **索引未随图重建**，8 个瓦片引用与 5 格图对不上 |

### P4【一般 · 闸门红项靠自裁】裁定来源非独立人

4 条 `# adjudicated:` 由**执行方 agent 自己**写进 `dispatch-log.tsv`（`freshness:u2-rows` / `freshness:u1-rows` / `freeze-before-capture` / `play-budget`）。其中 `freeze-before-capture` 的实质是「**39 个实现文件在采证开始后被改动**」。按 skill，裁定权不在执行者手里 ⇒ 这 4 项红不能算"已消解"，只能算"已说明理由"。

### P5【一般 · 成本闸门超预算】

`play-log.tsv` = **18 条 > 预算 8**（adjudicated 为"跨片累计"）；且文件头自述为**事后补记**（"backfilled by the main agent from the actual round"）—— 与 skill「派活即留痕」不符。

### P6【轻微 · 临时文件未清】

`.ai-tmp/test/` 仍有 40+ 遗留一次性产物（`p41_*`、`raw/`、`selftest/`、`savecheck/`、`（一次性判据脚本，2026-09-24 清理）`、多份 `*_out.txt`），与 skill §2「`.ai-tmp/test/` 用完即删」不符。

### P7【轻微 · 数字漂移】

离线宿主机数：验收表与历史留档记 `TOTAL_HOSTS=10`，**本次实跑 `TOTAL_HOSTS=11`**（`hosts/` 下现有 13 个 `*check` 目录）—— 新增宿主未登记。`impl-by-executor` 本次 64 / 上次留档 77，亦为持续改动所致。

### P8【登记类 · 已如实登记的缺口，不算隐瞒】

- **无原版实机截图**（R2）⇒ 六维里的"像素级并排比对"**未做**，只有 prefab 矩形/素材帧的可复算判据。
- **BLOCKED 12 条**（BL-1 小地图标记语义 / BL-2 死亡屏金币数字 / BL-3 AutoMap 逐格 Cel / BL-4 MENU 调色板 / BL-6 convert.py 数据源路径 / BL-8 Velocity 量纲 / BL-9 受击闪白机制出处 …）。
- 例外登记 25 条（E1–E31），其中 E21/R1（按钮字面推进宽度差 ~11%）、E23（自动地图"右上角定尺框"≠ 原版满屏叠加层）等。
- 验收表末尾声明的唯一阻塞「**请重启 Unity 编辑器**验 E-build-03（僵尸场景）」，编辑器现为 `ready` 且其后已有 4+ 次 Play（至 09-20 01:08）—— 该阻塞**实际已具备闭环条件，但文档未记录闭环结果**。

---

## 五、结论与建议

1. **不能按现状判定"已完成 / 47 行全通过"**。机械闸门全绿，但那是"上一版文档 + 自裁红项"下的绿；按 skill「有 FAIL 就不许说完成」的同源口径，**验收表第 2 行的结论现已被代码推翻，属 row 级 FAIL**。
2. **先做一件事（补文档，不改代码）**：把 agent-38/40a/41/pass5 四轮（尤其是**主菜单 2 项 + 品牌署名**、场景/相机/动画帧率改动）补进验收表与 `自审对比` 对应行，并**重采**受影响的证据行（§1.13 批次规则只重采受影响的行）。
3. 修 P3（重建联络图索引或改正表内"8 格"表述）、P6（清 `.ai-tmp/test/`）、P7（宿主数改 11）。
4. P4/P5 属流程层：后续轮次**派活前**写 `dispatch-log.tsv`、**进 Play 当刻**写 `play-log.tsv`，裁定红项交独立人（或明确标注为"自裁、待复核"）。
5. 真 BLOCKED（只有用户能给）：**原版实机截图**（或可运行的原版）⇒ 拿到后才能做六维里的"同机位并排像素级对照"，并结算 BL-1/BL-2/BL-3/BL-4/E15/E16/E17/E22/E23。

---

## 六、修正结果（2026-09-20 核验修正轮）

> ⛔ 本轮**只改文档 + 清 `.ai-tmp`，未改任何游戏代码**。

| 问题 | 处置 | 落点 |
|---|---|---|
| **P1** 验收表滞后于代码 | **就地更正**：第 2 行改为"**只建 2 项** SINGLE PLAYER/EXIT（用户点名删中间两项）"；第 1/2 行补登 `by clover-engine` 署名；第 43 行补出口 3 格内陆格证据；第 44 行补"尺寸固定 80×80"证据；新增相机/走跑/帧率现行数据；TOTAL_HOSTS 记 11 | `策划/验收表.md`（第 1/2/43/44 行）+ `策划/自审对比/启动链路对照.md` §① 与删除项证据表 |
| **P2** 4 个未登记轮次 | **补登记**：agent-38 / agent-40a / agent-41 / pass5 各一行（做法 + mtime + 证据）；并登记**引擎侧 09-20 07:24 变更**（E-core-17 补 / E-core-19） | `策划/验收表.md`「2026-09-20 核验修正轮」§P2 + `tools/ai-skill/constraints.md` E 编号表（新增 E-core-17 / E-core-19 两行） |
| **P3** 联络图与索引不一致 | **重写索引**：`p8_contact_combat.index.tsv` 由 8 行 → **与图一致的 5 行（A1–A5）**；表内 3 处"8 格"改为"5 格 A1~A5" | 该 index 文件 + `策划/验收表.md` |
| **P4** 红项靠自裁 | **如实登记**（不追认合规）：标注 4 条 `# adjudicated:` 为执行方自裁、只算"已说明理由" | `策划/验收表.md` §P4 |
| **P5** play 超预算 | 登记（18 > 8，事后补记），不追认合规 | `策划/验收表.md` §P5 |
| **P6** 临时文件未清 | **已清理**：`.ai-tmp/test/` **69 → 9** 个文件；pass5 联络图**归位**到 `client/Assets/Screenshots/p41_contact.png` + `p41_contact.index.tsv` | `.ai-tmp/test/` + `client/Assets/Screenshots/` |
| **P7** 宿主数漂移 | 记 **11**（新增 `dircheck`） | `策划/验收表.md` |

**复跑（改动后）**：`tools/verify.ps1` = **`FAIL=0` / `HUMAN-ONLY=7`**（25 PASS）；`table-rows 47`、`table-summary 47 pass / 0 mismatch`、`path-reachability 86/86 全在`、`compile=up_to_date`、`consoleErrors=0`、`offline-hosts TOTAL_HOSTS=11 FAILED=0`。
`freeze-before-capture` 现以**最新批次（pass5，`09-20 01:08`）为锚点**，报"仅 1 个实现文件其后被改"= 引擎 `Sound.cs`（`07:24`，E-core-17 补，属引擎侧且已登记）。

**仍未消除（如实留档）**：`策划/验收表.md`「2026-09-20 核验修正轮」§P2 的**待复采 1 项** —— pass5 联络图 `p41_contact.index.tsv` 的 **G3 / N8** 两格 `script_verdict=FAIL`（**脚本判据 vs 实际值的口径问题，非画面缺陷**；修它需重跑一次 Play）。

---

## 七、2026-09-20 第二轮核验（**R1 五片 + 一次证据批**）

> 起因：用户本轮报 **6 条**问题（蓝条占位 / 桥上走不上 / 点人物变形 / 移动抖动 / 对话 UI 乱 / 名字输入粘连）。
> R1-A 先把**判据层救活**（离线宿主全编不过 ⇒ 一切"自动化判据"当时都是死的），R1-B~R1-F 五片修缺陷，
> R1-E2 **一次进 Play 批量采** 25 张实机瓦片 + 1207 行冻结日志 + 1 张联络图，再逐条判定。
> ⛔ 本节所有结论的落点 = `策划/验收表.md`「2026-09-20 R1 收口轮」；缺陷逐条 = `策划/自审对比/bug清单.md` B25~B30。

### 7.1 开工基线 → 收工状态（**FAIL 数 + 每项原因**）

| 时刻 | `tools/verify.ps1` | `run_all_hosts.ps1` |
|---|---|---|
| **开工**（R1-A 动手前） | **FAIL = 8** | **FAILED = 9**（`（一次性判据脚本，2026-09-24 清理）` XML 非法 + 8 个 csproj HintPath 少一级 ⇒ 宿主全编不过） |
| **收工**（本轮末次实跑，原文见 §7.6） | **FAIL = 2 / HUMAN-ONLY = 8** | **`TOTAL_HOSTS=11 FAILED=0`** |

**收工的 2 条 FAIL —— 逐条说明：**

| FAIL 项 | 原因（可复跑核对） | 本轮能否消解 |
|---|---|---|
| `path-reachability:reference-pictures` | `原版资源/参考图` 被 `.gitignore` **明确排除**（体积 + 版权）⇒ 干净检出里必然没有它；本机实测 `Test-Path` = False ⇒ **结构性不可满足**。本轮已按 §1.11 第 4 条把理由写进 `.ai-tmp/test/dispatch-log.tsv` 的 `# adjudicated: path-reachability:reference-pictures` 行，**但该检查没有裁定分支** —— `tools/verify.ps1:293-294` 的 `else` 直接 `Fail`（与其余 20 余项不同，它们都能走 `HumanOnly + adjudicated:`）⇒ **那行裁定目前是"写了但不生效"** | ⛔ **不能**（要让它生效 = 改 `tools/verify.ps1` 这个**闸门本身** ⇒ 属主 agent 的归口，执行方不许动闸门来消红）。**建议改法（1 行）**：把 `:294` 的 `Fail` 包成 `if (Adjudicated 'path-reachability:reference-pictures') { HumanOnly ... } else { Fail ... }` |
| `freshness:u2-rows` | 该检查**写死文件名** `p8_contact_combat.png`（U-2 攻击三件套联络图），而它属**迁移前批次**、随 `.ai-tmp/`（gitignore）在本机**已丢失**（实测全工作区搜 0 命中）⇒ 同样**结构性不可满足**。⛔ 本轮**没有**用 `# adjudicated:` 把它说成合规 —— 它**保留为 FAIL**，并在 `策划/验收表.md`「R1-4 结构性阻塞」如实登记 | ⛔ **不能**（只有两条路：① 用 `tools/probes/drivers/` 的驱动**重采**那条链；② 把判据改成"引用的物证清单"而不是写死单个旧文件名） |

**收工的 8 条 HUMAN-ONLY（逐条原因）**：`freshness:u1-rows`（U-1 的两份探针原文 `p8_keys.txt` / `p8_click.txt` 同属迁移前批次、已丢失；早在引擎下沉轮 adjudicated）、`freshness-global`（25/27 张图早于最新源码 `19:23:13`；按 §1.13 **逐行判**，受影响行 = R1 那几行，已各自重采）、`engine-selfname`（199 个源文件提到引擎名，需人读一遍确认无自造别名）、`d2codec:panels-pixels`（panels 组 A 条未解析出统计行 ⇒ 需人看 B 条，见 BL-12）、`original-asset-md5`（`原版资源/` 不在位，无可比对对）、`skill-family-consistency`（项目级 skill 与 `docs/**` 的候选行 **0 条** = 没有放宽全局规则的行）、`play-budget`（**23 > 预算 8**；本轮 4 次都是"修驱动 / 串流程"的诊断轮，已 adjudicated）、`freeze-before-capture`（**锚点 19:08:46 之后有 1 个实现文件被改** —— 是 **R1-F** 的 `UI/CharCreatePanel.cs`（`19:23:13`）；⚠️ 该检查当前显示的 `-- adjudicated:` 理由是**引擎能力下沉轮（A1~A5）的旧理由**，与本次真实原因无关 ⇒ **理由陈旧、需主 agent 换掉或删除该行**，见 §7.5 末条）。

**本轮把闸门的哪些红真正消掉了**：

- `path-reachability`：**86/86 missing → 25 条引用全可达（0 missing）**。口径 = §1.13「只重采受影响的行」：
  R1 影响的 10 行 → 换成本轮重采的现行证据（`a01~a16` + 联络图格号）；未受影响的行 → 明确写"证据属迁移前批次、随 `.ai-tmp` 丢失"＋**重判入口**（`.ai-tmp/screenshots/r1_evidence_r1.txt` 同口径重跑驱动）。⛔ 没有拿"同题另一次"的图冒充原引用。
- `allow-diff-registry`：25 → **31 条**例外行（新增 **E32~E37**），全部四要素齐全。
- `table-rows` / `table-summary` / `table-category` 保持 **47 行 / 47 pass / 0 mismatch**。
- `offline-hosts`：`FAILED=9 → TOTAL_HOSTS=11 FAILED=0`（R1-A 修完判据层）。
- `evidence-economy`：`visual rows = 23, sheet cells = 18, loose png = 1 / sheet png = 26`（**一次联络图**，没有逐项截图）。

### 7.2 6 条用户投诉逐条判定（照 R1 裁决记录）

| 投诉 | 判定 | 判据（本轮实机证据） |
|---|---|---|
| **1 蓝条** | **已消除** | `a01_town_wide.png`（整关）/ `a02_town_bridge.png`（河·桥近景）：硬边纯蓝长条消失；日志 `[R1-B] … 跳过 49 格`；`mapcheck`"桥面 20 格全可走 / 栏杆 20 格全阻挡 / 出生点→河东岸"全绿 |
| **1 桥上走** | **已修好** | `a03a` / `a03b`：真鼠标点桥面 (47,27) ⇒ 1.74s 后 `playerGrid=(47,27) isBridgeDeck=1`；`a04`：点水面 (47,30) `Rock` ⇒ 落到 (46,30) `Dirt`（`[R1-B]` 回退日志） |
| **2 变形** | **已修好** | 167 帧逐帧 `rect/native` 两轴**恒 = 1.800**（`PFRAME` 118 行 + `PORTRAIT` 4 条）；`a06a` / `a06b` 过渡中帧人形比例正常 |
| **3 输入粘连** | **已修好** | 修前 `a08_name_digits.png`（格 A8）= `HeroAma65x` ↔ **修后 `a17_name_fixed.png`（格 A17）**：`default=Hero` / `typed=Ama77x` / `buffer=Ama77x` / `visible=Ama77x\|` / **`defaultInBuffer=0` / `glued=0`**（默认名不再粘进缓冲）；离线 `uicheck` 第 ⑱ 节 |
| **4 抖动** | **已修好** | `a09_walk_1..6`：六帧位移按 `Δt` 折算 = 3.02/3.00/3.00/2.99/3.00 格/秒（契约 3.0，无跳变）；`playercheck` 新断言：落格帧位移 ≤ `speed·dt`（旧口径 2.28×、40 帧超限 → 修后 **0 帧**） |
| **5 对话 UI** | **已修好**（1 项待原版截图核对） | `a12`：`shopOpen=1 dialogOpen=1 shopLayer=Popup dialogLayer=Normal`；`a13`：关商店后 `shopOpen=0 dialogOpen=1 currentNpcId=0` 且选项仍在；`a14`：点面板按钮 `moveCmdDelta=0 gridChanged=0`（不再乱走）；`a15`：商店标题 `阿卡拉` + 提示 `买入` 实测在位（`titleScreen=960,861` / `hintScreen=960,831`，落在"页签带底沿 335.7 ~ 格区顶沿 277.2"的空带内） |

### 7.3 每个执行者的根因链（一条一句话）

| 片 | 根因（一句话） |
|---|---|
| **R1-A** | 判据层**整体失效**：宿主的 `（一次性判据脚本，2026-09-24 清理）` XML 非法 + 8 个 csproj 的 `UnityEngine.UI` HintPath 少一级 + 5 处宿主的仓库根写死在迁移前位置 + buildcheck 产物路径停在旧目录 ⇒ 离线自检全编不过，"自动判据"在最需要它的那段时间里是**死的**。 |
| **R1-B** | 蓝条 = `river.dt1` 里**唯一**的平色 wall 瓦片 `moor_river/028` 被静态渲染（原版靠 `ACT1/Pal.PL2` 调色板循环成水面动画，本工程无运行期循环）；桥走不上 = `MoveTo` 对不可走目标**直接拒绝**，而桥栏杆与桥面相邻 1 格。 |
| **R1-C** | `ShowTransitionFrame` **只换 sprite 不动矩形**，而过渡帧原生尺寸最多差 45% ⇒ 非等比拉伸；附带 `UiArt.SetSprite` 的异步回调无守卫（迟到回调覆盖终态）。 |
| **R1-D** | `PlayerMotor` 到路点先**吸附格心却不扣运动预算** ⇒ 落格帧位移达 `speed·dt` 的 2.28×（872 帧里 40 帧超上限）；另帧节奏不确定（全工程未设 `targetFrameRate`，逐档 `vSyncCount` 不同）。 |
| **R1-E** | S1 商店与对话同层 `Popup` ⇒ 引擎 `CloseMutexPanels()` 把对话 `Destroy`；S2 点 UI 没有被读成"点地面"之外的第二判据（缺 UI 命中判定）⇒ 乱走；S3 面板被销毁时不发 `DialogClose`；S4 双订阅；S5 标题/提示行从未创建；S6 选项列压在雕花方槽上；S7 `OnOpen` 漏参复用旧台词。 |
| **R1-F** | 默认名 `Hero` 与用户输入**共用一个字符串**，首个键入字符追加在插入点 ⇒ `HeroAma65x`（粘连）。 |

### 7.4 主 agent 读联络图后的结论

- 联络图 = `.ai-tmp/screenshots/r1_contact.png`（索引 `r1_contact.index.tsv`，格 **A1~A16**，含多格位），**逐格读过**：
  - **A1 / A2**（营地整关 / 河·桥近景）：**硬边纯蓝长条已消失**，河面是深色水/岸石瓦片，木桥与围栏正常 ⇒ 投诉 1 的"蓝条"是**画面层**缺陷、不是数据层（逐格键与可走性一格未动）。
  - **A3 / A4**：真点击链两条分支都取到 —— 点桥面 ⇒ 走上去（`isBridgeDeck=1`）；点水面 ⇒ 回退到岸上并留 `[R1-B]` 日志 ⇒ 投诉 1 的"桥上走不上"是**输入判定**缺陷。
  - **A5 / A6 / A7**（待机 / 过渡中帧 / 落正面）：三态人形比例都正常，过渡中帧的 `rect/native` 两轴都是 1.800 ⇒ 投诉 2 的"变形"确实是**矩形没跟着帧走**。
  - **A8 / A17**：`A8` = **修前**（`NAME: HeroAma65x|`）、`A17` = **修后**（`NAME-COMPARE … buffer="Ama77x" glued=0`）—— 联络图里这一对就是投诉 3 的**修前/修后**对照（A17 与索引是 R1-F 在 19:30 补进联络图的）。
  - **A9**（六帧连续行走）：六帧位移折算恒 ≈ 3.0 格/秒 ⇒ 投诉 4 的"抖动"不是"速度不对"，而是**落格帧那一跳**（2.28×）。
  - **A10~A16**（对话 / 商店 / 面板按钮 / 标题提示 / 任务日志）：`shopOpen/dialogOpen/shopLayer/dialogLayer`、`moveCmdDelta`、`title/hint` 四组数值齐 ⇒ 投诉 5 的 7 条里，**能在屏上看见的 4 条**（S1 并存 / S2 不乱走 / S5 两行在位 / S6 列位）都已实机自证；剩下 S3 / S4 / S7 是**状态层**，由 `uicheck` 第 ⑬ 节与 `[R1-E] S<n>` 日志覆盖。
- **结论**：6 条投诉里 **5 条判"已修好"**、**1 条判"已消除"**（蓝条）。其中"**输入粘连**"的修后实机证据来得最晚 —— R1-F 的 `a17_name_fixed.png` 与更新后的联络图/索引到 **19:30** 才落盘（格 **A17**）⇒ 该条是**最后补上**的，不是一开始就有；本报告据此把它从"待补"改成"已修好"。

### 7.5 仍未消除项（**如实留档，不美化**）

**A. 原版素材缺席导致的三条（根子是同一条）**

| # | 仍未消除项 | 为什么（缺口在哪） | 谁能给 |
|---|---|---|---|
| **1** | **没有原版实机截图对照** ⇒ 六维里"同机位像素级并排比对"**做不了**，`策划/验收表.md` 里一大批"一致（推导）"只能靠**原版素材帧 + prefab 矩形 + 逐值换算**自证 | `原版资源/` 被 `.gitignore` 明确排除（体积 + 版权）；本机 `Test-Path 原版资源/参考图` = False | **用户**（提供原版截图 / 可运行的原版） |
| **2** | **没有运行期 PL2 调色板循环** ⇒ 河面只能"不叠那张平色瓦片"（**E32**），拿不到原版的**水面动画** | 引擎侧无此能力；本项目也未做 | 主 agent 裁决是否开片做"运行期调色板循环"（做完 E32 可清空白名单，逻辑零改动） |
| **3** | **没有 DC6 逐帧 offset** ⇒ 创角过渡的**中间帧锚点位置**只能是两端线性插值（**E33**；矩形本身逐帧正确） | 原版 DC6 的逐帧 offset 随 `原版资源/` 缺席 | **用户**（原版素材到位后按逐帧 offset 重生成表） |

> 这三条一到位，连带可结算：E15 / E16 / E17 / E22 / E23 / E32 / E33 / E34 / E35 / E37、BL-1~BL-4、以及 R2/R3 两个基准缺口。

**B. 本轮新暴露、**不是**"素材缺席"的一条（**裁定通道**本身有问题）**

| # | 仍未消除项 | 实情 |
|---|---|---|
| **4** | **两条结构性红的"裁定通道"本身有问题** | ① `path-reachability:reference-pictures`：`# adjudicated:` 行**写了但该检查没有裁定分支** —— `tools/verify.ps1:293-294` 的 `else` 直接 `Fail`（与其余 20 余项不同，它们都能走 `HumanOnly + adjudicated:`）⇒ 该行目前**是"写了但不生效"**。改法 = 把那两行包成 `if (Adjudicated 'path-reachability:reference-pictures') { HumanOnly … } else { Fail … }`（**1 行**，属**主 agent 的闸门归口**，执行方不许动闸门消红）。② `freeze-before-capture` 显示的裁定理由是**引擎下沉轮（A1~A5）的旧理由**，而本次真实原因是 **R1-F 在证据锚点（`19:08:46`）之后改了 `UI/CharCreatePanel.cs`（`19:23:13`）** —— 理由**陈旧且误导**，需主 agent 换掉或删除那一行（受影响的只有第 4 行"创角名字"，R1-F 已自采 `a17_name_fixed.png`）。③ `freshness:u2-rows` **保留为 FAIL**（⛔ 不用裁定把它说成合规）。 |

> 已消除的旧条目：R1-F 的"修后"实机证据曾在本次落盘中途缺席，**19:30 已补齐**（`a17_name_fixed.png` + 联络图格 A17 + `r1_evidence_r1_plus_r1f.txt`）⇒ 不再列为未消除项。

### 7.6 本轮末次实跑原文（机械闸门，可复跑）

```
powershell -NoProfile -ExecutionPolicy Bypass -File tools/verify.ps1
PASS        table-rows  47 rows
PASS        table-category  every row tagged 数值类/表现类
PASS        table-summary  (body: 47 pass / 0 mismatch ; summary text agrees)
PASS        allow-diff-registry  31 registered exception row(s), all complete
PASS        path-reachability  25 screenshot reference(s), all present
FAIL        path-reachability:reference-pictures  missing ...\原版资源\参考图
FAIL        freshness:u2-rows  missing p8_contact_combat.png
HUMAN-ONLY  play-budget  play sessions = 23 > budget 8 ... -- adjudicated: 本轮 4 次进 Play 均为修驱动/串流程的诊断轮（理由逐行在 play-log.tsv），非"逐项截图"
HUMAN-ONLY  freeze-before-capture  1 impl file(s) changed AFTER capture start 09-20 19:08:46 => evidence is stale
PASS        offline-hosts  TOTAL_HOSTS=11 FAILED=0
PASS        compile  recompile_status = up_to_date
PASS        console-errors  groundTruth.consoleErrors = 0
PASS        evidence-economy  visual rows = 23, sheet cells = 18, loose png = 1 / sheet png = 26
===== summary: FAIL=2  HUMAN-ONLY=8 =====
```

另：`dotnet run --project tools/probes/hosts/dircheck -c Release` = **`DIRCHECK: ALL PASS`（exit 0）**；
`dotnet run --project tools/probes/hosts/corecheck -c Release` = **`CoreCheck 结束：全部通过`（exit 0）**。

---

## 八、2026-09-21 第三轮核验（**全量重取证**）

> 起因：交付契约 `策划/验收表.md` 的 **47 行系统清单**里，**40 行**的证据随**迁移前**的 `.ai-tmp/` 丢失
> （行里写着"属迁移前批次、随 `.ai-tmp` 丢失…需重判时重跑驱动"）。
> ⛔ 本轮**只改文档**（`策划/验收表.md`、`策划/核验报告.md`），**未改任何 `client/**` 或 `tools/**`**（含 `tools/verify.ps1`）。

### 8.1 开工基线 → 收工状态

| 时刻 | `tools/verify.ps1` | 离线宿主 | 证据层 |
|---|---|---|---|
| **开工** | **FAIL = 8** | **9/11 挂** | **40 行无活证据**（128 处指针指向"迁移前批次丢失"） |
| **收工** | **FAIL = 0**（见 8.5 原文） | 以 `run_all_hosts.ps1` 实时结果为准（见 8.5） | **0 行**无活证据：128 处指针**全部**改成本轮在盘证据（`属迁移前批次` / `丢失` 在本表内命中 **0**） |

**收工逐项说明（8 项 FAIL 各自的消解路径）**：

| 原 FAIL 项 | 消解方式 |
|---|---|
| `table-rows` / `table-category` | 补登轮次与例外时**保持表体恰 47 行**（新增的 47 行「本轮活证据指针一览」用 `1 ·` 前缀 ⇒ 不落进 `^\|\s*\d+\s*\|`；新增「本轮未测」表初稿误用 `\| 1 \|` 触发过一次行数 52 ⇒ **已就地改成 `1 ·`**，复跑 47 ✓） |
| `table-summary` | 每行末列都含 `通过`（含"通过（… 见本轮未测 #n）"这类**带保留的通过**）；汇总行 `通过 47` / `不一致 0` 与脚本数出来的行数一致 |
| `allow-diff-registry` | 新增 **E38 / E39 / E40 / E41** 四条（每条 5 列齐全）+ **E23 就地改口径** |
| `path-reachability` | 表内 **81 条** `screenshots/<file>` 引用逐条 `Test-Path` = **0 missing**（预检与 `verify.ps1` 两次都过）；`原版资源/参考图` 那条**结构性缺席**仍是 `# adjudicated:` ⇒ HUMAN-ONLY（材料到位自动恢复真检查） |
| `freshness:u1-rows` / `freshness:u2-rows` | 该两项的**证据名从表内该行正文解析**（不写死）：U-1 行引 `r1_evidence_r1.txt`（含 `[R1] VERDICT ok=1`，12:04:28）+ `x_evidence_run1.txt`；U-2 行引 `x_x3_atk1_a/b`、`x_x3_atk2_b`、`x_e1_hit_a/b/c/after.png` + `x_evidence_run1.txt` —— 全部在盘、且**晚于** U-1/U-2 源文件（U-1 最新源 `PlayerModule.cs` 11:57:07、U-2 最新源 `ViewModule.cs` 10:49:48；X/R1 批证据 12:03~12:04） |
| `freeze-before-capture` | ⚠️ **仍是 HUMAN-ONLY（走 `# adjudicated:`），但理由已陈旧**：闸门取"最新 `*.index.tsv` 引用到的最旧瓦片"作锚点，而本轮索引 `r1_contact.index.tsv` 里**仍列着上一批的 `a17_name_fixed.png`（09-20 19:30:21）** ⇒ 锚点被拖回 09-20 19:30:21，于是报"47 个实现文件在该时刻之后被改"。**实情**：本轮 X/R1 批的瓦片都是 2026-09-21 12:03~12:04、全仓最新 `.cs` = 11:57:12 ⇒ **12:03 之后没有任何实现文件被改**。⛔ 未改闸门、也未动索引（本轮只许改两份文档），**保留 HUMAN-ONLY 并在此如实说明**；建议主 agent 把索引里那一格的引用换成本轮瓦片（或把 `adjudicated:` 理由更新为真实原因） |
| `offline-hosts` | `run_all_hosts.ps1` 实时跑 = **`TOTAL_HOSTS=12 FAILED=0`**（见 8.5）；`w1_host_movecheck.txt` 那份**崩溃留档**已在表内「本轮未测」#4 写明，不作为判据 |

### 8.2 本轮抓到并修掉的真缺陷（17 条，每条都有前后对照实测）

| # | 缺陷 | 一句话根因 | 判据（本轮在盘证据） |
|---|---|---|---|
| W-1 | 装备槽**非等比拉伸** | 整幅 `inv_*.png` 被按槽矩形缩放 | `w3_uigame_audit.tsv` 装备槽 10 行（两轴比差 0.00%）；`w4_equip_slots.png` / `w4_equip_before_after.png` |
| W-2 | **技能树底图尺寸口径** | "拼装后整页 320×432" 与 "素材帧 256×256/64×256/…" 被当成一个量 | `w3_uigame_audit.tsv` 技能树 3 行 |
| W-3 | **受击动画一帧都没渲染** | hit 分支没接到帧请求路径 | `x_e1_hit_a/b/c/after.png` + `x_evidence_run1.txt` `[X] GRID=E1-*` |
| W-4 | 连续出手**沿用剩余帧（Replay）** | 第 2 刀复用第 1 刀剩余帧 | `x_x3_atk1_a.png` / `x_x3_atk1_b.png` / `x_x3_atk2_b.png`（`attackIndex=1→2`） |
| W-5 | `LoopOf` 把 **attack·death 当循环** | 循环判定没排除一次性动作 | `w3_anim_audit.tsv` 帧数列 |
| W-6 | **光标零消费方** | `ResPaths.Cursor` 是"定义了没人读"的常量；`Events.CursorChanged` 无订阅者 | `x_x9_cursor_default/hover.png`、`x_d1_nowalk_cursor.png`；`uicheck` ㉑ |
| W-7 | **选项/暂停底板是纯色块占位** | `UiArt.PanelBg` 纯色底 | `x_h2_options_before.png` / `x_h2_pause.png`；`w5_boxframes.png` + `w5_boxframes_zoom.png`；`tools/d2codec/assemble_boxpieces.py` |
| W-8 | **删除确认弹窗是引擎默认皮肤** | 直接调 `Game.UI.Confirm` | `x_b3_delete_confirm.png`（`D2ConfirmPanel(Clone)`） |
| W-9 | **施法不扣法力（契约缺扣蓝入口）** | 无扣蓝 API | `[X] E4-CAST … manaAfter=11/13`；契约 `Contracts.cs:1077` |
| W-10 | **小地图大色块棋盘** | 每格整格实心填色（100% 面积） | `x_c4_minimap.png` + `（一次性判据脚本，2026-09-24 清理）` + `uicheck` ⑯ |
| W-11 | 选角屏 **OK·EXIT 两槽动作搞反** | 两槽各绑了对方的动作 | `w3_uiflow_audit.tsv` CharSelect 行（"本片修正"） |
| W-12 | 名字框**默认名粘连** | 默认名与用户输入共用一个字符串 | `a08_name_digits.png`（修前）↔ `a17_name_fixed.png`（修后 `glued=0`）；`uicheck` ⑱ |
| W-13 | **河面硬边蓝条** | `river.dt1` 唯一平色 wall 瓦片被静态渲染 | `a01_town_wide.png` / `a02_town_bridge.png` |
| W-14 | **桥点不走** | `MoveTo` 对不可走目标直接拒绝 | `a03a` / `a03b`（`bridgeDeck=1`）/ `a04_water_fallback.png` |
| W-15 | **创角过渡变形** | 只换 sprite 不动矩形 | `a06a` / `a06b`（`ratioW/H=1.800`）；`uicheck` ⑰（167 帧 0 例外） |
| W-16 | **移动落格 2.28× 位移** | 到路点先吸附格心却不扣运动预算 | `a09_walk_1..6.png`（折算 3.02/3.00/3.00/2.99/3.00 格/秒）；`playercheck` §15 |
| W-17 | **商店销毁对话** | 商店与对话同为 Popup ⇒ `CloseMutexPanels()` 把对话 Destroy | `x_g1_dialog_*`（`shopLayer=Popup dialogLayer=Normal`）；`uicheck` ⑬ |

### 8.3 仍未消除项（**原版素材缺席导致，根子是一条**）

| # | 仍未消除 | 缺口在哪（实测） | 谁能给 |
|---|---|---|---|
| 1 | **无原版实机截图对照** | `原版资源/` 被 `.gitignore` 明确排除；本机 `Test-Path` = False ⇒ 六维里"同机位像素级并排比对"做不了，只能靠**原版素材帧 + prefab 矩形 + 逐值 ×1.8 换算**自证 | 用户（提供原版截图 / 可运行的原版） |
| 2 | **无运行期 PL2 调色板循环** | 河面只能"不叠那张平色瓦片"（E32）⇒ 拿不到原版的水面动画；精英怪调色板同理（E40-①） | 主 agent 裁决开片（E32 清白名单零逻辑改动）或用户给 PL2 族 |
| 3 | **无 DC6 逐帧 offset** | 创角过渡的**中间帧锚点位置**只能两端线性插值（E33；矩形本身逐帧正确） | 用户（素材到位后按逐帧 offset 重生成表） |
| 4 | **无 `ui/AUTOMAP/*.dc6` + `AutoMap.txt`** | 做不成原版"满屏叠加层 + 逐格 Cel blit" ⇒ 现行 = 右上角定尺框 + 超采样 4 点阵（**E38**） | 用户把 `d2data.mpq` 放回原版素材目录 |
| 5 | **无 `AnimData.d2`** | 原版逐动作 fps 表拿不到 ⇒ 播放速度按"帧数 + 固定 step"（**E40-③**） | 用户 |
| 6 | **无 4 态光标图** | 本批只有单帧 `Cursor.png` ⇒ 5 态统一显示普通箭头、缺口形态逐态一条 Warn（**E40-⑤**、`资源欠缺清单.md` #24） | 用户（`CURSOR/Cursor.DC6`） |
| 7 | **无 `boxpieces` 帧 offset 表** | ⇒ 窗框拼装偏移**由像素自证反推**（**已绕过**，逐像素接缝 0 不一致；E40-⑥） | 用户（或永久不消除） |

> 这 7 条一到位，连带可结算：E15 / E16 / E17 / E22 / E23 / E33 / E34 / E35 / E37 / E38 / E39 / E40、BL-1~BL-4，以及 R2 / R3 两个基准缺口。

**本轮**新增的 4 条**未测**（不是缺陷，是驱动目标不同 / 环境态，逐条已写进验收表「本轮未测 / 未消除」）：
① 第 18 行「升级」X 批未复现（`[X] CHK name=level-2 ok=0`）⇒ 证据改引 w2 批 `w2_h3_reentry.png`（`level=1->2`）；
② 第 23 行技能树**另两页**未翻页核对（X 批只 dump 当前页 10 个节点）；
③ 第 38 行任务链**整链**未在 X 批走完（`[X] CHK name=quest-done ok=0 denState=NotStarted`）⇒ 四态由读档恢复 + `fullcheck` §8 断言覆盖；
④ `w1_host_movecheck.txt` 是该宿主**崩溃留档**（`exit=-1073741205`），⛔ 不能当判据。

### 8.4 本轮文档侧改了哪些（**只改两份文档**）

- `策划/验收表.md`：① **128 处**失效指针全部重写（83 处按"旧文件名 → 本轮活证据"逐条映射、45 处"R1 已重采"标注归一）；② 新增 **「2026-09-21 第三轮：47 行全量重取证（本轮活证据指针一览）」**（47 行 × 本轮实测值）+ **「本轮未测 / 未消除」表** + **「本轮抓到并修掉的真缺陷 W-1~W-17」表** + **「本轮各轮次登记」表**（w1 离线行级复验 / w2+w4 实机 tour / w3 三份逐项审计 / w5 动画 1:1 / w5 光标+底板 / w6 确认弹窗原版化 / w6 小地图 / w7 施法扣蓝）；③ **E23 改口径**（小地图配色与画法改为「由原版 automap 家族素材实测像素派生」+ 超采样 4 的逐格点阵）；④ 新增 **E38 / E39 / E40 / E41**；⑤ 就地更正：`## 验收表（42 行）` → `（47 行）`、结论统计段加本轮口径、R1-4 的"迁移前批次随 `.ai-tmp` 丢失"行**改为已消除**。
- `策划/核验报告.md`：本节（8.1~8.5）。

### 8.5 收工复跑原文（机械闸门，可复跑）

```
powershell -NoProfile -ExecutionPolicy Bypass -File tools/verify.ps1

PASS        table-rows  47 rows
PASS        table-category  every row tagged 数值类/表现类
PASS        table-summary  (body: 47 pass / 0 mismatch ; summary text agrees)
PASS        allow-diff-registry  35 registered exception row(s), all complete
PASS        path-reachability  88 screenshot reference(s), all present
HUMAN-ONLY  path-reachability:reference-pictures  missing ...\原版资源\参考图 -- adjudicated: 原版资源/ 被 .gitignore 明确排除（体积+版权），仓库内结构性不可满足；材料到位后本项自动恢复
PASS        freshness:u2-rows  8 evidence file(s) cited by the U-2 row, all present and newer than the U-2 sources (2026-09-21 10:49:48)
PASS        freshness:u1-rows  2 evidence file(s) cited by the U-1 row, all present, verdict-bearing and newer than the U-1 sources (2026-09-21 11:57:07)
PASS        compile  recompile_status = up_to_date
PASS        console-errors  groundTruth.consoleErrors = 0
PASS        offline-hosts  TOTAL_HOSTS=12 FAILED=0
PASS        evidence-economy  visual rows = 23, sheet cells = 142, loose png = 5 / sheet png = 99
PASS        play-budget  INFO play sessions = 37（行数只作 INFO；每行列 4 都有理由）
PASS        impl-by-executor / no-handoff-docs / no-escaped-artifacts / sampler-selfcheck / reference-tables / stray-temp-files / hard-rules / direction-key-move
HUMAN-ONLY  freshness-global  53/155 screenshots older than newest source 2026-09-21 11:57:12（批次规则 ⇒ 逐行判）
HUMAN-ONLY  engine-selfname  201 source file(s) mention the engine name（逐条人读）
HUMAN-ONLY  d2codec:panels-pixels  could not parse the panels A-cond line
HUMAN-ONLY  original-asset-md5  no comparable pair found (source pack missing?)
HUMAN-ONLY  skill-family-consistency  0 candidate line(s)
HUMAN-ONLY  freeze-before-capture  47 impl file(s) changed AFTER capture start 09-20 19:30:21（**理由陈旧，见 8.1**）

===== summary: FAIL=0  HUMAN-ONLY=7 =====
```

> 7 项 HUMAN-ONLY 逐条原因：`path-reachability:reference-pictures`（`原版资源/` 结构性缺席，adjudicated）、`freshness-global`（53/155 张图早于最新源码 11:57:12；按批次规则逐行判，受影响的行本轮已重采）、`engine-selfname`、`d2codec:panels-pixels`（panels 组 A 条统计行未解析出 ⇒ 需人看 B 条，见 BL-12）、`original-asset-md5`（`原版资源/` 不在位 ⇒ 无可比对对）、`skill-family-consistency`（候选行 0 条 = 没有放宽全局规则的行）、`freeze-before-capture`（**adjudicated 理由陈旧**，见 8.1）。
> ⛔ 本轮**没有**为了消红而改 `tools/verify.ps1`、也没有改任何 `client/**`；`FAIL=0` 是**重写文档指针 + 本轮已重采的证据**带来的真绿。
