# 场景对照 —— 三个场景 vs 原版《暗黑破坏神 II》Act I（**本轮：agent-42，收口 4 条残留 + 补木桥/过渡带/NPC 依据**）

> **本轮（agent-42）按主 agent 裁决做了 4 件事**（每件都先"找依据"再动手，依据逐条写在下面）：
> 1. **E12 收口**：`GameConst.TownWidth/TownHeight` 32×32 → **56×40**（= 原版 `Levels.txt`），
>    `MapGenTown.WarnSizeContractDrift` 那条"每次都 Warn"的噪音删掉，改成**不一致直接报错拒绝生成**。
> 2. **木桥**：`OUTDOORS/bridge.dt1` 加进 `export_tiles.PACKS`（新 pack `moor_bridge`）并重导出；
>    按原版 DS1 把桥**铺进营地**（跨河那 40 格）—— 营地的河**现在能走过去**了。
> 3. **NPC 站位 = 原版坐标**（不再启发式）：依据 = 原版 `TownW1.ds1` 的 objects 层里
>    **kind=1（怪物/NPC）预设单位** + `MonPreset.txt` 的 Act 1 块（见 §2.1 与 ⑤ 的出处链）。
> 4. **过渡带**：`TownETrans`（`LvlPrest` Def=2，8×40）按**原版引擎的位置**铺到**野外关卡**
>    靠城那条边上（不是营地外扩带 —— 这是本轮查出来的原版事实，见 ⑤-③ 的出处）。

> 用户原话（经主 agent 转述）：「**场景不一致**」—— 元素对了、**布局没按原版 DS1 拼**
> （对照图 `client/Assets/Screenshots/a41_vs_town.png`：上=原版 `townW1.ds1` 合成，
> 下=实机；**上一版**实机是"散落一片、没有方形合围、没有河"）。
>
> 本轮目标（主 agent 给的唯一目标）：三个场景都改成**按原版 DS1 逐块精确拼装**，
> **罗格营地必须有方形围栏合围 + 内部物件照 DS1 + 营地外的河流（水 = 阻挡）**；
> 野外/洞穴改成"**块与块按出口拼接**"（不是逐格随机）；随机性口径 = **块的组合顺序与出现与否**；
> 重拍**同机位对照图**并逐项写 `一致` / `不一致（差在哪）`。

---

## ① 对照基准（**先说清，否则"对照"是空话**）

| 基准 | 内容 | 出处 / 复跑命令 |
|---|---|---|
| **原版生成规则表** | `Levels.txt` / `LvlPrest.txt` / `LvlSub.txt` / `LvlTypes.txt` / `LvlMaze.txt` | `python 原版资源/storm.py extract 原版资源/d2mpq/d2data.mpq 原版资源/d2raw .txt` ⇒ `原版资源/d2raw/data/global/excel/*.txt` |
| **原版地图块** | `ACT1/{TOWN,OUTDOORS,CAVES}/*.ds1` + `.dt1` | `python 原版资源/storm.py list 原版资源/d2mpq/d2data.mpq`（`.ds1 1771 / .dt1 206`） |
| **原版合成图（对照基准的上半）** | 原版素材 1:1 拼出来的图 | `python tools/d2codec/ds1.py 原版资源/d2raw/data/global/tiles/ACT1/TOWN/townW1.ds1 --compose <out.png>`（`OUTDOORS/bord1.ds1` / `CAVES/caveNSEW.ds1` 同理） |
| **原版可走性口径** | `passable = (flags[subtileIndex] & (Walk\|PlayerWalk)) == 0`（**标志置位 = 不可走**） | `原版资源/参考工程_Diablerie/.../Engine/World/WorldGrid.cs:78-84`（**参考实现出处**）；**自洽检验的判据已改挂「在盘证据」**（口径同 `策划/验收表.md:142` / `:218`）= `.ai-tmp/screenshots/w1_host_mapcheck.txt` 实读（**原文逐字照抄，文件:行**）：第 2116 行「    ✅ seed=4242：可走率 68.7% ≥35%（开阔草地 + 一圈崖壁边界带）」/ 第 2117 行「    ✅ seed=4242：可走格连通片数 = 1（障碍不该把场地切碎）」/ 第 1764 行「    ✅ seed=8919：每一格的瓦片键都来自原版 `CAVES/cave.dt1`（越界/异包 = 0 格，地砖 2156 格 / 全黑实心岩体 1594 格 / 带岩壁物件 47 格）」/ 第 1766 行「    ✅ seed=8919：可走率 34.6%（原版洞穴块实测约 35% ⇒ 是走廊网，不是一坨空地）」；⛔ **「洞穴开口方向」那一支已作废为 `BLOCKED（缺原版包 原版资源/d2dc6 + 原版资源/d2raw）`、从判据中撤下**：原 `tools/d2codec/verify_walk_flags.py` 要读 `原版资源/d2raw/data/global/tiles/ACT1/**`，本机 `原版资源/` 结构性缺席 ⇒ 无法真跑（`tools/verify.ps1` 现判 `HUMAN-ONLY d2codec:verify_walk_flags.py ... BLOCKED`），原先那句「15 个 `cave{方向}.ds1` 的开口方向」的唯一来源是**已删的一次性脚本** `flagtest.py` / `lanes.py` |
| **块的四边开口** | `OpenN/S/W/E` = 该块**边界中段有没有可走格** | `tools/d2codec/export_wild_layout.py::edge_open`（生成物 `MapGenWildLayout.Piece.OpenN/S/W/E`） |
| **实机图** | `client/Assets/Screenshots/a4x_*.png`（本轮进 Play 实拍，`capture_game_view --source camera`） | 见 ④ |
| **NPC 站位依据** | ① 原版 `TownW1.ds1` 的 **objects 层**里 `type=1`（= 怪物/NPC）单位的 `id` + `(x,y)`；② `id` 索引**该 act 的 `MonPreset.txt` 块**（`Act == act+1` 的行，按文件顺序）；③ 子格 → 格 = `÷5` | `python -c` 探针（探针见 §1 末）；参考实现 `原版资源/参考工程_Diablerie/libd2/packages/drlg/src/`：`drlg/preset.zig:488-494`（`kind` 1=怪物 2=物件）、`drlg/presettables.zig:11-20`（id → MonPreset 块）、`drlg/structs.zig:149-154`（`nPosX/nPosY` 是 sub-tile）、`lib.zig:1136`（`SUBTILES (tile*5)`） |
| **`MonPreset.txt`** | Act 1 块的前几项：`0=gheed 1=cain1 2=akara 3=chicken 4=rogue1 5=kashya 6=cow 7=warriv1 8=charsi` | `原版资源/参考工程_Diablerie/libd2/packages/data/src/excel/MonPreset.txt`（**本批 `d2raw/data/global/excel/` 里没有这张表**，那批是经典版 `d2data.mpq` 的 43 张 txt） |
| **过渡带归属依据** | 原版把 `LvlPrest`「Act 1 - Town 1 Transition E/S」（Def=2/3）铺在**野外关卡**上：`SpawnTownTransitionsAndCaves` 里 `SpawnOutdoorLevelPresetEx(pLevel, 0, 1, 2, …)`（西边界）/`(…, 0, 0, 3, …)`（北边界），调用点只对 **Act1 的 2..7 号户外关卡** | `原版资源/参考工程_Diablerie/libd2/packages/drlg/src/drlg/outdoors/OutRoom.zig:251-292`、`drlg/outdoors/ActInit.zig:74-83` |

**关键规则表读数（逐条抄，原文来自上表文件）**：

| 场景 | 原版怎么拼 | 依据行 |
|---|---|---|
| 罗格营地 | `Levels.txt`「Act 1 - Town」：`SizeX=56 SizeY=40` / `DrlgType=2`(**预设**)；`LvlPrest.txt`「Act 1 - Town 1」`Def=1 Populate=1 FillBlanks=1`：4 块 = `Act1/Town/TownN1.ds1 / TownE1 / TownS1 / TownW1` | 见 ② 2.1 的"四块关系"实测 |
| 血腥荒野 | `Levels.txt`「Wilderness 1」：`SizeX=SizeY=80` / `DrlgType=3`(**户外随机**) ⇒ 10×10 个 8 格块；`LvlPrest` 的 `Act 1 - Wild Border 1..12`（8×8）/ `Wild Cliff Border *` / `Fence Fill 1..6` / `Tree Fill`（16×16）/ `Cave Entrance`（8×8） | `MapGenWildLayout.Pieces`（60 块，逐块带原版出处） |
| 邪恶洞穴 | `Levels.txt`「Cave 1」`DrlgType=1`(**迷宫**) + `LvlMaze`「Act 1 - Cave 1」；`LvlPrest` 的 `Act 1 - Cave N/S/E/W/NS/EW/…` + `Cave Treasure 1..5` | `MapGenCaveLayout.Pieces`（块名 = `cave` + 开通方向，后缀 = 随机变体） |

---

## ② 逐项对照（6 个维度 × 3 个场景）

### 2.1 罗格营地（固定布局）—— **本轮重做**

**四个块的关系（本轮实测反推，生成器每次自己复验）**：
四块 `TownN1/E1/S1/W1` **都是同一座营地**，只是**导出时的原点不同**。
锚点 = 营地围栏环左上角，独立复验 = `BARRACKS/warp.dt1` 的 3 个标记格 ——
对齐后四块的 warp 标记格**逐格重合（4/4 块通过）**，实测偏移（相对 `TownW1`）：
`TownN1 (+14,-11)`、`TownE1 (+18,-5)`、`TownS1 (+14,+6)`、`TownW1 (0,0)`。
⇒ 合并口径 = **参考块第一、其余按 `LvlPrest` 顺序逐格补空**（`FillBlanks=1` 的语义；
⛔ 是**逐格**补空不是逐层补：逐层补会让别的块的错位墙填进参考块的空地、**把出城口堵死**）。

| 维度 | 原版 | 本项目（本轮） | 结论 |
|---|---|---|---|
| **关卡尺寸** | 56×40（`Levels.txt`） | 56×40（生成物读表得出；**`GameConst.TownWidth/Height` 本轮已同步为 56/40**，不一致时 `MapGenTown` 直接报错拒绝生成） | **一致**（E12 已消除） |
| **原版地图块** | 4 块 `TownN1/E1/S1/W1`（`LvlPrest` Def=1） | 4 块全部读取 + 对齐 + 复验；逐格取参考块内容（= 原版那一座营地）+ **File2 独有的木桥** | **一致** |
| **方形围栏合围** | 围栏环 `x∈[0,30] × y∈[11,34]`；**北/南两条横边整条合围**；西边只有**出城口 3 格**缺口；**东边朝河一侧原版就没有栅栏**（只有两端的短桩，水即边界） | 同上（逐格原版 `town/fence.dt1` 瓦片；`x=0 / x=30` 是环的两条竖边） | **一致**（`mapcheck` 断言：北/南缺口 0、西侧缺口 3 格、出城口 3/3 可走、城内连通片数 1） |
| **内部物件摆放** | 帐篷/木车/篝火/木桶/石矮墙/树：位置来自 `townW1.ds1` 的 wall 层（+ wall 层 1 的帐篷顶棚叠层 10 格） | 同（逐格取自同一 DS1，**未移动任何一格**；本轮新增 wall 层 1 的叠层） | **一致**（裁切对照图 `client/Assets/Screenshots/a41_camp_vs_ref.png`：上=原版 `townW1.ds1` 合成，下=本项目布局表按同一摆位规则渲染 —— 围栏、石矮墙走向、帐篷/木车/篝火相对位置逐块对得上） |
| **营地外的河流/水域** | `OUTDOORS/river.dt1`：河带 `x∈[30,36]`（在关卡窗口内 **320 格**），西岸贴着营地东侧；营内另有水塘（`river.dt1` 地砖） | 同上（floor 层原版河水瓦片；`KindOf('r') = TileKind.Rock` ⇒ **水 = 阻挡**） | **一致**（`mapcheck` 断言：河 320 格、**可走的水 = 0 格**；实机图上蓝岸黑水带与营东侧贴合） |
| **★ 河上木桥** | `OUTDOORS/bridge.dt1`：只有 `TownE1.ds1`（与 `TownETrans.ds1`）有 —— floor `x∈[47,56] y∈[15,18]`、wall `x∈[47,56] y∈{16,18}` ⇒ 关卡坐标 `x∈[29,38] y∈[20,23]`，横跨河带 `x∈[30,36]` | **本轮补上**：生成器给桥单开一条规则（"任何一块里有桥瓦片的格一律用桥"），桥面 `'d'` 可走 / 栏杆 `'s'` 阻挡；`mapcheck` 断言 40 格地砖 + 20 格桥面可走 + 20 格栏杆阻挡 + **能走过去到河东岸** | **一致**（本轮新增；修前那几格只有水 ⇒ 河对岸走不过去） |
| **NPC 位置**（阿卡拉/卡夏/恰西/基德/瓦瑞夫） | 原版坐标**就在 DS1 里**：objects 层的 `type=1` 预设单位（`id` → `MonPreset.txt` Act 1 块 = `0=gheed 2=akara 5=kashya 7=warriv1 8=charsi`），子格 ÷5 换成格 | **本轮改成按原版坐标摆**：阿卡拉 (24,14) / 卡夏 (15,18) / 恰西 (4,16) / 基德 (5,28) / 瓦瑞夫 (11,20)（取**参考块 TownW1** 那一份；四块里的 akara/charsi/warriv 换到关卡坐标后逐格重合，gheed/kashya 差 ≤2 格 —— 作者在各块里手调过） | **一致**（E10/B10 已消除：不再是启发式） |
| **明暗/色调** | 原版瓦片原色（`ACT1/Pal.PL2`），无滤镜 | 原版瓦片 PNG 原样、`SpriteRenderer.color = 白`、无色调乘数 | **一致** |
| **规格** | 上一版只取 `TownW1` 并裁 32×32 | 整关 56×40（**河不再被裁成 1 列**）+ 桥 | **一致** |

### 2.2 血腥荒野（随机野外）—— **本轮改成"按出口拼接"**

| 维度 | 原版 | 本项目（本轮） | 结论 |
|---|---|---|---|
| **块级拼法** | `DrlgType=3`：8 格一块的网格；边界块（`Bord*`/`StnClf*`/`clfcave*`）**朝外的边是闭的**（崖壁朝外）、沿环/朝内的边是开的 | `StampBorderRing`：**按 `OpenN/S/W/E` 四条条件在「块 × 左右翻 × 上下翻」里精确匹配**（实测 20/20 ~ 36/36 槽位**全部精确匹配**）；朝外的那条边必为「闭」 | **一致**（上一版是"每格随机挑一块再按边镜像"，**不看开口标志**） |
| **内部** | 开阔草地 + 零星树丛/木栅栏（`Fence Fill` / `Tree Fill`） | 内部填充块**只从"四边全开"的块里挑**（实测 2/2 ~ 6/6），其余内部 = 基底草地（原版也是引擎先铺满草） | **一致** |
| **散落件**（`LvlSub` Type=6：石堆/树/水洼/沼泽/杂物） | 少量随机撒 | 3~8 件，来自同一批原版块；水（`puddle/swamp`）→ 阻挡 | **一致** |
| **洞穴入口** | `Act 1 - Cave Entrance`（`CAVES/CaveDr*.ds1`） | 同（洞口格再叠一张原版 `cavedr.dt1` 的洞口瓦片） | **一致** |
| **地形瓦片选择** | `LvlTypes.txt` Id=2 的 dt1 槽表：地面 = `Act1/Town/Floor.dt1`、崖壁 = `Cliff1/Cliff2/Corner`、树丛 = `TreeGroups`… | 全部从这些 dt1 解出（`moor_cliff1/2`、`moor_corner`、`moor_puddle`、`moor_swamp` 等） | **一致** |
| **可走/阻挡** | 崖壁/树/石墙/栅栏/岩石/水 = 阻挡；草地/路 = 可走 | 逐格 `class` 码 → `Tree/Fence/Wall/Rock`（水 = `Rock`）；`mapcheck` 断言可走率 ≥35%、可走连通片数 = 1 | **一致** |
| **随机性口径** | 块的**出现与否/组合顺序**随机 | 块选择/镜像/散落件位置/路拐点全走注入 `Rng`：**同 seed ⇒ 同图，不同 seed ⇒ 不同图** | **一致**（`mapcheck` §2/§3 断言） |
| **出入口** | 城镇口 + 洞穴口 | 2 个（回城口 + 洞穴入口），都在原版边界块的**开口**上，且都真的可走 | **一致** |
| **★ 城镇接缝（过渡带）** | 原版把 `LvlPrest`「Act 1 - Town 1 Transition E」(8×40) 铺在野外**西边界**（`OutRoom.zig:251-292` 的 `SpawnOutdoorLevelPresetEx(pLevel, 0, 1, 2, …)`；调用点 `ActInit.zig:74-83` 只对 Act1 的 2..7 号户外关卡） | **本轮补上**：西边界正对回城口那 **5 槽**（= 8×40）改铺这条带 ⇒ 进出门那一段不再是崖壁，而是过渡带的草地 + 石矮墙 + 树线（`mapcheck` §11 断言）；**S 那条（56×8）登记为未接**（本项目没有"城镇在北"的接缝，见 ⑤-③ / E13） | **一致（E 带）/ 不一致（S 带，已登记）** |

### 2.3 邪恶洞穴（随机地牢）

| 维度 | 原版 | 本项目 | 结论 |
|---|---|---|---|
| **块级拼法** | `DrlgType=1` + `LvlMaze`；块名 = `cave` + **开通方向**，后缀 = 同形状的随机变体 | `MapGenCave`：随机 DFS 生成树 + 少量环路（**块的组合顺序与出现与否**随机）；块按 `DirMask ⊇ needMask` **按出口拼接**、优先取"多余开口最少"的块 | **一致** |
| **地形/物件瓦片** | 只挂 `Caves/Cave.dt1`；洞口 = `Cavedr.dt1` | 逐格原版瓦片键（含"什么都不画"的实心岩体 = 纯黑） | **一致** |
| **可走/阻挡** | 按 **5×5 subtile 的中心那一个**带不带阻挡标志 | **判据改挂「在盘证据」**（口径同 `策划/验收表.md:142` / `:218`）= `.ai-tmp/screenshots/w1_host_mapcheck.txt` 实读（**原文逐字照抄，文件:行**）：第 1764 行「    ✅ seed=8919：每一格的瓦片键都来自原版 `CAVES/cave.dt1`（越界/异包 = 0 格，地砖 2156 格 / 全黑实心岩体 1594 格 / 带岩壁物件 47 格）」/ 第 1765 行「    ✅ seed=8919：可走格是**一整片**（连通片数 = 1，走廊+房间全通）」/ 第 1766 行「    ✅ seed=8919：可走率 34.6%（原版洞穴块实测约 35% ⇒ 是走廊网，不是一坨空地）」；⛔ **「洞穴块开口方向」= `BLOCKED（缺原版包 原版资源/d2dc6 + 原版资源/d2raw）`、不作判据**（原 `tools/d2codec/verify_walk_flags.py` 缺原版包 ⇒ 无法真跑；口径同 `策划/验收表.md:218`） | **一致** |
| **明暗** | 整体偏暗；未画地砖的岩体纯黑 | 原版瓦片原样；`' '`/未解析格不铺地砖 ⇒ 纯黑 | **一致** |
| **规格** | — | 2~3 块/轴（50~75 格），块边长 25 | **一致**（契约允许范围） |

---

## ③ 本轮改了什么（不一致 → 处置）

| # | 维度 | 上一版现象 | 处置 |
|---|---|---|---|
| **A1** | 营地·规格 | 只取 `TownW1` 的营地本体并裁成 **32×32** ⇒ **营地外的河被裁成 1 列**、整关照不全 | **已修**：生成器改成读 `Levels.txt`（56×40）+ `LvlPrest.txt`（4 块），按"参考块第一 + 逐格补空"合并。`tools/d2codec/export_town_layout.py` 重写 |
| **A2** | 营地·多块拼接 | 只用了 4 块里的 1 块（B9 登记为"契约尺寸限制"） | **已修**：4 块全部读取 + 对齐（**warp 标记格 4/4 复验**）+ 合并；对齐口径与偏移写进生成物文件头 |
| **A3** | 营地·帐篷顶棚叠层 | `wall` 层第 1 层（10 格帐篷顶棚）**整层没画** | **已修**：生成器优先取 wall 层 1 的瓦片 |
| **A4** | 野外·块级拼法 | 周圈是"随机挑一块再按边镜像"，**不看开口标志** ⇒ "块与块能不能接上"没保证 | **已修**：`MapGenWilderness.StampBorderRing` 改成**按 `OpenN/S/W/E` 四条条件精确匹配**（朝外闭 + 沿环开 + 朝内开），并把审计数据交给 `mapcheck` 断言 |
| **A5** | 野外·内部 | 内部撒块可能挑到"有闭边"的块 ⇒ 场地中间出现一段四面不通的墙 | **已修**：内部只挑**四边全开**的块（实测 100%） |
| **A6** | 营地·出城口 | （隐患）"逐层补空"会让别块的错位墙堵死城门 | **已修**：逐格补空 + 出城口口径**固定取参考块的栅栏缺口**（其它块不许在那几格画东西） |
| **A7** | 营地·**木桥** | 河上那座原版木桥**没进图**（合并口径让参考块在河那几格赢，桥只有 `TownE1` 有） | **已修**（agent-42）：`bridge.dt1` 加进 `export_tiles.PACKS`（pack=`moor_bridge`，40 地砖 + 20 物件）并重导出；生成器给桥单开规则 ⇒ 桥落在关卡 `x∈[29,38] y∈[20,23]`、**河能走过去** |
| **A8** | 营地·**NPC 站位** | 5 个 NPC 是"可达区内均匀分散"的启发式（B10 登记为素材缺口） | **已修**（agent-42）：改成**原版坐标**（DS1 的 `kind=1` 预设单位 + `MonPreset` Act 1 块）⇒ E10/B10 消除 |
| **A9** | 契约常量 | `GameConst.TownWidth/TownHeight`=32×32 与生成物 56×40 不一致，每次生成打一条 Warn | **已修**（agent-42，主 agent 裁决）：常量同步为 **56/40**，Warn 删掉，改成不一致即报错拒绝生成（噪音归零） |
| **A10** | 野外·**城镇接缝** | 西边界（回城口那一段）原样铺崖壁边界块，进门就是一道崖壁 | **已修**（agent-42）：按原版把 `LvlPrest`「Act 1 - Town 1 Transition E」（8×40 = 5 槽）铺在西边界正对回城口那一段（依据见 ① 表 + ⑤-③） |

**上一轮已修、本轮回归确认仍然成立**：B1 野外边界是崖壁不是石矮墙 / B2 野外不撒"尖刺栅栏" / B3 野外用原版块 / B4 草地门限 / B5 野外有水 / B6 洞穴可走口径 / B7 洞穴不凿洞 / B8 洞穴开口车道。

---

## ④ 实机证据（**本轮进 Play 实拍 + 自己读图**）

| 图 | 内容 | 读图结论 |
|---|---|---|
| **`client/Assets/Screenshots/a4x_vs_town.png`（本轮重拍）** | **上 = 原版 DS1 合成**（`TownW1.ds1` 的营地 + `TownE1.ds1` 的木桥，1:1 原版瓦片，56×40）；**下 = 实机**罗格营地（Play 实拍，`capture_game_view --source camera`，`ortho=27` ⇒ 整关取景） | 逐项见下表 ①~⑧ |
| `client/Assets/Screenshots/a42_moor.png`（本轮重拍） | 实机血腥荒野（整关取景，`ortho=27`，72×72） | **一致（同类）**：暗绿草地 + 3 格宽土路 + 成片树丛 + 石墙残段 + 碎石堆；**西边界（画面左上那条边）不再是崖壁**，而是过渡带那片草地 + 石矮墙 + 树线（= A10 的效果），其余三边仍是崖壁边界块 |
| `client/Assets/Screenshots/a41_vs_moor.png`（上一轮，**已过期**） | 上=原版 `bord1.ds1`（野外边界块=崖壁，1:1）；下=上一轮实机血腥荒野 | 上一轮的结论；**本轮野外西边界已改（过渡带）**，故请以 `a42_moor.png` 为准 |
| `client/Assets/Screenshots/a41_vs_cave.png` | 上=原版 `caveNSEW.ds1`（洞穴四通块，1:1）；下=实机邪恶洞穴 | **一致（同类）**：原版 `cave.dt1` 地砖 + 岩壁立面 + 大片纯黑实心岩体（原版不画）；单块 vs 整关 ⇒ 只比"素材同类与形态"（本轮未改洞穴） |
| `client/Assets/Screenshots/a41_town_default.png` / `a41_camp_vs_ref.png` / `a41_town_wide.png` | 上一轮的三张营地图（默认取景 / 营地本体裁切对照 / 整关取景） | 上一轮的结论仍然成立（围栏环、帐篷/木车/篝火/水塘逐块对得上）；**但都拍摄于"补桥之前"** ⇒ 请以 `a4x_vs_town.png` 为准 |

### ①~⑧ 逐项读图（`a4x_vs_town.png`，上图 vs 下图）

| # | 维度 | 结论 |
|---|---|---|
| ① | 关卡范围与取景 | **一致**：两图覆盖同一块 56×40（上图按 DS1 合成、下图整关取景）；四周背景色不同（上=合成画布深灰 / 下=关卡外的黑）**不算不一致** —— 那是画布，不是地图内容 |
| ② | 方形围栏合围 | **一致**：北/南两条横边**整条**是木栅栏；西边**只有 3 格缺口**（出城口，画面左下那条边）；**东边朝河一侧没有栅栏**（水即边界） |
| ③ | 营内物件摆放 | **一致**：帐篷、木车、篝火、石矮墙、水塘、树丛的**相对位置逐块对得上**（同一批原版瓦片、同一格坐标） |
| ④ | 营地外的河 | **一致**：河带位置/宽度对得上（蓝岸 + 黑水），两图都是同一段原版 `river.dt1` |
| ⑤ | **河上木桥**（本轮新增） | **一致**：桥横跨河带、位置与走向都对得上（上图=原版 `bridge.dt1` 瓦片、下图=实机同一批瓦片：桥面可走、两侧栏杆） |
| ⑥ | 土路 / 泥土地面 | **一致**：营内土路与泥土块的走向一致 |
| ⑦ | 明暗 / 色调 | **一致**：原版瓦片原色（`ACT1/Pal.PL2`），两边都没有滤镜/色调乘数 |
| ⑧ | NPC 站位（阿卡拉/卡夏/恰西/基德/瓦瑞夫） | **本图无法判定**（合成图只画 floor/wall 层、不含单位；实机的 NPC 小人太小）⇒ 该项证据 = **坐标断言**（见 §① 的出处链 + `mapcheck` §10：5 个坐标逐条等于原版值） |

> ⚠️ **口径提醒**：上图是"原版 DS1 合成"（160×80 瓦片、等距缩放 1:1），下图是实机（128×64 瓦片 + 相机取景 + 光源），
> 两者**投影尺寸不同**（缩放到同宽后单格 ~25px vs ~24.5px，接近但非逐像素）⇒ 只能比"元素、相对位置、素材同类"，
> **不能**拿它做逐像素叠加判定。逐像素的事由生成器/`mapcheck` 的**逐格瓦片键**断言负责。

> **取景口径（取证用，不改游戏逻辑）**：整关对照图是把跟随相机的 `ortho` 临时放大到 28
> （`CameraRig._ortho` 反射改；整关世界范围 `x∈[-39,55] y∈[0,47]` ⇒ 半高 23.5、半宽 47 ⇒
> `ortho≈26.4`，取 28 留边），拍完改回默认 6。默认取景图 = 游戏原样（原版不缩放）。
> 世界内容一律 `capture_game_view --source camera`（不含 Screen Space Overlay 的 HUD）。

**本轮实机日志行（`client/Logs/Editor.log`，可 grep `MapGenTown` / `过渡带` / `[A42]`）**：

```
3383327: [Map] MapGenTown: 原版罗格营地布局已铺（源 TownN1.ds1, TownE1.ds1, TownS1.ds1, TownW1.ds1
         关卡尺寸 56x40（出处 Levels.txt Name=Act 1 - Town 的 SizeX/SizeY））：
         栅栏 95 / 摊位·帐篷 62 / 树 133 / 石矮墙 78 / 水·河 277 / 泥土 217 / 草地 1375 /
         出城口 3 / NPC 5 / 出生点 (15, 23)      ← 本轮（08:32 那次同批）：石矮墙 58→78、水 309→277
                                                   = 桥那 40 格（20 栏杆 + 20 桥面）换掉了水；**没有尺寸漂移 Warn**
   （同次）[Map] MapView.ShowArea: area=Town(罗格营地) size=56x40 分块=否 迷雾=关 块数=36
3387218: [Map] MapGenWilderness: 周圈 27/27 块原版边界块，**按四边开口拼接**…精确匹配 27/27 块；
         另有 5 槽（西边界 x 带 0、槽 0..4）改铺**原版罗格营地过渡带**「Act 1 - Town 1
         Transition E」(8×40，出处 LvlPrest Def=2) ⇒ 接缝不再是崖壁而是营地外那片开阔地
3387625: [Map] MapView.ShowArea: area=BloodMoor(血腥荒野) size=72x72 分块=是 迷雾=关 块数=12
   [A42]  app=True flow=True 站点=Boot → 已请求 GoStage(Town)；站点=Stage，CameraRig._ortho 6 → 27（整关取景）
```

生成器自检（可复跑，`python tools/d2codec/export_town_layout.py --debug`）：
四块对齐复验 4/4 通过、围栏环北/南缺口 0、西侧缺口 3 格（= 出城口）、水 256 格（`kind=='r'`）、
"有 kind 但取不到原版瓦片的格 = 0"、逐格渲染 0 张 PNG 缺失。

---

## ⑤ 仍未做到"一致"的项（**列全，别挑**）

1. ~~`GameConst.TownWidth/TownHeight` 仍是旧的 32×32~~ ⇒ **已消除（agent-42）**：常量已同步为
   **56/40**（= 生成物 = 原版 `Levels.txt`），`WarnSizeContractDrift` 与其"每次都 Warn"的噪音
   一起删掉，改成**不一致 ⇒ 报错拒绝生成**（`Module/Map/MapGenTown.cs`）。
2. ~~5 个 NPC 的站位不是原版坐标~~ ⇒ **已消除（agent-42）**：改成**原版坐标**（出处链见 §① 与 §2.1）。
3. ~~营地 4 块里的过渡块（`TownETrans` / `TownSTrans`）没接~~ ⇒ **部分接了（agent-42）**，
   并且**纠正了上一轮对它的理解**：
   - **它不是"关卡外扩带"**：原版引擎把 `LvlPrest`「Act 1 - Town 1 Transition E/S」（Def=2/3）
     铺在**野外关卡**靠城那条边上 —— `OutRoom.zig:251-292` 的 `SpawnTownTransitionsAndCaves`
     用 `SpawnOutdoorLevelPresetEx(pLevel, 0, 1, 2, …)`（**西边界**）/ `(…, 0, 0, 3, …)`（北边界），
     调用点 `ActInit.zig:74-83` 只对 **Act1 的 2..7 号户外关卡**调用。
   - **本轮已接**：`TownETrans`（8×40 = 1 块宽 × 5 槽）铺在野外**西边界正对回城口**那 5 槽
     （`MapGenWilderness.StampBorderRing` + 生成物 `MapGenWildLayout` 的 `GroupBand` 块；
     `mapcheck` §11 断言"西边界 5 槽 = 过渡带 / 带内桥瓦片 > 0"）。依据：本项目野外与城镇
     只有**一条**相连的接缝（回城口的西边界），引擎也正是把这条带放在西边界。
   - **仍未接**：`TownSTrans`（56×8 = 7 块宽 × 1 槽）。引擎把它铺在野外的**北边界**
     （`(0, 0)` 与 `(w−7, 0)` 两处），本项目没有"城镇在北"的接缝 ⇒ **登记为允许的差异**（E13）。
   - 附带事实（写下来备查）：这两条带与**具体的那一份营地**配对 —— `TownE1` 的桥压在它自己的
     第 56 列（DS1 的 +1 共享边列）、`TownETrans` 的桥就在它自己的第 0 列 ⇒ 两条带共享那一列；
     `TownS1` 的围栏环南边压在它自己的第 40 行（+1 共享边行）、`TownSTrans` 的第 0 行**正是**
     那一排同宽的栅栏（31 格）⇒ 共享那一行。
4. ~~`TownE1` 的河上木桥没进图~~ ⇒ **已消除（agent-42）**：`bridge.dt1` 进了 `export_tiles.PACKS`
   （pack=`moor_bridge`），生成器给桥单开规则 ⇒ 桥落在关卡 `x∈[29,38] y∈[20,23]`，
   **桥面可走、栏杆阻挡、能过河**（`mapcheck` §10 四条断言）。
5. **野外的随机是"块级"的近似（允许的差异，本轮登记清楚 —— 主 agent 裁决第 4 条）**。
   **本项目实际口径**（`Module/Map/MapGenWilderness.cs` + `tools/d2codec/export_wild_layout.py`）：
   | 环节 | 本项目怎么做 | 引擎（`DrlgType=3`）真实过程 |
   |---|---|---|
   | 场地尺寸 | 每轴 6..10 个 8 格块（48..80 格），`rng.Next` 抽 | `Levels.txt` 固定 80×80（10×10 块） |
   | 基底 | 每格一张原版 `TOWN/floor.dt1` 草地瓦片（随机变体） | 引擎先按 lvltype 铺满地板 |
   | 周圈 | 4·cells−4 个槽位，每槽在 **42 块原版边界/崖壁块 × 左右翻 × 上下翻**里按 `OpenN/S/W/E` **四条件精确匹配**（朝外闭 + 沿环开 + 朝内开），蓄水池抽样；匹配不到 ⇒ 放宽为"朝外闭"+ Warn | `AddAct124SecondaryBorder` 按 lvltype 的 dt1mask 铺崖壁，再 `SpawnOutdoorLevelPresets` 按每格的 junction/出入口标志 + **seed 抽样**放边界预设（`OutPlace.SpawnOutdoorLevelPresetEx`） |
   | 内部 | 目标覆盖 **35%**（`FillCoverage=0.35`，写死）⇒ `want = max(2, round(interior×0.35/4))`，在内部格随机撒 16×16/16×8/8×16/8×8 的原版填充块，**只挑四边全开的块**、不覆盖纯可走格 | 内部按 `LvlSub` 的**替换组**逐格抽样（每组有 `Dt1Mask` + 密度/概率），是**逐格**随机而不是块级 |
   | 散落件 | `LvlSub` Type=6 的 6 种块里随机撒 **3..8 件**（叠加盖章） | 同样是 `LvlSub Type=6`，但逐格概率与组顺序由引擎的替换表决定 |
   | 土路 | 回城口 →（**拐一次**）→ 洞穴口，3 格宽，铺原版泥土瓦片 | `BuildExitPointArray` + `BuildVertexPathsWithJitter`（出口点之间**走真正的寻路** + 抖动） |
   | 洞口 | 洞穴口那一"行"块换成 `Act 1 - Cave Entrance`（`CAVES/CaveDr1/2.ds1`） | `SpawnTownTransitionsAndCaves` 里的 `SpawnPresetFarAway`（**放在离城镇最远处**） |
   | 可复现 | 块选择/镜像/散落件位置/路拐点全走注入 `Rng`：**同 seed ⇒ 同图** | 引擎走 `pLevel.sSeed`，同样按 seed 可复现 |
   ⇒ **差距的准确表述**：**逐格瓦片是原版的**（每格地面/物件键都来自原版 ds1），**块的选择策略是近似的**
   （"按开口拼接"的确定性规则 + 固定覆盖率，而引擎是"逐格替换组 + seed 抽样"）。
   要 1:1 复刻引擎的 `DrlgType=3` 需要把 `d2-drlg` 的户外生成器整段移植过来（那是独立的一揽子工程）。
   本项**登记在** `策划/验收表.md` 的「允许的差异」**E14**。
6. **`export_pieces.py` 仍是死工具**（B12，上一轮遗留，不属本轮范围）。

---

## ⑥ R1-B 轮补登：河面（用户报的「奇怪的蓝条图片占位」）处置 + 差异登记（2026-09-20）

> 范围：`Module/Map/MapView.cs`（wall 层**平色**水墙瓦片**白名单不叠**）+ `Module/Player/PlayerModule.cs`（点不可走格 ⇒ **最近可走格回退** ≤2 格）。
> ⛔ **逐格瓦片键与可走性判定一格未动**（营地仍是原版 56×40 的逐格键、河仍是阻挡、桥面仍可走）。

### 6.1 现象 → 根因（实测，不是推断）

| 项 | 值 | 判据 |
|---|---|---|
| "蓝条"到底是什么 | `Objects/moor_river/028` —— `river.dt1` 的 **wall 层纯色瓦片** | `（一次性判据脚本，2026-09-24 清理）` 扫**全部 295 个被引用键** ⇒ **唯一色数 = 1 的平色瓦片只有这一个**（RGBA(0,32,68)），铺在河带 x=47/54 共 **49 格** |
| 为什么静态看像占位 | 原版靠 `ACT1/Pal.PL2` 的**调色板循环**把这张平色瓦片变成水面动画；本工程**没有运行期调色板循环** ⇒ 静态 = 硬边平色块 | 同上脚本（平色 = 像素无变化） |
| 排除「Exit 亮青占位」 | 取不到文件的键 = **0** ⇒ `GroundColor(Exit)` 亮青 (0,217,255) 在营地**无处触发** | 同上脚本第 ③ 项 |
| 排除「半格错位」 | 逐格键与 tile 中心对齐逐格核对无偏 | 同上 |
| 桥为什么"走不上" | `PlayerModule.MoveTo` 对**不可走**目标**直接拒绝**，而桥栏杆与桥面**相邻 1 格** ⇒ 点到栏杆整次点击作废（视觉误点） | 实机 `[R1-B]` 回退日志 + `playercheck::Step4_Unreachable` / `Step4b_BridgeWaterFallback` |

### 6.2 处置

| 处置 | 口径 | 为什么这样 |
|---|---|---|
| **wall 层平色水瓦片不叠** | `MapView` 内一张**只含 1 个键**的白名单（`moor_river/028`）；命中即跳过壁层叠图，floor 水瓦片照旧铺 | 引擎无运行期调色板循环 ⇒ 该瓦片静态渲染**必然**是硬边平色块（观感等同占位）；登记 **E32** —— 做出运行期循环后**清空白名单即可，逻辑零改动** |
| **点不可走格 ⇒ 最近可走格回退** | 仅「图内但不可走」回退，半径 ≤ 2 格（Chebyshev；按 ① 离点击点最近 → ② 离角色最近 → ③ (dx,dy) 升序 **确定性**取格）；**图外一律拒绝**；半径内无可走格 / 走不到 ⇒ 仍按不可达拒绝 | 原版 D2「点不可走处 ⇒ 走向最近的可走处」；桥栏杆紧邻桥面，误点概率高 |

### 6.3 实机证据（本轮一次进 Play 批量采；联络图格 A1~A4）

| 格 | 证据 | 读到的值 |
|---|---|---|
| A1 | `.ai-tmp/screenshots/a01_town_wide.png` | 整关营地（probe ortho 28）：`map=56x40`、`exits=[(17,26),(17,27),(17,28)]`、`npcPoints=5`；**硬边纯蓝长条消失**，河面 = 同 `river.dt1` 的深色水 / 岸石瓦片 |
| A2 | `.ai-tmp/screenshots/a02_town_bridge.png` | 默认取景（`rigOrtho=camOrtho=3.75`、`pxPerWorldUnit=144`）：**木桥横跨河带**、与围栏正常 |
| A3 | `.ai-tmp/screenshots/a03a_bridge_click_050.png` / `a03b_bridge_click_150.png` | 真鼠标点桥面 (47,27)：`moveCmdDelta=1` → 0.5s `(45,27)` → 1.5s `(47,27)`（`isBridgeDeck=1`；1.74s 到达） |
| A4 | `.ai-tmp/screenshots/a04_water_fallback.png` | 点水面 (47,30) `Rock`（不可走）⇒ `[R1-B] 点到不可走格 ⇒ 最近可走格回退（生效）：点击 (47,30) 地形=Rock ⇒ 落到 (46,30) 地形=Dirt，路径 6 格` |

### 6.4 差异登记（本轮新增）

- **E32**｜河面 wall 层纯色水瓦片（49 格）不叠 —— 「是什么 / 为什么必须这样（实测证据）/ 出处 / 何时能消除」四要素见 `策划/验收表.md`「允许的差异」**E32** 行。
- 本项**不影响**营地的「关卡尺寸 / 方形围栏合围 / 内部物件 / 营地外的河 / 木桥 / NPC 站位 / 明暗」七维结论：
  那些逐格键与可走性**一格未动**，`tools/probes/hosts/mapcheck` §10 / §15 全绿（含"桥面 20 格全可走 /
  栏杆 20 格全阻挡 / 出生点能走到河东岸"与"平色水墙瓦片不叠：白名单恰 1 键 / 命中 49 格全在 x=47,54 /
  这 49 格仍是水=阻挡"）。

---

## ⑦ 2026-09-21 全量重取证轮：场景复核（**本轮未改动任何场景代码**）

> ⛔ **先说清范围**：本轮（全量重取证）**没有动场景相关的任何文件** ——
> `Module/Map/**`（除 R1-B 的 wall 层白名单，见 §⑥）、`Module/Player/PlayerModule.MoveTo`（R1-B 的最近可走格回退）
> 与生成器 `tools/d2codec/export_*.py` **一格未改**。故本节**不新增任何场景差异登记**，
> 只把「河面处置」与「野外随机是块级近似」的**现行口径**做一次复核引用，并补本轮重采的实机整关取景。

| 项 | 现行口径（复核引用） | 本轮复核证据 |
|---|---|---|
| **河面处置** | **E32**｜河带 wall 层的**平色水瓦片不叠**（白名单恰 1 键 = `moor_river/028`，命中 **49 格**，全在 x=47/54，这 49 格**仍是水=阻挡**）；floor 水瓦片照旧。**逐格键 / 可走性一格未动** —— 细则见本文件 §⑥ | 实机 `x_contact.index.tsv` 格 `C1-wide`：`map=56x40 seed=2143926554 spawn=(32,28) walkable=1528 blocked=712 exits=[(17,26),(17,27),(17,28)] npcPoints=5`（图 `x_c1_town_wide.png` —— 河面无硬边纯蓝长条）；离线 `mapcheck` §15 全绿（白名单恰 1 键 / 命中 49 格全在 x=47,54 / 这 49 格仍是水=阻挡） |
| **野外随机的现状 = "块级"近似** | **E14**｜**逐格瓦片是原版的**（每格地面/物件键都来自原版 ds1），**块的选择策略是近似的**（"按四边开口精确拼接"的确定性规则 + 固定覆盖率 `FillCoverage=0.35`，而引擎 `DrlgType=3` 是"逐格替换组 + seed 抽样"）。差距的完整对照表见本文件 **§⑤.5** | 实机 `x_contact.index.tsv` 格 `C2-wide`：`area=BloodMoor map=80x80 seed=517111064 exits=[(0,60),(79,36)] cave=(79,36) monstersAlive=46`（图 `x_c2_moor_wide.png`）；同批格 `C3-wide`：`area=DenOfEvil map=50x50 seed=807650041 exits=[(1,6)] monstersAlive=6`（图 `x_c3_den_wide.png`，本轮洞穴亦未改） |

> **结论**：本轮场景侧 = **复核通过、口径不变**（营地的「关卡尺寸 / 方形围栏合围 / 内部物件 / 营地外的河 / 木桥 / NPC 站位 / 明暗」七维结论、以及洞穴五维结论全部保持，见 §②）。
> 本轮 `bug清单.md` 里两条与场景沾边的缺陷（**B43** 河面 = R1-B 的 B25/E32、**B44** 点桥 = R1-B 的 B26）都是 **R1 轮已修、本轮复核**，**不是本轮新改**。
