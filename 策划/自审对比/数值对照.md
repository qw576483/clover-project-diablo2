# 数值对照 · 战斗伤害 + 怪物数值（片 6）

> **本文件 = 片 6 的核心产物**：把 `Module/Combat/**` 与 `Module/Monster/MonsterTuning.cs`
> 里的**每条常量 / 公式**与**原版出处**逐行对照。
> 出处只有三种合法形态：**原版数据表 `文件:行`** / **参考实现 `文件:行`** / **本项目新增（必须登记【例外】）**。
> ⛔ **"看起来合理"不是出处**；写不出出处的**不许进工程**。

## 0. 权威载体在哪（本片真正用到的）

| 编号 | 载体 | 绝对路径 | 覆盖什么 |
|---|---|---|---|
| **T1** | **LoD 1.10 官方 txt（含 `SkillCalc.txt` / `MonLvl.txt` / `MonStats2.txt`）** | `原版资源/参考工程_Diablerie/d2lod1.10txt/data/global/excel/*.txt` | 技能公式串、怪物全字段、按等级倍率 |
| **T2** | **本批 d2raw 解包（98 张，经典版）** | `原版资源/d2raw/data/global/excel/*.txt` | 交叉核对（同名的 `skills.txt`/`monstats.txt`） |
| **R1** | **参考实现（社区复刻 libd2，Zig）** | `原版资源/参考工程_Diablerie/libd2/packages/game/src/*.zig` + `packages/drlg/src/drlg/*.zig` | 引擎函数的**整数口径逐行移植**（注释里带 RVA） |
| **P1** | 本项目代码 | `client/Assets/Scripts/**` | 现状 |

### 0.1 ⚠️ 三条**任务书前提的更正**（先说清，免得后续片又按错的前提找）

| 任务书原文 | 实况（可复跑） |
|---|---|
| `d2raw/data/global/excel/` 里有 **98 张** txt，含 `monstats.txt`/`monlvl.txt`/`monstats2.txt`/`SkillCalc.txt` | **98 张是对的一半**：`d2raw` 的 excel 目录下只有 **55 张**，另 43 张在 `data/local/{font,LNG,UI,docs}`（合计 98）。**`SkillCalc.txt` / `MonLvl.txt` / `MonStats2.txt` 三张在 `d2raw` 里都不存在**（实测 `cmd /c for %f in (…) do @if exist`）。本批 d2raw 是**经典版**表：`monstats.txt` 是 **229 列 / 411 行**、每只怪的 HP/AC/AR/伤害带 `(N)/(H)` 内联列，且 `skills.txt` 只有 `Param1..6`、没有 `EMinLev1..5` |
| `DamageFormula` 注释「待官方 `SkillCalc.txt` 导入后替换」= 一个**错的期待** | **反面**：`SkillCalc.txt` **一直都在磁盘上** —— 在 **T1**（参考工程自带的 1.10 官方 txt，98 张全套：`SkillCalc.txt`/`MonLvl.txt`/`MonStats2.txt`/`MonUMod.txt`… 全在）。那些注释里"官方未公开/未导入"的判断是**因为没往参考工程里找**。⇒ 本片把它用起来了 |
| `monster_c` 的换算依据（`docs/配表说明.md` §4 写的是 `_assets_tmp\d2src\d2lod1.10txt\…`） | 该目录**已不存在**（工作区里没有 `_assets_tmp`）⇒ 那份"1.10 官方 txt"的唯一现存副本就是 **T1**。**已登记 BL-6**（下节） |

---

## 1. 逐行对照表（**每条被改动的常量 / 公式**）

| # | 公式 / 常量 | 我们现在的值（文件:行） | 原版值（出处） | 怎么改 | 状态 |
|---|---|---|---|---|---|
| A1 | **命中率** | `DamageFormula.cs::HitChance` —— 浮点式 `2×ALvl/(ALvl+DLvl) × AR/(AR+DR)`，算完再夹 5%~95% | 引擎 `DAMAGE_RollAttackHit @0057d9b0`；逐行移植 **R1 `combat.zig:72-108`**：负数交叉；`pct = ar*100/(def+ar)`（**整数截断**）；`chance = pct*2*alvl/(alvl+dlvl)`（**整数截断**）；`<6⇒5`、`>94⇒95` | 改成官方**整数两步截断**口径 | **已改** |
| A2 | **物理伤害** | `PhysicalDamage` —— `RoundToInt(roll × (1+力量/100) × 技能倍率)`（**四舍五入**） | 引擎 `DAMAGE_CalculatePhysicalDamage @0057b420`；**R1 `combat.zig:149-180`**：`dmg% = str*StrBonus/100 + dex*DexBonus/100 + 技能ED%`（整数截断）；`out = base + base*dmg%/100`（官方 `D2ApplyPercent` = **截断**） | 改成 `武器 + 武器×dmg%/100` 整数截断；`StrBonus` 取官方 `Weapons.txt::StrBonus` 的近战值 **100** | **已改**（逐武器 `StrBonus/DexBonus` 见 **E25**） |
| A3 | **抗性结算** | `ApplyResist` = `ClampResist(-100,75)` 后 `FloorToInt(raw×(100-res)/100)` | `DAMAGE_ApplyElementalDamageWithResist @0057bf80`；**R1 `spell.zig:252-264`**：`resist>=100 ⇒ 0`（免疫），否则 `trunc(raw×(100-res)/100)`。**怪物抗性没有上限**：**R1 `combat.zig:299-313`** 原文 "players cap at 50 … **MONSTERS have NO cap**" | 删掉对**怪物**的 75% 夹取；加"≥100 = 免疫" | **已改** |
| A4 | **致命一击** | `25% + 3%×(slvl-1)`，上限 75%（注释自认「本项目新增的线性近似」） | **T1 `SkillCalc.txt` 第 4 行**：`dm12 = ((110*lvl)*(b-a))/(100*(lvl+6)) + a`；**T1 `skills.txt`「Critical Strike」行** `passivecalc1 = dm12`、`Param1=5`、`Param2=80`（**T2 经典版同一行也是 5/80**）；引擎 `SKILLS_CalcDiminishingReturns @00645b20` → **R1 `calc.zig:33-36`**（整数序 + 上限 = b）；用例 **R1 `skills_amazon.zig:123-131`**（1/5/20 级 = **16/42/68**） | 换成官方 `dm12(5,80)`；**去掉 75% 上限**（官方只夹到 Param2=80） | **已改** |
| A5 | **技能伤害等级缩放** | `dmg_min × slvl .. dmg_max × slvl`（线性，注释自认近似） | 引擎 `SKILLS_GetDamage`：**分段** `a = EMin + EMinLev1×(l-1)`（8/16/22/28 换档）再 `<<HitShift>>8` —— **R1 `spell.zig:88-116`**（`staged`） | **改不动**：`skill_c` 只有 1 级值（`phys_min/elem_min/hit_shift`），**没有每级增量列**（配表列名契约冻结）⇒ 保持线性 | **未改，登记 E26** |
| A6 | **技能法力消耗** | `mana + lvlmana×(slvl-1)` | **T1 `skills.txt`** 的 `mana`/`lvlmana`（`skill_c.mana_cost/mana_per_lvl`） | 无需改（已合官方） | 通过 |
| A7 | **空手伤害 1~2** | `UnarmedMin/MaxDamage` | 官方 fists（原版未装备武器时 1~2） | 无需改 | 通过 |
| A8 | 命中率上下限 5%/95% | `GameConst.Min/MaxHitChance` | 官方夹取阈值见 A1（`<6⇒5`、`>94⇒95`） | 语义已对齐；新增 `HitChanceClampLow/HighThreshold` 常量 | **已改** |
| A9 | `mag`（魔法）伤害无对应系 | `DamageTypeOf("mag") ⇒ Physical` + Warn | **T1 `MonStats.txt`** 有独立的 `ResMa`（`monster_c.res_magic` 也导了） | 需给 `Def.DamageType` 加"魔法"系 = **改契约** ⇒ 登记 **E27** | **未改，登记 E27** |

### 1.1 怪物数值（`monster_c`）—— 逐条复算（可复跑的探针）

复算口径（引擎 `MONSTER_CalculateLevelScaledStats @006538a0`；**R1 `montable.zig:256-286`** + `drlg/monpop.zig:399-411`）：

```
level   = 区域等级（= Levels.txt MonLvl1；本项目 level_c.area_level = 1）
AC      = MonLvl.AC[level] × MonStats.AC     / 100      (整数截断)
AR      = MonLvl.TH[level] × MonStats.A1TH   / 100
dmg_min = MonLvl.DM[level] × MonStats.A1MinD / 100
dmg_max = MonLvl.DM[level] × MonStats.A1MaxD / 100
exp     = MonLvl.XP[level] × MonStats.Exp    / 100
hp_min  = MonLvl.HP[level] × MonStats.minHP  / 100
hp_max  = MonLvl.HP[level] × MonStats.maxHP  / 100
```
`MonLvl` 第 1 行（**T1 `MonLvl.txt`**，Normal 列）= `AC 6 / TH 8 / HP 7 / DM 2 / XP 30`（第 0 行全为 1）。

**复算结果：64 项里 51 项逐值相等，13 项不一致 —— 13 项全部只差"取整口径"**：

| 怪（官方 id） | 列 | 表值 | 官方（截断） | 差异来源 |
|---|---|---|---|---|
| 堕落萨满 fallenshaman1 | dmg_min | 1 | **0** | `2×34/100 = 0.68`：表用四舍五入 |
| 堕落罗格 corruptrogue1 | dmg_min | 1 | **0** | `2×34/100 = 0.68` |
| 血鹰 foulcrow2 | dmg_min / hp_max / ac | 1 / 3 / 1 | **0 / 2 / 0** | `2×41/100=0.82`、`7×41/100=2.87`、`6×14/100=0.84` |
| 尖刺鼠 quillrat1 | hp_max / hp | 6 / 4 | **5 / 3** | `7×81/100 = 5.67` |
| 僵尸 zombie1 | hp_max | 13 | **12** | `7×181/100 = 12.67` |
| 幽灵 wraith1 | hp_min / hp_max / hp / ar / exp | 6 / 12 / 9 / 8 / 28 | **5 / 11 / 8 / 7 / 27** | `7×83/100=5.81`、`7×165/100=11.55`、`8×94/100=7.52`、`30×93/100=27.9` |

- **根因**：`tools/table-convert/convert.py:495-506` 用 `rnd()`（**四舍五入**）算这 8 列；官方 `D2ApplyPercent` 是**整数截断**（`docs/配表说明.md` §4 已自认"本项目改四舍五入并在此登记"）。
- **状态**：**未改，登记 E30 + BL-7**（原因见 E30：改它必须同时改 `策划/数值文档/monster_c.xlsx`，否则打表第 ③ 步会用旧 xlsx 把 txt 覆盖回去 ⇒ 会动到其他片的数据基线）。
- **实机旁证**：本片实机跑出的 19 只怪日志里，`僵尸 hp=10 dr=5 ar=8 dmg=1-3`（`hp` 表值 10 与官方 `(7+12)/2=9`+四舍五入口径一致）⇒ 差异**确实只落在截断位上**，量级 ≤1。

---

## 2. 怪物 AI 节奏常量（`Module/Monster/MonsterTuning.cs`）

| # | 常量 | 现在的值（改前 → 改后） | 原版出处 | 状态 |
|---|---|---|---|---|
| B1 | `LogicFps` | **新增** 25 | **R1 `gameserver.zig:675`**："One `tick()` is one iteration of the real **25-fps** per-game server loop" | **已加**（换算基准） |
| B2 | `OfficialAiDelayFrames` | **新增** 15 | **T1 `MonStats.txt`** 8 只怪的 `aidel` 列全 = 15；语义 **R1 `montable.zig:50`**（"the AI think-delay (frames between AI runs)"） | **已加** |
| B3 | `RepathIntervalSeconds` | 0.40 → **0.60** | = `aidel 15 帧 ÷ 25 fps`（B1×B2） | **已改** |
| B4 | `OfficialCorpseTtlFrames` + `CorpseLifetimeSeconds` | 25 → **20** | **R1 `gameserver.zig:3604-3606`**：`CORPSE_TTL = 500` 帧，注释 "~20s at 25fps" | **已改** |
| B5 | `ShamanReviveCooldownSeconds` | 4.0 → **0.6** | = `aidel ÷ 25 fps`（官方萨满**每个 AI 思考帧**才判定一次复活；动作序与参数语义 **R1 `monai.zig:151-211`**，`aip1` = 复活/召唤概率 —— 与 **T1 `AiParms.txt`「Fallen Shaman」P1**"有尸体时复活概率"一致） | **已改** |
| B6 | `AggroMemorySeconds` | 5.0（不变） | 官方**没有**这一状态量：原版由 `aidel`（思考周期）+ `aidist`（感知距离）+ `MonStats.ai` 选定脚本共同决定；**T1 `AiParms.txt`** 8 只怪的参数段里也没有"记忆时长" | **未改，登记 E28** |
| B7 | `AttackIntervalSeconds` / `AttackAnimSeconds` / `HitStunSeconds` | 1.10 / 0.35 / 0.18（不变） | 官方出处是**动画帧数**（`MonStats.Code` 的 `.cof` 里 A1 / GH 模式的帧数 ÷ 25）—— 本批**没把 `.cof` 逐怪 A1 帧数解析成表**。⚠️ `AiParms.txt` 的 `P1`（"Pct chance to strike - set this to affect attack speed"）是**概率**、不是秒数，**不能当出处** | **未改，登记 E28** |
| B8 | `RangedKeepDistance` | 4.0（不变） | 官方是**逐 AI 的 `aipN` 且单位不同**：`AiParms.txt`「Spike Fiend」`P1` = 考虑飞弹的距离（`quillrat1.aip1 = 10`）；「Fallen Shaman」`P5` = 施放 Skill2 的距离。本项目 `monster_c` **无 `aipN` 列**且一个常量覆盖两种怪 | **未改，登记 E28** |
| B9 | `ShamanReviveRange` | 7.0（不变） | 官方 = `fallenshaman1.aip4 = 24`（**T1 `AiParms.txt`**「Fallen Shaman」段：复活扫描半径；**R1 `monai.zig:170-175`** 原文 "aip4 (revive scan radius) is applied by the host's corpse scan"）。⚠️ 单位是**世界 subtiles**，`subtile→格` 换算本片未确证 | **未改，登记 E28** |
| B10 | `CowardFleeHpRatio` | 0.35（不变） | ⛔ **与官方机制不同**：堕落者的官方逃跑触发 = **同伴尸体在 15 subtiles 内**（**R1 `monai.zig:139-149`**：`FALLEN_CORPSE_FLEE_RANGE = 15`、`fallenShouldFlee(corpseWithinRange)`，原版 `AI_Function1_Fallen` 的**无条件**逃跑标志）。按血量比例逃的是 `Conservative` AI（**T1 `AiParms.txt`**「Conservative」`P4` = "Pct health to flee"，如 Evil Spider）与 Sand Raider（`P1`）—— 本项目 8 只怪**都不是**。改触发条件要动 `MonsterAi.cs`（不在本片归口） | **未改，登记 E28** |
| B11 | `CowardFleeSeconds` / `CowardFleeDistance` / `FleeStepDistance` / `BackOffSearchRadius` | 3.0 / 6.0 / 1.5 / 3（不变） | 官方无对应量（纯本项目玩法参数） | **未改，登记 E28** |
| B12 | `SpeedToTilesPerSecond` | 1.0（不变） | 官方 `MonStats.Velocity` 的**量纲**在本批材料里查不到出处（`montable.zig` 只读 AC/TH/DM/HP/res；`ai.zig` 自称近似、**不采信**）⇒ 见 **E29 + BL-8**。旁证（**不是出处**）：`GameConst.PlayerWalkSpeed = 6.0` 与 `charstats.txt::WalkVelocity = 6` 同值 | **未改，登记 E29** |
| B13 | `ReviveHpRatio` | 0.5（不变） | 官方 txt / `AiParms.txt` 里没有这条 | **未改，登记 E28** |
| B14 | `DensityPerPack` | 200（不变） | 官方 `Levels.txt::MonDen` **不是"群数"**：**R1 `monpop.zig:23`** "The per-slot density gate (game seed % 100000 <= MonDen)"、`:460` "clamped to 10000" ⇒ 它是**逐槽抽样门槛**，要配引擎的逐槽循环才成立 | **未改，登记 E28** |
| B15 | `ElitePackFallback` | 1（不变） | 官方 = **T1 `Levels.txt`** 的 `MonUMin/MonUMax`（本项目三区域**官方值都是 0** ⇒ 按官方一次都不刷）⇒ 保底 1 群是**本项目新增** | **未改，登记 E28** |
| B16 | `MinMoveSpeed` / `MaxPacksPerSpecies` / `SpawnMinDistanceFromPlayer` / `SpawnPlaceAttempts` / `MaxElitePacks` | 0.8 / 8 / 5 / 8 / 4（不变） | **守卫/上限**参数（防配表写 0、防刷爆），官方无对应量 | **未改，登记 E28** |
| B17 | `RepathOnGoalDrift` | 1（不变） | 纯逻辑节流参数 | **未改，登记 E28** |

> **本片归口说明**：任务书只把 `Module/Combat/**` 与 `Module/Skill/SkillTuning.cs`、`Module/Monster/MonsterTuning.cs`
> 交给本片，**`MonsterAi.cs` 不在归口** ⇒ 凡"改常量还不够、必须改行为"的项（B6/B7/B8/B10）
> 一律**只登记不改**，并在 E28 里写清"何时能消除"。
> 另：**`SkillTuning.cs`（15 条换算常量）与 `NpcShop.cs`（卖价系数）按任务书留给后续片**，本文件不覆盖。

---

## 3. 实机期望 vs 实测（数值类证据，`client/Logs/*.log`）

驱动：`editor_stop → clear_console → editor_play → eval_file _dev/p_runbg.cs → eval_file .ai-tmp/test/p6_combat.cs`
（探针**只经生产入口**：`ICombatModule.RequestAttack` / `.RequestMonsterAttack` / `IMonsterModule.ApplyDamage` / `.All`；
`AppContext` 是 `internal` ⇒ 用反射取模块引用，之后一律按 public 接口调用）。

| # | 项 | 期望（按新公式手算） | 实测（日志行） | 结论 |
|---|---|---|---|---|
| E1 | 命中率（整数口径，同等级） | ALvl=1 DLvl=10 AR=DR=100 ⇒ `100/11` 截断 = **9%** | 探针 `[P6] A1 … ⇒ 9.00%`（`compilationFailed=false` 的本轮编译产物） | **一致** |
| E2 | 命中率（夹取） | AR=1/DR=100000 ⇒ 5%；AR=100000/DR=1 ⇒ 95% | `[P6] A3 ⇒ 5.00% / 95.00%` | **一致** |
| E3 | 物理伤害 | roll=10 str=100 ⇒ 20；roll=13 str=33 ⇒ `13 + 13×33/100` = **17** | `[P6] A4 ⇒ 20 / 17` | **一致** |
| E4 | 致命一击 | dm12(5,80)：1/5/20 级 = **16/42/68** | `[P6] A5 ⇒ 16% / 42% / 68%` | **一致**（与 R1 用例逐值相同） |
| E5 | 抗性（含免疫/负抗性） | raw=100：0%⇒100、50%⇒50、75%⇒25、**100%⇒0、200%⇒0**、-50%⇒150 | `[P6] A6 ⇒ 100/50/25/0/0/150` | **一致** |
| E6 | **实机命中判定** | 怪 AR=8 / 玩家 DR=6 ⇒ `8×100/14 = 57` 截断 = **57%**（旧浮点式会显示 57.1%） | `[Combat] [怪攻] m#1008 堕落者（Coward）掷=2 命中率=**57.0%** ALvl=1 DLvl=1 AR=8 DR=6 ⇒ 未命中（距离 1.41）`、`m#1009 … ⇒ 命中`、`m#1007 … ⇒ 命中` …（**命中 / 未命中两条分支都有**；全程 65 条 `[怪攻]` 行） | **一致** |
| E7 | **实机抗性分支（生产入口 `IMonsterModule.ApplyDamage`）** | 僵尸毒抗 50% / raw=13 ⇒ `13 - 13×50/100` = **6** | `[P6] C5 Poison：M#1000 僵尸 抗性=50% raw=13 ⇒ 期望实扣=6 实测实扣=6 **PASS**（hp 10→4）` | **一致** |
| E8 | **实机怪物数值** | `monster_c` 8 只怪（49 行 `[Monster] spawn` 日志） | `[P6] C2 M#1000 僵尸 lvl=1 hp=10/10 dr=5 ar=8 dmg=1-3 抗=[物0 火0 冰0 电0 毒50]`、`M#1005 堕落者 hp=3/3 dr=5 ar=8 dmg=1-2`、`M#1013 尖刺鼠 hp=4/4`、`M#1018 狂热 僵尸 hp=30/30 ar=14 dmg=2-6`（精英 ×3 血 / ×1.75 AR）—— 与 §1.1 的官方复算表**同值** | **一致** |
| E9 | `console --level error` | 0 | 本轮 `clear_console` 后 `console_status.groundTruth.consoleErrors = **0**`（`consoleWarnings=19`） | **一致** |

### 3.1 **未取到的证据（如实登记，不假装有）**

| # | 项 | 为什么没取到 | 谁能给 |
|---|---|---|---|
| **U1** | **玩家普攻（`ICombatModule.RequestAttack`）的 `[普攻]` 行** | 断言那一刻玩家格 =(15,14)、最近怪格 =(24,11) ⇒ **距离 > `GameConst.MeleeRange`(1.6)**，模块按设计"距离不够 ⇒ 不结算（先靠近）"并只打 `atk.outofrange` Warn。**注意**：`HitChance` 的实机口径已由 E6 的 `[怪攻]` 行覆盖（**同一个函数、同一条 `CombatModule` 代码路径**，只是攻守互换） | 再进一次 Play、等怪 AI 走到贴身（日志里 02:44 那批已经在 1.41 距离上互殴）后重跑探针（下一棒 5 分钟即可补） |
| **U2** | **技能施放（`ISkillModule.TryCast`）的实机伤害** | 同上：断言时刻没有已学技能 + 射程外 | 同 U1 |
| **U3** | **同一场景"连续两次进 Play 同结论"** | 本片两次进 Play 各到了一个**不同**场景（第 1 次在罗格营地 ⇒ 按配表 `mon_density=0` **0 只怪**，属预期；第 2 次在血腥荒野 ⇒ **19 只怪**、E1~E9 全 PASS） | 同 U1 |

> ⛔ 上表三项**一律不写成"通过"**：本片只声明 E1~E9 逐条实测一致；U1~U3 是**已定位、可复现**的待补项。

---

## 4. 复跑口径（照着能重跑出上面的数）

```powershell
# ① 官方基数（本片全部出处的源头）
#    T1 = 原版资源\参考工程_Diablerie\d2lod1.10txt\data\global\excel\
#    T2 = 原版资源\d2raw\data\global\excel\   （经典版，交叉核对用）
#    R1 = 原版资源\参考工程_Diablerie\libd2\packages\game\src\*.zig

# ② 怪物数值逐条复算（64 项逐值比对官方公式）
python .ai-tmp\test\verify_monster_values.py        # ⇒ "64 项，不一致 13 项"（13 项全是取整口径，见 §1.1）

# ③ 离线自检宿主（秒级，含本片新增/改写的断言）
cd .ai-tmp\hosts\combatcheck ; dotnet run -v q --nologo   # ⇒ "自检全部通过"

# ④ 实机取证
cd client
unity command editor_stop ; unity command clear_console ; unity command editor_play
unity command eval_file --file _dev/p_runbg.cs
unity command eval_file --file ..\.ai-tmp\test\p6_combat.cs
#   探针行：[P6] 前缀 → client\Logs\Editor.log
#   引擎行：[怪攻] / hurt m# / [Monster] spawn → client\Logs\2026-09-19.log
```
