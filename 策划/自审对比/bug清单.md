# 实测 bug 清单（agent-21 §B 发现；**agent-22 已修 B1~B4**）

| # | 现象 | 证据 | 归口模块 | 状态 | 修复 / 证据 |
|---|---|---|---|---|---|
| B1 | 等距相机不跟随主角 | 相机 pos 始终 (1,-15,-10)；acc_31 角色在左下 → acc_32 角色不在画面 | Module/Camera/CameraRig.cs | **已修** | `CameraRig.RefreshFocus()`：每帧按 ①契约 `Follow(transform)` ②主角 `IPlayerModule.World` 刷新焦点（原先焦点只在进图时设一次、`_target` 存了不读）。修后实测 `camPos=(1,-15,-10)→(-3,-6.5,-10)`，主角屏幕 `(600,1620)→(960,855)` 回到画面内（`_dev/a22_verify_out.txt`，两张 Play 一致） |
| B2 | 创角屏无法输入角色名 | 注入 A/C/C 后文本仍 'Hero' | UI/CharCreatePanel.cs | **已修** | **实测根因**：`ProjectSettings.asset` `activeInputHandler: 1`（只用新 Input System）⇒ uGUI `InputField` 走 `BaseInput → UnityEngine.Input`，直接抛 `InvalidOperationException`（见 `_dev/a22_b2b_out.txt`）。改由面板自己从 `Keyboard.onTextInput` 收字符（`#if ENABLE_INPUT_SYSTEM && !ENABLE_LEGACY_INPUT_MANAGER` 包裹，改成 Both 即自动交回 uGUI）。修后实测 `'Hero'→''→'acc'→'accAma'→退格×2 'accA'`，且 `T20 角色名='accA'` |
| B3 | 技能树格子空白灰块，无图标/名称 | 图 acc_44_skilltree.png | UI/SkillTreePanel.cs | **已修** | 三条根因：① `D2Icon` 用 `class_c.Code`（`Amazon`）拼文件名，素材是 `amaSkillicon_*`（3 字母码在 `skill_class` 列）⇒ 30 节点全「贴图缺失」；② 同档并列技能只用了 `slotRow`、x 全相同 ⇒ 完全重合；③ 技能名被**下一档**格子盖住。修后实测 `节点=30 无图标=0 无名字=0 重复格=0`，图 `Screenshots/a22_11_skilltree.png` |
| B4 | 小地图面板标题与信息文字重叠 | 图 acc_40_minimap_on.png | UI/MiniMapPanel.cs | **已修** | 标题 rect 顶边跑出画布 4px + 与图例只隔 4px ⇒ 两行压字、右端还越框。改为**表头带**（`HeaderH=46`）：两行各占一行、右端与地图框右沿对齐、不换行。修后实测 `Title 屏幕 y 0..24 / Legend 26..46`，图 `Screenshots/a22_12_minimap.png` |

> 修复详情（根因链、代码位置、日志原文）见 `策划/验收表.md` 末节「本轮修掉的 bug」。
> 本轮另发现并记入验收表的两点（非 B1~B4，属"缺实机取证"而非代码缺陷）：见 `README.md` §⑤。

> **编号说明**：本表 B 编号**自成一系**（B1~B4 = agent-21 发现 / agent-22 修复）。下表 **B25~B30** 按主 agent 的
> R1 收口裁决记录编号（6 条用户投诉 → 6 条缺陷）。⚠️ 与 `策划/验收表.md` 的 B 系列编号**不通用**
> （那边 B25/B26 是另一件事：a52 轮的字面 markdown / 启动屏 logo 路径）。

---

## 2026-09-20 R1 收口轮：用户 6 条投诉 → 6 条缺陷（B25~B30）

> 每条 = 现象 / 根因（`文件:行`）/ 修复 / 证据。全部**先离线取证、再一次进 Play 批量采实机证据**（§1.13 四拍批次）。
> 实机证据的原文 = `.ai-tmp/screenshots/r1_evidence_r1.txt`（联络图 `.ai-tmp/screenshots/r1_contact.png` +
> 索引 `r1_contact.index.tsv`）；量法/驱动 = `（一次性判据脚本，2026-09-24 清理）` / `（一次性判据脚本，2026-09-24 清理）`、
> `（一次性判据脚本，2026-09-24 清理）`。

| # | 现象（用户投诉） | 根因（文件:行） | 修复 | 证据 |
|---|---|---|---|---|
| **B25** | 河面上有**硬边纯蓝长条**（"奇怪的蓝条图片占位"） | `Module/Map/MapView.cs`：河带那些格子的 **wall 层**叠的是 `Objects/moor_river/028` —— `river.dt1` 里**唯一**的平色瓦片（RGBA(0,32,68)）。原版靠 `ACT1/Pal.PL2` 的**调色板循环**把它变成水面动画，本工程**没有运行期调色板循环** ⇒ 静态 = 硬边平色块 | `Module/Map/MapView.cs:732` 起新增白名单 `PaletteCycledFlatWallTiles` + 判定 `IsPaletteCycledFlatWallOverlay`（命中即**不叠**该 wall 瓦片；floor 水瓦片照旧）。**逐格键 / 可走性一格未动** | 量法 `（一次性判据脚本，2026-09-24 清理）`：扫 295 个被引用键，**平色只有这一个**、命中 **49 格**（x=47/54）；取不到文件的键 = **0**（排除 Exit 亮青占位）。离线 `mapcheck::Step15_FlatWaterWallNotOverlaid`（白名单恰 1 键 / 命中 49 格全在 x=47,54 / 这 49 格仍是水=阻挡）。实机 `.ai-tmp/screenshots/a01_town_wide.png` + `a02_town_bridge.png`（蓝条消失） |
| **B26** | **点木桥走不上**（"为什么不是从桥上走？"） | `Module/Player/PlayerModule.cs:455 MoveTo`：目标格**不可走就整次点击直接拒绝**（只 Warn 一行、角色不动）；而**桥栏杆与桥面相邻 1 格** ⇒ 点到栏杆那次点击完全作废 | `MoveTo` 加"最近可走格回退"（`:496-539`）：半径 `MoveFallbackRadius = 2`（`:105`）内按 ①离点击点最近 → ②离角色最近 → ③(dx,dy) 升序 **确定性**取格；**图外 / `TileKind.Void` 一律直接拒绝**（`:100-101`）；生效口径只报一次 `[R1-B]`（`:509-516`） | 实机 `.ai-tmp/screenshots/a03a_bridge_click_050.png` / `a03b_bridge_click_150.png`（真点桥面 (47,27) ⇒ `isBridgeDeck=1`，1.74s 到位）；`a04_water_fallback.png`（点水面 (47,30) `Rock` ⇒ 落 `(46,30)` `Dirt`，路径 6 格 + `[R1-B]` 日志）；离线 `playercheck::Step4_Unreachable` / `Step4b_BridgeWaterFallback` |
| **B27** | **点人物（创角）后人物变形** | `UI/CharCreatePanel.cs:1158 ShowTransitionFrame`：过渡逐帧播放时**只换 sprite、矩形沿用帧 0 那一套**；而过渡帧原生尺寸最多差 **45%**（例 amazon `fw_0` 118×198 → `fw_21` 215×228）⇒ 非等比拉伸。另：过渡帧未预热；`UI/UiArt.cs` 的 `SetSprite` 异步回调**无守卫**（迟到的旧回调会覆盖终态） | ① 逐帧矩形表 = `tools/probes/gen_portrait_frame_table.py` 读每张 PNG 的 IHDR 宽高生成、**就地重写** `UI/UiLayoutFlow.cs` 的 `ClassMenu.Transition`（167 帧 = Amazon 54+30 / Barbarian 64+19）② 过渡帧预热 ③ `UI/UiArt.cs:254-292` 加**请求守卫**（同一 `Image` 只有最新一次请求的回调会落地；`:284` 报一次生效口径） | 实机 `PORTRAIT` 行逐帧 `rect/native` 两轴**恒 = 1.800**（`PFRAME` 118 行 + `PORTRAIT` 4 条）；中帧 `.ai-tmp/screenshots/a06a_amazon_transition.png`（`fw_21` 215×228 → 387.0×410.4）/ 落正面 `a07a_amazon_front.png`；离线 `uicheck` 第 ⑰ 节（回读 PNG 逐帧比对，**167 帧 0 例外**） |
| **B28** | **人物移动抖动**（一顿一顿） | ① `Module/Player/PlayerMotor.cs:195-218`：到路点时先**吸附到格心**却**不扣运动预算** ⇒ 落格那一帧的位移 = 白送量 + 预算(`speed×dt`)，最多达 **2× speed×dt**（实测 872 帧里 **40 帧超上限**，最大 **2.28×**；`dt=1/60` 时预算 0.05 < eps 0.08 ⇒ 每过一个路点必走这条分支）② 帧节奏不确定：全工程**从未**设 `Application.targetFrameRate`，而 `ProjectSettings/QualitySettings.asset` 逐档 `vSyncCount` 不同（VeryLow/Low/Ultra = 0 不封顶；Medium/High/VeryHigh = 1）⇒ 选 LOW/MED 时 dt 随机抖 | ① `PlayerMotor` 新积分口径：**能走到格心就走到并同步扣预算**（位移恒 ≤ `speed×dt`；`:206-218`）② 新增 `Core/FramePacing.cs`：`targetFrameRate=60` + `vSyncCount=0`（与画质档位无关），随 `Bootstrap` 与**每次改画质后**重钉（非预期分支各只报一次 Warn） | 实机 `.ai-tmp/screenshots/a09_walk_1..6.png`：六帧位移按 `Δt` 折算 = **3.02/3.00/3.00/2.99/3.00 格/秒**（契约 3.0，无跳变）；离线 `playercheck::Step15_Jitter`（逐帧位移上界 / 变 dt 终点一致 / 相机低通 / 帧节奏 / 重铺合并）—— 修后**落格帧超限 = 0 帧** |
| **B29** | **关掉商店后对话没了** + **点对话面板按钮角色乱走**（"人物对话时 UI 逻辑乱七八糟"） | **S1** 商店与对话条同为 `Popup` 层 ⇒ 引擎 `UIManager.Open<Popup>` 对同层其它面板调 `CloseMutexPanels()`（**= `Destroy`，且不发关闭事件**）⇒ 开商店把对话条**销毁**（`UI/NpcDialogPanel.cs:271`）；**S2** 点 UI 被**同时读成"点地面"**（没有引擎 UI 命中判定）⇒ 角色乱走、还会顶掉商店；**S3** 面板被引擎销毁时不发 `DialogClose` ⇒ `_currentNpcId` 残留、"凭空再弹对话"；**S4** 面板与模块**双订阅** ⇒ 一次打开刷新两遍；**S5** 商店**标题行/提示行从未创建**；**S6** 菜单项**压在原版雕花方槽上**；**S7** `OnOpen` 漏参时**复用上一次台词** | ① 对话条降 `Normal`（商店留 `Popup`，`UI/NpcDialogPanel.cs:271`）② `Module/Input/InputReader.cs:198 UiEatsIntent(pressed, pointerOverUi)` 纯函数 + `:85 PointerOverUi`（默认源 = `UiPointerProbe.PointerOverUi`，取不到则降级 + Warn 一次）③ `NpcDialogPanel.OnClose` 补发 `Events.DialogClose`（`:335`）④ 面板不再订阅（`:289`）⑤ `UI/ShopPanel.cs:126 BuildTitleLines` 建两行，位置走 `UI/UiLayoutGame.cs:380-397`（`ShopTabBottomY`/`ShopGridTopY`/`ShopTitlePos`/`ShopHintPos`）⑥ 选项列改走两雕槽中点（`NpcDialogPanel.cs:216 OptionW=66` / `:225 OptionX=Cx(103)`）⑦ `OnOpen` 拿不到 args ⇒ **置空态 + Warn**（`:299`）；`Module/Npc/NpcModule.cs:217`/`:754` 以 `_currentNpcId != None` 作"面板开着"不变式 | 实机 `.ai-tmp/screenshots/a12_shop_and_dialog.png`（`shopOpen=1 dialogOpen=1 shopLayer=Popup dialogLayer=Normal`）/ `a13_shop_closed.png`（`shopOpen=0 dialogOpen=1 currentNpcId=0` 且选项仍在）/ `a14_dialog_button_pre.png`（点面板按钮 `moveCmdDelta=0 gridChanged=0`）/ `a15_shop_title_hint.png`（`阿卡拉` + `买入` 两行实测在位）/ `a10_dialog_not_started.png` / `a11_dialog_in_progress.png` / `a16_questlog.png`；离线 `uicheck` 第 ⑬ 节（S1~S7 逐条 + "每条都有只报一次的 `[R1-E] S<n>` 日志"） |
| **B30** | **名字输入粘连**：输入 `Ama65x` 后变 `HeroAma65x` | `UI/CharCreatePanel.cs`：名字框开屏**预填默认名** `Hero`（`Args.defaultName` ← `Cfg.DefaultPlayerName` ← `config.json:3`），而第一个键入的字符是**追加在插入点上**的 ⇒ 默认名与用户输入**共用一个字符串**、粘在一起（`:338` 处原先没有 `_nameDefaultPending`） | 把默认名当**整体单元**：① `_nameDefaultPending` 只在 `OnOpen`（默认名非空时）置位（`:272` / `:338`）② 新增纯函数 `EditNameDefault(text, caret, defaultPending, op, arg, maxLen, …)`（`:623`）：**首次键入任一有效字符 ⇒ 整个缓冲被该字符替换**，之后才是追加；**空字段上退格/Delete 无效果** ⇒ 默认名永不会剩半截 ③ 面板实际走它（`ApplyNameEdit`，`:665`） | 离线 `uicheck` 第 ⑱ 节 `CheckNameDefaultReplace`（三态 + **反向断言**：不许绕开 `EditNameDefault` 直连 `EditName`）；**旧状态留档** `.ai-tmp/screenshots/a08_name_digits.png`（格 A8，`NAME: HeroAma65x\|`）↔ **修后实机取证 `.ai-tmp/screenshots/a17_name_fixed.png`**（格 A17，2026-09-20 19:30：`default=Hero` / `typed=Ama77x` / `buffer=Ama77x` / **`defaultInBuffer=0` / `glued=0`**）⇒ 默认名不再粘进缓冲；证据批原文 `.ai-tmp/screenshots/r1_evidence_r1_plus_r1f.txt` |

---

## 2026-09-21 全量重取证轮：流程屏 / 游戏内 UI / 动画逐项审计 + 两次冷启动实机 tour（**B31~B47**）

> 本轮 = **一次全量重取证**：三份**逐项审计表**（`.ai-tmp/screenshots/w3_uiflow_audit.tsv` 流程屏 80 行 ·
> `w3_uigame_audit.tsv` 游戏内 90 行 · `w3_anim_audit.tsv` 动画 130 行 + `w3_anim_trigger.tsv` 触发点表）
> ＋ **两次冷启动实机 tour**（`.ai-tmp/screenshots/x_contact.index.tsv` 联络图索引 81 格 ·
> `x_evidence_run1.txt` 逐格原始行）。每条 = **现象 / 根因（`文件:行`）/ 修复 / 证据**。
> **离线判据宿主**：`tools/probes/hosts/uicheck`（流程屏 = `W3FlowCheck.cs` / 游戏内 = `W3GameCheck.cs`）、
> `tools/probes/hosts/animcheck`（`Program.cs` §1~§6）。
>
> ⚠️ **编号说明**：本表编号**自成一系**。**B31~B41 = 本轮新发现并修掉的 11 条**；
> **B42~B47 = R1 轮的 B30 / B25 / B26 / B27 / B28 / B29 六条在本轮「全量重取证」中被逐条复核**，
> 复核行**不重复计数缺陷**（与既有编号指同一处修复），只补「本轮重采证据」。
> ⛔ 与 `策划/验收表.md` 的 B 系列编号**不通用**（那边 B34/B35 是另一件事）。

| # | 现象 | 根因（文件:行） | 修复 | 证据 |
|---|---|---|---|---|
| **B31** | 装备栏底图**非等比拉伸**（`inv_armor` 横竖压缩比差 **35.2%**、`inv_belt` **11.3%**） | `UI/InventoryPanel.BuildEquipFrame` 把**整幅** `inv_*.png` 塞进 prefab 节点矩形 —— 而素材**四周有透明边**（`inv_armor.png` 64×128 里不透明内容只占 (0,2)-(54,83) = **54×81**）⇒ 横 54/64 = 84.4% vs 竖 81/128 = 64.8%，**两轴压缩比不同**；双槽拼图（`inv_helm_glove` 128×64）按"半宽"裁还会**带出隔壁槽的图案**（两图形落在 x 0..53 与 56..109，中间两列是空的） | `UI/UiLayoutGame.cs:520-560` 新增**素材侧数据**（每槽 = 「本槽图形外接框」+「整幅 IHDR」两张表，逐槽实测）；`InventoryPanel` 改口径 = **裁剪框（素材外接框 ×K）+ 整幅贴图（IHDR ×K，1:1 不缩放）**，双槽按列投影分块取左/右半 | 离线 `uicheck` W3GameCheck（"声明原版尺寸 == 素材 IHDR（0 例外）"、"矩形宽高比 == 素材原生宽高比（防非等比拉伸；0 例外）"、"整幅缩放比两轴都 == K"）；审计 `w3_uigame_audit.tsv` 背包 33~42 行：**12 个装备槽逐槽**「裁剪框 == 内容框 ×K，非等比 **0.00%**」 |
| **B32** | 技能树底图的**「声明的原版尺寸」与「素材帧尺寸」被混为一谈**（审计行声明「拼装后整页 = 320×432」，却把路径指向**逐帧**目录 `D2/UI/SkillTree/skltree_a_back_0`，而那张图**原生只有 256×256**）⇒ 断言"声明 == 素材 IHDR"**必然红** | `UI/UiLayoutGame.SkillPanelSize` 把**两个不同的量**写在同一行：① **素材帧尺寸** = 原版 `SPELLS/skltree_{cls}_back.DC6` 的 16 帧，循环 **256×256 / 64×256 / 256×176 / 64×176**（4 帧一"页"tile 打包：左 256 + 右 64 = 320 宽、上 256 + 下 176 = 432 高）② **拼装后整页矩形** = **320×432**（`Panel/skltree_{cls}_back_{0..3}.png`，实测 IHDR 320×432） | `UiLayoutGame.cs:702-722` 把两个概念**拆清**写进注释与断言模型：**底图**判据走「拼装后整页」（`UI/D2Icon.SkillTreeBackPath`，`D2/UI/Panel/` 前缀），**逐帧**文件只用于**帧尺寸循环**对账；⛔ 明确「不许用 `ResPaths.SkillTreeBack`（逐帧目录）当底图」 | 离线 `uicheck` W3GameCheck（"技能树底图路径 == 运行时 `D2Icon.SkillTreeBackPath` 的拼法"、"源码 **0 命中** `ResPaths.SkillTreeBack`"、"16 个逐帧文件尺寸循环 == 256×256/64×256/256×176/64×176"、"拼装后整页 IHDR == 320×432"）；审计 `w3_uigame_audit.tsv` 技能树 59~66 行 |
| **B33** | **受击动画一帧都没渲染过** —— `PlayHit` 定义了 `Hit` 动作、素材 48~72 帧全在盘，实机上却**一帧不出**（"定义了但没人用"的最隐蔽形态） | `Module/View/ViewModule.cs` `TickPlayer` 的 `want` 三元链**没有受击档**（旧 ~`:725`）：`PlayHit`(`:523`) 设的 `Hit` 在**同一帧**被覆盖成 `Idle`，而两次贴图（Hit[0] 与随后的 Idle 帧）都发生在**渲染之前** ⇒ 该单位那 6 帧（亚马逊）**一帧都不进渲染** | 新增 `Module/View/ViewAnimState.cs`（**纯函数**，无 Unity 依赖 ⇒ 离线可逐帧驱动）；`ViewModule.TickPlayer:731-738` 改用它，优先级 **Death > Hit > Cast > Attack > (Run\|Walk) > Idle**；保持条件 = `IsHitHolding`（受击动画**还没播完**且不止一帧）⇒ ⛔ 不引入任何新时长常量（时长 = 该单位该动作真实帧数 ÷ 基准帧率） | 离线 `animcheck` **§5**（`SelectPlayer/SelectMonster` 优先级真值表）+ **§6**（真 `SpriteAnimator` 逐帧推进：旧口径反例出 **0 帧**、新口径 `hit` 播完回 `idle`）；`w3_anim_audit.tsv` / `w3_anim_trigger.tsv`（hit 触发点 `ViewAnimState.cs:44/61/75` + `ViewModule.cs:523`） |
| **B34** | **连续出手 / 连续施法 / 连续受击时沿用上一次的剩余帧** —— 看着像"没抬手就出法术""第二刀没挥" | `ViewModule.PlayAnim` 的**早退判据不比较 loop**（`want == Playing` 就直接 return）⇒ 上一刀/上一个法术还没播完时，新的一次动作被**吞掉**（只显示上次的中间帧） | `Module/View/ViewModule.cs` 三处各补一次 `Anim.Replay()`：`:875`（`OnSkillCast`）、`:905`（`OnPlayerAttacked`）、`:528`（`PlayHit`，并顺带 `ApplyFrame` 立即出帧）。⚠️ 只重开**帧号**，不动 `SpeedScale` | 离线 `animcheck` **§6**（逐帧断言：连击第二刀从第 0 帧起）；`w3_anim_trigger.tsv` 的 attack / cast / hit 三行触发点 |
| **B35** | `LoopOf` 把 **attack / death 也当循环播** ⇒ 死亡动画**永不停**（`Finished` 永不为真）、出手播完不静默 | `Module/View/SpriteFrames.cs` `LoopOf` 旧口径 = `anim != Death`（攻击/施法/受击全循环） | `Module/View/SpriteFrames.cs:166-169` 改为**只有 `Idle` / `Walk` / `Run` 循环**（原版语义：一次出手 = 一套 `A1`，播完回静止；死亡姿态停在末帧） | 离线 `animcheck` **§4**「LoopOf：只有 Idle/Walk/Run 循环（attack 播完回 idle / death 停末帧 的前提）」 |
| **B36** | **光标机制没有任何消费方**（`ResPaths.Cursor` **0 引用**、`Events.CursorChanged` **0 订阅**）—— 贴图与事件都在，光标永远不出现 | 光标**没有承载视图**：引擎 `Cursor.SetCursor` 走不了（素材 `isReadable = 0`）；硬件光标不吃 uGUI 缩放（会与整屏 ×1.8 口径不一致）⇒ 需要一层自己的画布，而**本轮之前那一层不存在** | 新增 `UI/CursorView.cs`：`[RuntimeInitializeOnLoadMethod(AfterSceneLoad)]` 建 `DontDestroyOnLoad` 的**独立画布**（`sortingOrder=1` > 引擎常驻画布 0 ⇒ 盖在所有面板上）；订阅 `Events.CursorChanged`（形态）+ `StageEntered`/`StageLeft`（何时接管）；Top 画布上跟随鼠标的 `Image` + `Cursor.visible = false`；**只在游戏内接管**（菜单保留系统光标），且**贴图没到位就不隐藏系统光标**；缺口形态**逐态一条 Warn**（⛔ 按 skill §0「A 没有就不加」**不自画 4 个光标**） | 离线 `uicheck` W3GameCheck **光标 5 条断言**（消费方存在 / 真的改光标 / 只在游戏内接管 / 素材在位 32×26 / 4 态缺口已登记）；实机 `x_contact.index.tsv` 格 `X9-default`（`cursor=Default cursorChanges=27`）/ `X9-hover`（`cursor=Attack cursorChanges=110 hover=hasTarget=1 name='堕落者'`）/ `D1-nowalk`（`cursor=NoWalk cell=(22,15) walkable=0`） |
| **B37** | 选项屏 / 暂停菜单的底板是**纯色块（占位物）** | `UI/SettingsPanel` / `UI/PausePanel` 旧版只用 `UiArt.PanelBg` 纯色底 —— 原版 `MENU/boxpieces.DC6`（22 帧窗框）**没接** | 用原版 `MENU/boxpieces.DC6` **像素自证**拼装窗框：生成器 `tools/d2codec/assemble_boxpieces.py`（单元格 pitch = **12**；角块 4 个压帧内 1..3/10..12、竖边块压 5..7 ⇒ 偏移 **左 −4 / 右 +5 / 下 +9 / 上 0**）→ `D2/UI/Panel/boxframe_settings.png` **432×348** / `boxframe_pause.png` **288×180**；`Core/ResPaths.cs:578-582` 加 `PanelBoxFrameSettings` / `PanelBoxFramePause`；`SettingsPanel.cs:298` / `PausePanel.cs:77` 各一行 `UiArt.Art`（层序 = **底色 → 窗框 → 内容**） | 离线 `uicheck` W3GameCheck（22 帧素材在位 + **偏移自证** + 边带朝向自证 + 两个整幅窗框在位且 IHDR == 面板常量 ÷1.8 + **接缝自证**（内部接缝 0 不一致 / 四条边带 0 空洞）+ 源码真的在加载窗框）；生成器自带同一套自证（exit≠0 即失败）；证据图 `.ai-tmp/screenshots/w5_boxframes.png`；审计 `w3_uigame_audit.tsv` 89 行（残留登记 `资源欠缺清单.md` #25） |
| **B38** | 选角屏 / 暂停菜单的**二次确认弹窗是引擎默认 uGUI 皮肤**（深灰方块 + 两颗纯蓝按钮），与同屏的原版石雕按钮**两种风格** | 两处都调引擎通用件 `Game.UI.Confirm(...)`（`Runtime/Presentation/UI.cs:318` → `ConfirmLayer.Build`；`UIWidgets.cs:628-649` **硬编码** `new Vector2(760,420)` 与 `new Color(0.16f,0.44f,0.78f)`）；`IUIManager.Confirm` 签名（`PresentationContracts.cs:111`）**只有文案与回调、无外观参数**，`ConfirmLayer` 是引擎包内 `internal sealed` ⇒ **项目侧无法换皮** | 新增 `UI/D2ConfirmPanel.cs`（`UIPanel`，层 **`Top`**）+ 预制体（**原版窗框** `ResPaths.PanelBoxFramePause` + **原版中等按钮三态**）；替换 `UI/CharSelectPanel.cs:211`（删除角色确认）/ `UI/PausePanel.cs:97`（回主菜单确认）；几何组 `UiLayoutFlow.Confirm`（`UiLayoutFlow.cs:1131-1179`，5 个矩形全进对照表）；`Assets/Editor/ProjectBuilder.cs:76-79` 面板清单加 `D2ConfirmPanel`（**16 → 17**） | 离线 `uicheck`（`UiLayoutFlow` 对照表 Confirm 组两两不重叠 + 乘适配系数后落在 1920×1080 内）；实机 `x_contact.index.tsv` 格 `B3-confirm`：`confirmBtn=Confirm@[UI]/Top/D2ConfirmPanel(Clone)/ScreenFit/Confirm`（图 `x_b3_delete_confirm.png`） |
| **B39** | **施法不扣法力**（`TryCast(重击): 13 → 13`） | `Module/Contracts.cs` 的 `IPlayerModule` **缺扣蓝入口** —— 只有 `RestoreMana(int)`，而实现对**非正数一律忽略** ⇒ 用 `RestoreMana(-cost)` 扣蓝被**静默钳掉** | 契约新增 `IPlayerModule.TrySpendMana(int)`（`Module/Contracts.cs:1077`）+ `PlayerModule.TrySpendMana`（`Module/Player/PlayerModule.cs:979`，≤0 或法力不足 ⇒ 返回 false 且**不改值**，⛔ 不改 `RestoreMana` 既有语义）+ `SkillModule` 改**先校验后扣蓝再施放**（`Module/Skill/SkillModule.cs:279-283` 校验法力 / `:312-313` 扣蓝 / `SpendMana:756` 实现；校验失败是 **Warn** 不是 Error） | 实机 `x_contact.index.tsv` 格 `E4-a`：`phase=cast skillId=121 ok=1 knownLevel=3 **mana=11/13**` —— `重击`(id 121) 的 `mana_cost = 2` ⇒ **13 → 11**；同批 catalog 行 `121:'重击' … mana=2` |
| **B40** | 小地图是**大色块棋盘**（饱和蓝 / 黄 / 白**实心矩形**，每格占格面积 **100%**） | 旧版 `UI/MiniMapPanel` 每个已探索格在 `Texture2D` 上**整格**填一个饱和色（1 纹理px = 1 格 ⇒ 只能整格填色） | `UI/MiniMapPanel.cs` 改口径 = **暗底 + 逐格点阵**：贴图**超采样** `SuperSample = 4`（`:111`，1 纹理px = 1/4 格 ⇒ 才画得出细线/小点）、每格只画 1 / 4 / 9 个纹理像素（占格面积 **6.25% / 25% / 56.25%**，`FloorDotPx:113` / `PaintSquare:383`）、`FilterMode.Point` 保像素风；三色由**原版 automap 横幅素材实测像素**派生（`#1C1C1C` 5.9% / `#2C2C2C` 3.8% / `#484848` 3.2%，出处 `AutoMapCenter_0.png` 实测量） | 离线 `uicheck` **⑯ 节**（把 **6.25% / 25% / 56.25%** 三个比值钉成断言）；实机 `x_contact.index.tsv` 格 `C4`（`miniMapOpen=1 markers=8 markerSprites=[mapicon_0 ×8]`，图 `x_c4_minimap.png`）；审计 `w3_uigame_audit.tsv` 小地图 77~80 行 |
| **B41** | 选角屏底部 **OK / EXIT 两槽动作搞反**（左下 EXIT 槽放的是「新建 NEW HERO」，右下 OK 槽放的是「主菜单 MAIN MENU」）—— 与原版语义**相反**，且与本工程**创角屏自相矛盾**（从选角屏点进创角屏时，左下那颗按钮从"新建"变成"返回" = 位置语义中途翻转） | `UI/CharSelectPanel.cs` 建底部两钮时把 `Create`（新建）放进了 **Exit 槽**、把 `Back`（主菜单）放进了 **Ok 槽**（未按原版 `ClassSelectMenu.prefab` 的 `ExitButton.m_Text=EXIT` / `OkButton.m_Text=OK` 语义对应） | `UI/CharSelectPanel.cs:293-312` **只把动作与文案归到原版对应的槽上**（坐标 / 尺寸 / 底图帧**一个数都没动**，仍走 `ClassMenu.ExitPos`/`OkPos` + `MediumButtonOrig`）：**Exit 槽(−540) = `MAIN MENU` → `Events.ToMainMenuRequest`**；**Ok 槽(+540) = `NEW HERO` → `Events.Fsm.TriggerNeedCreate`**；节点名随动作（`Back` 在左、`Create` 在右） | 离线 `uicheck` W3FlowCheck（"选角屏 Exit 槽放「离开本屏」/ Ok 槽放「确认/推进」" + "创角屏与选角屏底部槽位语义**一致**"）；审计 `w3_uiflow_audit.tsv` 25/26 行（"**本片修正**：修前这一槽放的是「新建」"） |
| **B42** | 创角名字框**默认名粘连**：输入 `Ama65x` 后变成 `HeroAma65x`（**= R1 的 B30，本轮复核**） | `UI/CharCreatePanel.cs` 名字框开屏**预填默认名** `Hero`，而第一个键入的字符是**追加在插入点上**的 ⇒ 默认名与用户输入**共用一个字符串** | **同 B30**：默认名当**整体单元**（`_nameDefaultPending` 只在 `OnOpen` 默认名非空时置位；纯函数 `EditNameDefault` **首次键入任一有效字符 ⇒ 整个缓冲被该字符替换**；空字段退格/Delete 无效果） | 本轮复核 = 实机 `x_contact.index.tsv` 格 `B4`：`nameVisible='Hero\|' nameBuffer='Hero'`（未键入时默认名**整体**在位，不是残片）；原修复证据 `.ai-tmp/screenshots/a17_name_fixed.png`（`defaultInBuffer=0 / glued=0`） |
| **B43** | 河面有**硬边纯蓝长条**（**= R1 的 B25，本轮复核**；处置 = **E32**） | `Module/Map/MapView.cs`：河带那些格的 **wall 层**叠的是 `Objects/moor_river/028` —— `river.dt1` 里唯一的**平色**瓦片（RGBA(0,32,68)）；原版靠 `ACT1/Pal.PL2` **调色板循环**把它变成水面动画，本工程**没有运行期循环** ⇒ 静态 = 硬边平色块 | **同 B25**：`Module/Map/MapView.cs:732` 起新增白名单 `PaletteCycledFlatWallTiles` + `IsPaletteCycledFlatWallOverlay`（命中即**不叠** wall 瓦片；floor 水瓦片照旧）。**逐格键 / 可走性一格未动** | 本轮复核 = 实机 `x_contact.index.tsv` 格 `C1-wide`（`map=56x40 seed=2143926554 exits=[(17,26),(17,27),(17,28)] walkable=1528 blocked=712`，图 `x_c1_town_wide.png` —— 河面无硬边纯蓝长条）；离线 `mapcheck::Step15_FlatWaterWallNotOverlaid` |
| **B44** | **点桥走不上去**（**= R1 的 B26，本轮复核**） | `Module/Player/PlayerModule.cs:455 MoveTo`：目标格**不可走就整次点击直接拒绝**（只 Warn、角色不动）；而**桥栏杆与桥面相邻 1 格** ⇒ 点到栏杆那次点击完全作废 | **同 B26**：`MoveTo` 加"最近可走格回退"（`:496-539`）：半径 `MoveFallbackRadius = 2`（`:105`）内按 ①离点击点最近 → ②离角色最近 → ③(dx,dy) 升序 **确定性**取格；**图外 / `TileKind.Void` 一律直接拒绝**；生效只报一次 `[R1-B]` | 本轮复核 = 实机 `x_contact.index.tsv` 格 `D1-detour-start`（`from=(14,18) to=(11,15) nonWalkableCellsOnPath=0`）/ `D1-detour-stop`（`want=(11,15) playerGrid=(11,15) nonWalkableCellsOnPath=0 **framesOnNonWalkableCell=0**`）；离线 `playercheck::Step4_Unreachable` / `Step4b_BridgeWaterFallback` |
| **B45** | 创角**点击立绘变形**（最多拉 **45%**）（**= R1 的 B27，本轮复核**） | `UI/CharCreatePanel.cs` `ShowTransitionFrame`：过渡逐帧播放时**只换 sprite、矩形沿用帧 0 那一套**；而过渡帧原生尺寸最多差 45%（例 amazon `fw_0` 118×198 → `fw_21` 215×228）⇒ 非等比拉伸。另：过渡帧未预热；`UI/UiArt.cs` `SetSprite` 异步回调**无守卫** | **同 B27**：① 逐帧矩形表 = `tools/probes/gen_portrait_frame_table.py` 读每张 PNG 的 IHDR 宽高生成、**就地重写** `UI/UiLayoutFlow.cs` 的 `ClassMenu.Transition`（167 帧 = Amazon 54+30 / Barbarian 64+19）② 过渡帧预热 ③ `UI/UiArt.cs:254-292` 加**请求守卫** | 本轮复核 = 实机 `x_contact.index.tsv` 格 `B4`：`Portrait0 sprite=nu1_0 native=118x198 rect=212x356` / `Portrait2 sprite=nu1_0 native=86x183 rect=155x329` ⇒ **两轴比恒 = 1.800**（= 原生宽高比 ×K，无拉伸）；离线 `uicheck` **⑰ 节**（回读 PNG 逐帧比对，**167 帧 0 例外**） |
| **B46** | 人物移动**抖动**（一顿一顿）（**= R1 的 B28，本轮复核**） | ① `Module/Player/PlayerMotor.cs:195-218`：到路点时先**吸附到格心**却**不扣运动预算** ⇒ 落格那一帧位移 = 白送量 + 预算(`speed×dt`)，实测最大 **2.28×**；② 帧节奏不确定：全工程**从未**设 `Application.targetFrameRate`，而 `QualitySettings` 逐档 `vSyncCount` 不同 | **同 B28**：① `PlayerMotor` 新积分口径（**能走到格心就走到并同步扣预算**，位移恒 ≤ `speed×dt`；`:206-218`）② 新增 `Core/FramePacing.cs`：`targetFrameRate=60` + `vSyncCount=0`（与画质档位**无关**），随 `Bootstrap` 与**每次改画质后**重钉 | 本轮复核 = 实机 `x_contact.index.tsv` 格 `X1-frame1..6`：逐帧 `dWorldFromPrev` = **0 / 0.5234 / 0.4408 / 0.4189 / 0.4267 / 0.4281**（全部 ≤ `speed×dt`，**无白送跳变**）；离线 `playercheck::Step15_Jitter` |
| **B47** | **关掉商店后对话没了** + **点对话面板按钮角色乱走**（**= R1 的 B29，本轮复核**；含 7 个子缺陷 S1~S7） | S1 商店与对话条同为 `Popup` ⇒ 引擎 `UIManager.Open<Popup>` 对同层其它面板调 `CloseMutexPanels()`（**= Destroy，且不发关闭事件**）⇒ 开商店把对话条**销毁**；S2 点 UI 被**同时读成"点地面"**；S3 面板被引擎销毁时不发 `DialogClose` ⇒ `_currentNpcId` 残留；S4 面板与模块**双订阅**；S5 商店**标题行/提示行从未创建**；S6 菜单项**压在原版雕花方槽上**；S7 `OnOpen` 漏参**复用上一次台词** | **同 B29**：① 对话条降 **`Normal`**（商店留 `Popup`）；② `Module/Input/InputReader.cs:198 UiEatsIntent` + `:85 PointerOverUi`；③ `NpcDialogPanel.OnClose` 补发 `Events.DialogClose`；④ 面板不再订阅；⑤ `UI/ShopPanel.cs:126 BuildTitleLines` 建两行；⑥ 选项列走**两雕槽中点**（`NpcDialogPanel.cs:216 OptionW=66` / `:225 OptionX=Cx(103)`）；⑦ `OnOpen` 拿不到 args ⇒ 置空态 + Warn | 本轮复核 = 实机 `x_contact.index.tsv` 格 `G2-buy`（`shopOpen=1 title='恰西' **hint='买入'** pages=buy`）/ `G2-sell`（`hint='卖出'`）/ `G2-repair`（`page=repair`）—— **商店标题/提示两行实测在位**；格 `X9-default` + `D1-*`（点 UI 不触发移动）；离线 `uicheck` **⑬ 节**（S1~S7 逐条 + 每条只报一次的 `[R1-E] S<n>` 日志） |

---

## 2026-09-23 用户第二轮投诉：24 条 → 逐条处置（U25~U48）

> **编号说明**：本节的 `U` 系列 = **用户报障**专用（与前文 `B` 系列不通用，避免与 `策划/验收表.md` 的 B 编号混淆）。
> **现象列 = 用户原话（逐字）**；**状态**：`已修` = 有实机/日志证据 · `部分` = 改了但未验全或只做了一半 · `未修` = 尚未做。
> **证据**：实机图在 `.ai-tmp/screenshots/`，报告在 `.ai-tmp/test/report-*.md`，判据资产在 `tools/probes/`。
> 提交：`03d627d3`（**用 `SKIP_VERIFY_GATE=1` 绕过闸门**，闸门残留项见该提交的 `dispatch-log.tsv` `# bypass:` 行）。

| # | 现象（用户原话） | 根因 / 现状 | 归口模块 | 状态 | 修复 / 证据 |
|---|---|---|---|---|---|
| U25 | 桥的上面过不去 | 桥面可走格与贴图错位；东端接缝无出口 | `Module/Map/{MapSeam,GridMap}.cs`、`Module/Player` | 已修 | 桥面 40 格、可走 20 格全连通、`(46,25)→(55,27)` 13 步；`（一次性判据脚本，2026-09-24 清理）`；V5 `T8-BRIDGE isDeck=1 walkable=1` |
| U26 | 人物穿模 | **根因已定死（三级键缺决胜键）**：绘制次序 = `(sortingLayer, sortingOrder, 相机距离)`；主键 = `ViewModule.EntitySortOrder(Grid)`（`:1291-1315` → `Core/Iso.cs:123` + `GameConst.cs:152-192`）是 **Grid 的纯函数**、**只在格变化时重算**（`:437/794`）；**第三键 = 节点 z**（正交相机 + `rotation=identity` + 机位 z 恒 `-CameraRig.CameraDistance` ⇒ 距离差 == z 差）。**同格 / 反斜对角相邻时主键完全相等**，而改前 z 恒 = `worldZ`(0) ⇒ **没有任何决胜键** ⇒ 引擎按内部提交顺序交替。⛔ 任务书点名的另两个假设**已被证伪**（主键不是每帧重排；正交下距离不随插值抖动 —— 相机 x/y 完全不参与） | `Module/View/ViewModule.cs`（`SortTieRank/SortTieZ/EntityWorld`） | **已修（离线 + 实机双重判决，2026-09-24）** | **修**：`:940` 受击闪白结束撤位移处 `v.LastWorld` → `EntityWorld(v.EntityId, v.LastWorld)` —— 这是 `EntityWorld`「唯一入口」的**唯一漏网路径**（逐行核过 `Module/View/**` 全部 `position` 赋值），它把 z 次级键**整条丢掉**、落回 z=0 无键带；另加 `DumpSortKeys()`（逐节点 `类型\|id\|spriteCode\|格\|sortingOrder\|z`，`SpriteRenderer` 实际次序无公开读入口）。**判据 = 判过程**（主键相等时第三键必须不相等），真输入 = 真 MapModule(Town,seed0) + 真 `NpcPoints[0]` + 真 A* 12 步 + 真 PlayerMotor(dt=1/60)：三形（严格同格 / 相邻格同 y `(+1,-1)` / 同 y 不同格 `(+2,-2)`）各 60 帧 **零不可判别帧、翻转 0 次**，`movecheck §11` **98/0**（含第 5 条**源码级不变式**判据 = `Module/View/**` 所有 `Root.transform.position` **赋值**必须经过 `EntityWorld`，实测「扫描 5 处 / 绕过 0 处」—— 理由：数值断言看不见"有人新加一条绕过它的路径"，只有源码级扫才拦得住）；**两次变异自检** = ① `SortTieZ → return 0f` ⇒ **85 OK / 12 FAIL** ② `:947` 改回 `v.Root.transform.position = v.LastWorld;` ⇒ **97 OK / 1 FAIL**（精确指出违例行），还原后 SHA256 == 冻结值、`MUTANT` 残留 0 行、重跑 98/0。⚠️ 两个留痕坑：① `Copy-Item` 保留源时间戳 ⇒ 还原后 MSBuild 判"最新"、第一次重跑跑的是**变异版二进制**（须 `-t:Rebuild`）；② **"翻转 0 次"单独看会假绿**（旧口径 undefined 帧不进翻转统计）⇒ 必须与「**零不可判别帧**」**合取**。**已闭环（实机，片 `zorder`）**：两批独立 Play（`u26c` 12:30:56→12:32:29 · `u26d` 12:39:46→12:41:11，后者带完整排他自证 `LOCK-PRE-TAKE=absent`×2 + 三段 ASCII 无 BOM + 回读校验 + 镜像已写 + `device=AMD Radeon RX 5700 XT` 非软光栅）⇒ 三种重合位形（严格同格 / 反斜对角相邻 / 同 y 相距 2 格）**各 35 冻结帧**：**主键相等逐值对上**（player `(39,21)` 与阿卡拉 `(41,19)` **主键同为 342**、z=`1.0001` vs `2.0001`）、**不可判别 0 / 次序翻转 0 / A-B-A 交替 0**、像素指纹 rle 逐段恒定 ⇒ **未复现"闪"，判决复现**；联络图 `.ai-tmp/screenshots/u26-contact-u26{d}.png`、逐帧读数 `u26-sort-u26{d}.tsv`（362 行）。⚠️ **判据口径修正一条（⛔ 不是放宽）**：`签名数 > 1` **不能**判 FAIL —— 冻结生效的**那一帧**（`LateUpdate` 才生效）必然带"未冻结的那一步"而与之后帧不同**一次**，这是**探针 settle 口径**；真正的"闪"形状 = **A-B-A 交替**。判决条件 = `冻结帧≥30 ∧ 不可判别==0 ∧ 翻转==0 ∧ A-B-A交替==0 ∧ 变化次数≤1`，且判官极性自检仍 PASS（合成 A-B-A 样本照样 `exit=1`）。前两次跑（`u26`/`u26b`）的 6 个缺陷**全落在驱动里、不在被测代码**（private 反射用错 `BindingFlags` / 冻结被游戏自身每帧重钉 / **TSV 行 24 字段 vs 表头 26 列致其后所有列左移** / 位形分类 `dx>0` 写反 / 每帧新建 RT 只有 ~3fps），已修并加 `ROW-COLUMNS row==header` 列数守卫。**残余处置**：① 首帧 settle 的 **1 次像素差**已**离线判明与次序无关**（不再留人眼判）—— `u26_sigseq.py` 对三种位形一致给出：冻结窗口内**键元组 distinct = 1**（`342/342|1.0001/2.0001`）且**冻结前一帧的键元组相同** ⇒ **键恒定不可能产生两种次序**，故该差异不是次序翻转；差块 ≈5126 px / 76800 = **6.67%**、bbox 83×126，与"**未冻结那一帧仍推进了一步动画**"一致 = 探针 settle 口径（判据资产 `.ai-tmp/test/u26_sigseq.py`）；② 三个 **v2.3 之前的持锁窗口**按规则标 `suspect (pre-v2.3 lock-name-split era)`（当时无镜像、且队列里有只认旧名的 runner）⇒ 判决落在带全套自证的 `u26d` |
| U27 | 人物抖动 | **抖源已定量定罪 = 帧节奏（时间轴），⛔ 不是位置层**：① 三层位置（逻辑 / 渲染 / 相机）**逐帧逐位相等**（638 帧最大偏差 **0**）；② 同一段输入**只把 dt 序列置换成均匀值** ⇒ 抖动消掉 **90.1%**（`J_A=0.017573 → J_B=0.00174 格`，判据线 0.2），`g2b` 两口径相差 10×（判据有分辨力）、`g4` 终点差 0；③ `corr(相机纵向偏差, dt−均值)=0.9038`，正落在真机实测带（0.729/0.765/0.923）内；④ **玩家每帧格步 = `PlayerWalkSpeed × dt` 逐帧精确成立**（6 帧逐位对上）⇒ 位移严格 ∝ dt；⑤ 量级互证 `J_A = 2.53px@1080p` ↔ 真机同口径 `2.80~3.13px/frame`；⑥ 排除"逻辑锁 25Hz / 格子级台阶"：`PlayerModule` 无固定 tick / 无逻辑帧率，`IPlayerModule.World` **每帧无条件写**（`PlayerMotor.cs:299-307`，在 `Tick` 的 while 之后）。**另修一处机制**：相机夹制原会**改写平滑状态**（`_pos = clamped` + 咬合时清零速度状态）⇒ 抽成纯函数 `CameraRig.StepFollow`（生产 `Tick` 与离线宿主**同一实现**），夹制**只限输出**、状态里只留 `speed×smoothTime` 的滞后 | `Module/Camera/CameraRig.cs`（夹制段）+ 引擎 `Runtime/Presentation/{CameraMath,FramePacing}.cs` | **部分**（抖源已定罪 + 相机机制修正已落 + 夹制段判据补齐；帧节奏档位待 Play 的 A/B dt 证据） | 判据 = `playercheck` **389 通过 / 0 失败**（起点 368，新增 21 条 = `f` 11 + `g` 6 + `h` 4，**未放宽任何上界**），原始输出 `.ai-tmp/test/u27-playercheck-final.txt`，报告 `.ai-tmp/test/report-u27.md`。**夹制段判据（洞已补）**：`h1` 389 帧 / **被夹 215 帧** / 脱开后 88 帧（不再是 `_cam == null` 早退绕过）；`h2` 构造性不变量「相机不领跑」= 0.060001 ≤ 玩家 0.060005；`h3` 退化样本（旧口径）0.069288 vs 0.060001 = **+15.5%（1.3px@1080p）**。⚠️ **两条防误引口径**：① 夹制带 `167/638 格` 与最大夹制位移 `7.3604 格 = 1059.9px@(55,25)` 是**静态位移量**、**不是每帧跳变量**（"旧口径脱开猛跳 7.36 格"已被实测否掉，真实差仅 1.3px）；② **⛔ 不声称**本改动修掉了"一顿一顿" —— `h4` 实测两口径脱开首帧步长**是同一个数**（`smoothTime=0.02s ≪ dt=1/60s` ⇒ 误差项主导），阶跃来源是**夹制本身** ⇒ 本改动收益 = 机制正确 + 「相机不领跑」+ 夹制段首次纳入判据 + 单帧过冲 −15.5%。**A/B 真机读数已拿到并跑全（2026-09-24 · `u27v9` = 两档 × 六场景全收齐 · `FINISH=why=all-scenarios-done frames=5590`，⛔ 不是 `frame-cap`/`area-changed`；`clampband=2271 / diag=1549 / line=1245 / clickspam=484 / idle=41`）**：`A: n=1545 dt **中位=0.016724s** / 均值=0.017204s（≈59.8fps，p95 0.019072，sd 0.006426，max 0.155077）` / `B: n=4045 dt **中位=0.005175s** / 均值=0.005604s（≈193fps，p95 0.007381，sd 0.003874，max 0.104158）` 〔⚠️ **2026-09-24 更正（`closefix` 从 tsv 现算并逐值核对）：原写"中位=0.017204 / 0.005604"实为**均值**；中位是 0.016724 / 0.005175（n/p95/sd/max 与它逐值相同）。按中位换算 = A≈**59.8fps**、B≈**193fps**（落在另处写的 ≈180–196fps 带内）**〕** ⇒ **VERDICT-AB：B 档更稳（sd 1.66×↓、p99 2.19×↓）**；**两档速度中位数都是 `3.3541`** ⇒ **位移在两种帧节奏下都严格 ∝ dt**。**同批复核**：`R1 渲染≡逻辑 max|diff|=0.000000`（**5590 帧**、`nan-render=0`）、`R2` 五场景全 PASS ⇒ **无层次分裂**；`R5 raw=1851×` 的 FAIL **归因闭死** = **全批唯一一帧 24.0832 单位位移**（`clampband frame=4063`、`spr=idle_w_0`、`cad=B` = **A→B 换档时的驱动复位**）＋ dt 未归一化 ⇒ **判据形态问题，⛔ 非缺陷**（口径：桶按"行为"分 + 先按 dt 归一化；归一化后每方向桶 `vel` 中位数恒为三个方向常数，>5% 离群仅 0.00%~1.64%，且全是段起止帧）；`R3 clampband frozen-while-walking=382 帧` 判 **informational**（边界夹制带相机不动 = `CameraRig.ClampMapBounds` 本职）。**残余（非本条缺陷）**：**帧时间卡峰**（A `max=0.155` / B `max=0.104` 单帧）⇒ 属**引擎帧节奏策略**。**下一步（引擎侧决策，⛔ 不由本片改）**：`FramePacing` 档位取舍需用户/引擎方裁 —— **⛔ 但本批数据不足以定档**（见下"降级"）。**档位映射（已回填，`closefix` driver 的 `SetCadence` 调用）**：`SetCadence("A", 60, 0)` = **A：`targetFrameRate=60` + `vSyncCount=0`**；`SetCadence("B", -1, 1)` = **B：`targetFrameRate=-1`（不封顶）+ `vSyncCount=1`**；两档均有读回校验（`readFps/readVSync ok=1`，`refresh=100Hz` 只是**桌面报告值**）⇒ 措辞钉死：**"A=封顶 60fps；B=不封顶(-1)+vSync=1（读回 ok，本环境未真正节流，实测 ≈180–196fps）"**，⛔ 不许写"显示器刷新率 ≥196Hz"。**⚠️ 口径敏感 + 暖机效应（`closefix` 离线复核 `（一次性判据脚本，2026-09-24 清理）`，report-only）**：**绝对口径**（sd/p99）⇒ "B 更稳"（sd 1.66×↓、p99 2.19×↓）；**相对口径**（CV）⇒ **方向相反**（A 0.374 / B 0.691；B 的 dt 中位仅 5.6ms ⇒ 任何 100ms 卡峰都是 18× 中位）；且 **A 有显著暖机趋势**（sd 三分位 `0.009054→0.005206→0.003798`、趋势比 0.420；**A 的 155ms 卡峰落在 `passIdx=1/1545`（frac≈0.0006 = A 遍第一帧采样）** —— ⚠️ **更正**：不是"前 1/3"；"前 1/3"指的是**组内 sd 趋势**（A firstHalf `0.008399` → secondHalf `0.003439`，B 反向 `0.002540→0.004837`））⇒ **去掉暖机段后两档绝对口径基本相同**（A 后 1/3 sd=0.003798 vs B 全段 0.003874）。⇒ **最终降级（`jitter` 判官 + `closefix` 离线归因）**：本条由"B 更稳（待顺序确认）" **降为「本批不可识别：`order-confounded` + `metric-dependent`」** —— **两套口径反号**（绝对 sd ⇒ B 更稳 / 相对 CV ⇒ A 更稳），根源 = **两档不在同一运行区间**（⚠️ **2026-09-24 二次更正（`jitter` 实测，原写"被 60fps 封顶 ⇒ 尾部截断"**不成立**）**：真"贴在目标周期上"的帧只占 **9.9%**（153/1545）；`dt < cap×0.99`（**比 60fps 还快**）占 **43.8%**、`dt ≤ cap` 占 48.3% ⇒ **A 不是硬封顶、没有可分离的"被钳住"子集**；能成立的说法 = **`dt` 均值被钉在 60fps（p50=0.016724s）而 B 在 193fps（p50=0.005175s）⇒ 帧预算差 3.2×**）⇒ **任何"只在未触顶帧上算"的过滤都只是"换抽样区间"、不是剔除同一现象** ⇒ 跨档比 `sd`/`CV` **更不成立**。⇒ **⛔ 不得据此改任何生产帧节奏**（"选哪档"不作为可行动结论）。**真正方向 = 单帧卡顿（hitching），不是选档**：**100–155ms 单帧在两档都存在**（A `max=155.077` / B `max=104.158`），而位移侧已干净（`R1=0` 5590 帧、`|step|/dt` 常数 ⇒ 长帧只让运动按比例变慢、**不突跳**）。**⭐ ≥100ms 帧的跨批复现性预演（`jitter` 跑 v5f / v8 / v9 三批）**：都集中在**遍头或场景收尾**（v5f `passIdx` 1,5；v8 1,9,1530；v9 1,345（场景收尾）,2147,4041（遍尾））⇒ 支持"**遍头暖机**"那一类，**而不是**"同一世界坐标 / 同一动作点"⇒ **这正是 `u27v10`（`BFirst=true`）要证的东西**：**若 v10 的最大卡顿从 A 的 `passIdx≈1` 移到 B 的 `passIdx≈1`，则"先跑的那一遍更抖"成立**（`[M1]` 的 `passIdx`/`frac` 就是这条判据的读数面）。**⚠️ 必带混淆项**：全部读数取自 **Unity 编辑器内 Play** ⇒ 编辑器自身停顿（域重载 / 资源导入 / GC）可能就是主因；**要谈发布行为必须 standalone build 复测**（**未做，列为待定项**）。**收尾批 `u27v10`**（B 先 A 后）口径**先行固定**：主口径 = 同场景同行为 `sd`（绝对，**只在未触顶帧上算**）、辅口径 `CV` ⇒ 用于把"档位"这条**同向或否一次终结** |
| U28 | 原版的地图边界是黑边吗？ | 原版边界外**确实是黑**；"斜切黑三角"是等距投影下等距边界的必然（东 38/40、南 16/56、西 30/40、北 45/56） | `Module/Map` | 已查明 | `report-M3.md`；未复现"石墙缺一段" |
| U29 | 穿过桥去不了下一张地图 + 暗黑这种图的衔接是不用读图的 | 东边界接缝未当出城口 | `Module/Map/MapSeam.cs` + `Module/Player` | 已修 | V5 `T12 area Town→BloodMoor changed=1`，图上有 "Entering Blood Moor"。⚠️ 原版同 act 的 outdoor 区域之间**仍有极短加载屏**，不是完全无缝（已在报告说明） |
| U30 | 文字框太小了，我记得原版也不是这样的 | 石框按原版像素需 ×2、字号需 ×1.8；另有"标题/正文画到框外" | `UI/NpcDialogPanel.cs`、`UI/UiLayoutGame.cs` | 已修 | 石框 `210×158 × DialogArtScale(2) ×1.8 = 756×568.8`；V6 修掉 `BuildFallback` 在 `Stretch` 后设 `sizeDelta` 的越框 |
| U31 | 商店标题栏没有字 | 标题行未接数据 | `UI/ShopPanel.cs` | 已修 | V5 图上见到「阿卡拉 / 買入 / 修理 / 關閉」；`S5 商店标题/提示落在空带、无缺字` |
| U32 | 传送点没效果 | **整功能此前不存在**（全仓无 Waypoint 物件/TileKind/生成器）；`33f75bb5` 接线（修静默失效，缺的就是 `App/AppWiring.cs:88` 那行装配）+ `2aa48419` 激活列表落盘 | `Module/Map` + `UI/WaypointPanel.cs`（新）+ `App/AppWaypoint.cs` | **部分**（**功能线闭环**；表现线残余见右） | 链路 **21/21 跳**逐跳都有写者+消费方（`AppWaypoint.cs:65/144-168/295-304` 判锚点格 → `:171-191/218-232` 走到即开面板（8 邻 + 不可走放弃）→ `WaypointPanel.cs:331-342` 点目的地 → `AppWaypoint.cs:255-290` 校验+切区（4 拒各自点名 Warn）→ `AppFlow.cs:972→1412-1415→1475-1476→1534-1560` 切区消费 → `:1460-1465` 落地区域 id → `SaveModule.cs:208/644-662` 落盘 → `AppProgress.cs:193-229` 读档回灌）；离线判据 `tools/probes/hosts/uicheck/WaypointFlowCheck.cs`（**①-b/①-c/①-d 共 +10 条**：R1 自洽闸门三分支 + **注入式直证**（真文本 0 处 → 同一文本追加一行必须报 1 处）+ **注释里的同串算 0**（剥注释走宿主唯一实现）+ R3 帧界守卫；真跑生产类 + 同进程对立退化样本；**读数（`waypoint` 15:40:43 一手）：`EXIT=0 / [FAIL]=0 / [OK]=`866** —— 改动前 15:38:37 = **856** ⇒ **+10，一条不多不少**；⚠️ 原记"25 条 / `[OK]`=726"是**更早快照、已作废**；`run_all_hosts` 15/15 FAILED=0）；**L3 实机**（`.ai-tmp/screenshots/u32_evidence_u32play.txt` 158 行）= `WP-PANEL open=1 dests=[1,2]` / `WP-DESTS-CHK dest=1 inList=1 currentInList=0` / `LANDED area=1` / `AREAID-AFTER mapArea=1 selectedAreaId=1` / `VISITED-AFTER count=3 [0,1,2]` / `BLACKWINDOW 0.428s` / `console errors=0` + **读档回灌 L3+L2 互证**（存档 `"visitedWaypoints":[0,1,2]` 与内存逐值相同）。**G1/G2 已裁定**（出处 = 官方 `Levels.txt` 140 列：索引 29 `SubWaypoint` / 119 `Waypoint`；`Act 1 - Wilderness 1` id=2 `Waypoint=255`、`Act 1 - Cave 1` id=8 `Waypoint=255`；Act1 有传送点的 9 个里**只有罗格营地在工程范围内**）⇒ ①「野外传不回来」**符合原版**、不算缺陷；② 但本项目「**到过该区域即成为可用目的地**」与「落地 = 目的地出生点」是**本项目新增口径**，须登记；③ 保留硬约束「未到过的区域**永不**入列目的地」。**表现线残余（⛔ 不混进已完成）**：① 传送台**本体美术** = `WP-ART-1` **BLOCKED**（`MapView.cs` 对 Waypoint **0 命中** ⇒ 锚点无渲染写入方，玩家看不出点哪；载体 `d2exp.mpq` 不在盘、取件路径已穷举落空）；② **换区落地黑窗 0.428s**（`chunk-hole2` 登记残余 = 建块耗时，非永久黑）；③ 1 张面板截图待重采（根因已定案 = 同帧两次 `ScreenCapture` 被后一张顶掉，L3 两次 `SHOT` frame 都是 192；驱动已修） |
| U33 | 鼠标在人附近移动没效果，必须要远 | 世界→格换算已逐行核过并对 1 格内精确断言（3 条 OK），但实机无可见反馈 ⇒ 疑 `HoverPicker` 的"怪物须与鼠标同格" | `Module/Input/HoverPicker.cs` | 部分 | `report-U4.md`；需实机复现后再改 |
| U34 | 鼠标放在按钮上，按钮是花的。文字太小了 | 字号族已统一（15 处改引用唯一出处）；"花"只修掉静态可查的一条（画布px 传进按原版px 判的选帧阈值） | `UI/UiArt.cs`、`UI/UiLayoutGame.cs` | 部分 | 实机字高 28 画布px（改前 ~16）；hover 帧序未与原版比对 |
| U35 | 下面的栏 100% 不是原版，我现在找不到人物属性 技能 各种控件 | 迷你面板 7 钮→8 钮（原版 `minipanelbtn.DC6` 16 帧=8 对 + `string.tbl` 8 条 `minipanel*`）**且默认展开**（此前 `SetMiniPanelOpen(false)` + 开合箭头 `SetActive(false)` ⇒ **鼠标入口 0 个**） | `UI/HudPanel.cs` | 已修 | V5 图上见到 8 枚按钮；`UiLayoutGame.MiniButtonCount=8` |
| U36 | 人物与 npc 重合时候，会闪一会人物一会 npc | **同 U26 根因（三级键缺决胜键）**；**本轮新增解释"为什么是闪一会而不是闪一帧"**：城镇阿卡拉 `(41,19)` 与出生点 `(32,28)` 的 **`gx+gy` 恒等 = 60** ⇒ 沿这条反斜对角走 12 步（实测 60 帧）**主键一路相等** ⇒ 整段走位每帧都靠第三键决胜，旧口径下 = 整段都在交替 | `Module/View/ViewModule.cs` | **已修（离线 + 实机双重判决，2026-09-24）** | 同 U26：`EntityWorld` 漏网路径已补 + `DumpSortKeys()` + `movecheck §11`（三形各 60 帧零不可判别帧、`movecheck §11` **98/0** 含源码级「唯一入口」不变式扫描、两次变异自检 12 FAIL / 1 FAIL）。**已闭环（实机）**：同 U26 口径 —— 两批独立 Play 的三种重合位形（含本条点名的**城镇阿卡拉 `(41,19)` 反斜对角 12 步**、`gx+gy` 恒等 60）**各 35 冻结帧、次序翻转 0、A-B-A 交替 0**，像素指纹逐段恒定 ⇒ 旧口径"整段都在交替"的现象**未复现**；读数与联络图见 U26 行。残余同 U26 —— 其中 settle 帧那条**已离线判明与次序无关**（键元组恒定 ⇒ 不可能产生两种次序；差块 6.67% = 探针口径），三个 pre-v2.3 窗口标 `suspect` |
| U37 | 鼠标放现在道具上没显示信息 | V5 报的是**探针假红**（hover 前抛 NRE）；产品侧经反射读实测正常 | `UI/ItemTooltip.cs` | 已修 | V6 `visible=True texts=2 title="微型治疗药水"` |
| U38 | 鼠标放在怪身上也没显示信息 | 缺怪物 tooltip；建框链路缺字号 | `UI/EntityTooltip.cs`（新）→ **已迁至 `UI/EnemyBarView.cs`** | 已修（**2026-09-24 改口径**） | 原实现（V5/V6）：`texts=["堕落者","血量 2/2"]`、`fontSize=28 kids=1`，但**画在怪头顶**、自陈"本项目选定值、原版无载体"。**U44 落地后改口径**：悬停可击杀怪 ⇒ **屏幕顶部 `EnemyBar`（怪名 + `health/maxHealth`）**，几何/配色/字体**逐值**出自原版 `EnemyBar.prefab`（根 `anchor(0.5,1) pos(0,−22) size 150×20`；底色 `(0,0,0,0.478)` / 填充 `(0.75,0.0221,0.0221,0.251)`；`font16`；**无位图**）；头顶那份按 U44-C4 **已删除**，`策划/验收表.md` 第 14 行（「交互光标」）同步改口径。⚠️ 实机读数（**11:59 城镇批**）：`P5-PLATE plateVisible=1 plateText="阿卡拉" barVisible=0`（NPC 不出顶部条 = 分派正确）；血沼泽批（12:38）该处为 `P5 SKIPPED: no NPC (area=BloodMoor)` / `plateVisible=0` —— ⛔ 引用分派证据时**标明批次**（同族：`u3bverify` 已指出我把这两批的行混引过）；**怪的三格已于 2026-09-24 采到**（`u44impl`，**非用户阻塞**，已跑通）：`[TRAVEL-RESULT] area=BloodMoor aliveMonsters=19`（取法 = `TeleportTo(SpawnPoint)` + **`IPlayerModule.MoveTo(exit)` 真走** ⇒ 走格触发 `ExitEntered`，⛔ 不绕过；第一次 `TeleportTo(出口格)` 的卡点原文 = `area=Town playerGrid=(17,26)` = 站上出口格但没换区）+ `[P3-HOVER-ON] m#1007 _Brightness=3 _Contrast=1.01 shader=Sprite` / `[P3-BAR] visible=1 title="僵尸" value=9 max=9 hp=9` / `[P4-HOVER-OFF] _Brightness=1 shader=Sprite-Lit-Default + barVisible=0` / `[P5-KILL] alive=0 barVisible=0 _Brightness=1`；读数 `.ai-tmp/screenshots/u44hover_evidence.txt`、联络图 `u44_hover_sheet.png` |
| U39 | 打击感就是一坨，你是圆形判断的打击范围，怪物没音效，打击也没声音！！ | ① 判定是圆阈值（原版按 `Missiles.txt` 碰撞模式 = 扇形/矩形）；② 音效链路曾被"音效池满"丢弃 | `Module/Combat/MeleeShape.cs`（新）、`Module/Combat/CombatModule.cs`、`Module/Audio/{AudioHook,SfxThrottle}.cs` | 已修 | 判定 14 条断言全 OK（正面扇形 ±60° / 走廊半宽 1.2 / Bresenham 阻断）；音效实测 `portal` 24214→**2**、`hit`/`monster_attack` 真播、池满 Warn **0** |
| U40 | 红色哥布林移动就是一坨，没有音效，没有打击动画 | 移动存在；逐类怪物脚步/受击音**确实缺**（素材未到位） | `Module/Monster`、`Module/Audio` | 部分 | V5 两只红色「堕落者」位置有变化；已登记 `C3-1/C3-2/C3-3`；受击动画上游登记 `E28` |
| U41 | 贴图问题。地图边界问题 | 同 U28；"取错帧/变体"未验 | `Module/Map` | 部分 | `report-M3.md` |
| U42 | 为什么没拦住 | 加了"线段不得被不可走地形阻断"；墙缺口未复现 | `Module/Combat/CombatModule.cs` | 部分 | `report-C3.md` |
| U43 | 屏幕外都能打我？？？？？ | 怪物攻击未夹屏幕内射程、无线段阻断 | `Module/Combat/CombatModule.cs`、`Module/Monster/MonsterTuning.cs` | 已修 | "屏幕内射程夹一次" + 线段阻断 + `WarnThrottled` 留痕 |
| U44 | 为什么选中怪物没有什么选择效果啊！！！！！！为什么打击范围这么奇怪。为什么丝毫没有打击感 | 判定范围已修（同 U39）；**选中高亮出处已补齐** = Diablerie **逐字参考实现**（`client/Assets/Resources/Clover/Shaders/Sprite.shader`，**唯一有效路径**；旧路径 `…/D2/Shaders/Sprite.shader` **已废**，逐字节同源：blob-sha1 `20498502…` / SHA256 `5B2E6A00…` / 2469 B）⇒ `E31-② + BL-9` 的"出处缺失"不再是阻塞；组件 = `Module/View/EntityHighlight.cs`（**3.0/1.01/1.0/1.0 的唯一真源**）+ `UI/EnemyBarView.cs`（顶部怪条，几何/配色/字体逐值出自原版 `EnemyBar.prefab`） | `Module/View/EntityHighlight.cs`、`UI/EnemyBarView.cs`、`Resources/Clover/Shaders/Sprite.shader` | **已修（实机证据，2026-09-24）** | L3 五拍一次采齐（读数 `u44hover_evidence.txt`）：`P3-HOVER-ON m#1007 _Brightness=3 _Contrast=1.01 shader=Sprite visible=1` → `P3-BAR visible=1 title="僵尸" value=9 max=9 hp=9`（**真怪**：真名 + 真血量）→ `P4-HOVER-OFF _Brightness=1 shader=Sprite-Lit-Default + barVisible=0`（**还原原始材质**）→ `P5-HOVER-AGAIN` 可复现 → `P5-KILL alive=0 barVisible=0 _Brightness=1`（击杀后条消失、高亮复位）；另 `P0-ROUNDTRIP write _Brightness=2.5 _Contrast=1.25 -> read 2.5/1.25 ok=1`（**运行时口径自证**，防"读回的是别的槽位"）；`run_all_hosts.ps1` **15/15 FAILED=0**；联络图 `u44_hover_sheet.png`（4 格）。**残余（如实登记）**：① `P2-TARGET onScreen=0` 那一拍的**归因口径**（目标怪在屏外时注入鼠标不可归因 ⇒ 已改"先 `P2-APPROACH` 带进画面"）；② 图片判读只做到"条在/条不在"这一层；③ `Uicheck.csproj` 三行 `Compile`（链入 `EntityHighlight.cs`/`ViewLog.cs`）**尚未落地** ⇒ C2 仍是"文本提取"级判据（正是 12:14 `Shader.HasProperty` CS1061 假绿的成因）⇒ **已排收尾片**（要先取锁、并把 `Program.cs` 末尾那份搬到 `HoverSelectCheck.cs`，⛔ 两处并存 = CS0101） |
| U45 | 你看你这B贴图 | 未定案（需并排图） | `Module/Map`、`Module/View` | 部分 | 同 U41 |
| U46 | tab 渲染地图不对，背景不用压暗，你去搜一下好吗？ | 压暗已去掉；但绘制图元稀疏、揭示口径只有"本格+8 邻域" | `UI/MiniMapPanel.cs`（`BackdropAlpha=0`）+ 需 `Module/Map` 提供"已探索记忆" | 部分 | V5 实测全屏无压暗遮罩；`opaquePixels=135→775`；已登记 `E23④` |
| U47 | 界面乱七八糟。而且找不到入口只能按 c | 同 U35 | `UI/HudPanel.cs` | 已修 | 同 U35 |
| U48 | 背包里道具描述框也乱七八糟 | `ItemTooltip` 4 处排版错（取整宽、标题对齐、正文框高未按行数、Wrap/Overflow） | `UI/ItemTooltip.cs` | 已修 | `report-U4.md` |
| — | **道具没法拖动！**（同批第 25 条） | UI 侧已修（拖影+目标格高亮）；**"放下/交换"卡在契约层**（`AppEventRouting` 收到 `MoveInInventoryRequest` 只打 Warn） | `UI/InventoryPanel.cs`、`Module/Contracts.cs`、`Module/Item/{ItemModule,Inventory}.cs`、`App/AppEventRouting.cs` | 部分 | V6 实测拖影 `act=1 colorA=0.3 ghostActive=1` + 高亮可见；落格链路已落盘**未验证** |

### 同轮附带完成（用户未报、自查挖出）

| # | 现象 | 归口模块 | 状态 | 证据 |
|---|---|---|---|---|
| U49 | 一进 Play 就把**全项目位图字模永久降级成系统 TTF**（"文字太小"的真根因之一） | `UI/D2Text.cs`、`UI/D2TextMirror.cs` | 已修 | `EnsureChi` 在 `Game.Res==null` 时调 `OnAtlasFailure`（静态开关一局不恢复）⇒ 改延迟 + `RetryDeferred()` 自愈；现 `bitmapUnavailable=0` |
| U50 | 位图字号有 15 处手写 magic number（属性面板 17/16、按钮 20、对话正文 12…） | `UI/**` | 已修 | 全部改引用唯一出处 `UI/UiLayoutGame.cs` 的 `FontPx16/24/30/42`；全仓 19 处调用点机械扫零漏 |
| U51 | 出口事件被每帧重复触发（探针自造 39070 次，生产侧全日志仅 7 次） | `Module/Map/ExitLatch.cs`（新） | 已修 | 发送侧闩锁 + 接收侧 `SfxThrottle`；`audiocheck` 95/0 |

---

## 2026-09-24 用户第三批投诉（**现象列 = 用户原话逐字**）

> 派活留痕 = `.ai-tmp/test/dispatch-log.tsv` 的 `d2fix/charstat` / `d2fix/popupaudit` 两行。
> 状态口径同前：`已修` = 有实机/日志证据 · `部分` = 改了但未验全或只做了一半 · `处理中` = 本轮已派活、结论未回 · `未修` = 尚未做。

| # | 现象（用户原话） | 根因 / 现状 | 归口模块 | 状态 | 修复 / 证据 |
|---|---|---|---|---|---|
| U52 | 「人物状态框，**数值信息不对**啊，**ui显示不对**啊」 | **数值真错 3 条**：起始生命 60→**50**、法力 22→**15**、耐力 20→**84**（根因：`Module/Player/PlayerStats.cs:210-215` 把「每点成长」乘到**起始四维**上；官方起始值 = `charstats.txt` 的 `hpadd` + 起始体力 / 起始精力 / `stamina` 列）；**显示真错 4 条**：数值列跨压在底图分隔条上（D1）/ 数字压行框下沿（D2）/ 标签字号 22/18/17/16 是魔法数字（D3）/ 右上「等级+经验+技能点」三段硬塞进原版 150×26 空框（实测凹槽 117×17）（D4） | `UI/CharacterPanel.cs`、`UI/UiLayoutGame.cs`、`Module/Player/PlayerStats.cs`、`UI/CharCreatePanel.cs`（创角预览同一错式子） | **部分**（D1~D6 已修且已判；**配表列已闭环**（`class_c` 19→22 列：`hp_add`←官方 `charstats.hpadd` / `base_stamina`←`charstats.stamina` / `block_factor`←`charstats.BlockFactor`，值 30×5 / 84·74·79·89·92 / 25·20·20·30·25；五步打表链 + 读表切换点 `Module/Flow/ClassTable.cs:66-67` → `PlayerStats.cs:327-329`；`tableverify` **14/0**、`sync_tsv --check` 一致 11/0/0/0、`fc /b` SAME、旧常量 0 命中；见 `.ai-tmp/test/report-u52-classcols.md`）；剩 Play 采证 / 箭头取件） | 对账表 = `.ai-tmp/test/report-u3-charstat.md` §1；判据 = `playercheck` §1 修后 5 职业逐一对上（`50(50)/15(15)/84(84)`、女法 `40/35/74`…）+ **退化样本「旧公式 5 职业全红 0/5」**；量法 `（一次性判据脚本，2026-09-24 清理）`；驱动 `tools/probes/drivers/d2u3_charstat_*.cs`。**D1~D6 处置**：D1 数值列改走底图凹槽实测中心（含退化样本）/ D2 行框净高 art 18 + 文字 y 修 4.95 / D3 标签字号统一 `FontPx16` / D4 右上三段**拆三格**且常量由"扫描近似"150×26 **更正为底图逐像素实测 117×17 @(91,198)**（旧值比凹槽宽 33 art px、左移 11，正是越框/越边根因）/ D5+D6 按**一手原版 prefab 逐节点对完** —— 该 prefab 只有 11 个节点（Panel/CharName/四维 4 行/派生 4 行/CloseButton），**没有**等级/经验/技能点/命中/格挡/四系抗性节点 ⇒ 登记为**本项目新增**（唯一出处 = 底图剩余空框；原版这 6 类字段在别的屏） / 四系抗性**标签折行**已按生产路径口径修 + 实机判绿（片 `u52resist`）：判据 = 生产画字两步复算（`availPx = max(1,round(boxCanvas/scale))`、`scale=fontSize/cellH`、**单行 ⇔ `MeasureNative < availPx`** 严格小于）—— ⛔ 裸 `lineCount`/`preferredWidth` **都不能用**（`D2TextMirror.Attach` 把镜像 uGUI `Text` 设成 `font=null` ⇒ TextGenerator 从不布局 ⇒ 两者**恒 0**，属恒真/恒假判据）；修前标签框 46 art（`availPx 38 < need 52` ⇒ 折 2 行、4 行共 8 行交错）→ 修后 `CharResistNameX/W` = art 0..66（66 ≥ 最小框 63）、`CharResistValueX/W` = 45 art（右沿 art 112.5 **一个数没动**）、两框不相交；5 条退化样本（46 必折 / 62 折·63 单 / 41 折·42 单 / 拉丁表被改必红 / 缺字形必须报 `missing`）+ 离线**穷举可达区间 176 个值在 45 art 下全单行**（最长 `-100%` need 47 < availPx 52）；实机 `FIT node=ResistName0..3 needNative=52 availPx=55 lineCount=1 verdict=single-line`（`lineCount` 取 `D2Label.LineCount` 真实渲染器行数）+ 值列 4 行同判 + 截图 `d2u3_charstat_u52resist.png`。**残余**：`-100%` 强制实机态**未采**（1 级无装备合法抗性=0，⛔ 不伪造存档）；`ExtraValue0`（命中 AR）**6 位数**样本 `123456` 会折（need 48 > 45，现量级不可达）⇒ **只登记不改**。**该面板关闭钮已可见化**（`alpha 0` → 可见兜底色 + **原版中等按钮帧** `ResPaths.BtnMedNormal` + **原版字模文案「关闭」**；命中区 32×31 未动，⛔ 无自画 X；依据 = 原版 prefab 的 `CloseButton` 自带 `text: Close` 标签组件）。**闭环进展**：① **配表加列已闭环**（片 `classcols`）—— `class_c` **5×22**（`hp_add`=30×5 / `base_stamina`=84,74,79,89,92 / `block_factor`=25,20,20,30,25）；5 步链跑通（`convert.py` 默认输入 = `参考工程_Diablerie/d2lod1.10txt` 92 张，⛔ 任务书里 `--src 原版资源/d2raw/...` 只有 16 张、照抄必失败；`-pack` **必须带 `-force`**）→ 11 张客户端表（只 3 个文件变：`Base/BaseClass.cs`、`Tsv/Class.tsv` 519→597B、逐表 `ALL_HEADERS_MATCH`）→ `sync_tsv.py --check` **11/11 一致**；**表驱动 == 旧常量口径逐职业同一读数**（亚马逊 50/15/84 · 女法师 40/35/74 · 亡灵 45/25/79 · 圣骑 55/15/89 · 野蛮人 55/10/92，差 **0**）；机械判据「`CharStatsHpAdd|CharStatsStartStamina` 全仓 **0 命中**」且**退化样本有效**（同 pattern 在含该标识的文件上命中 2 行）；`playercheck` **389/0**、`uicheck` **0 FAIL**。② 实机 Play 采证 P1/P2（片 `u52play`）**待采**。③ **D7 加点箭头（原记"素材缺口/待取件"）—— 根因已被 `u52play` 离线证伪**：`menubutton_0.png` **322/360 不透明像素**（亮度图就是一枚真实箭头钮面：帧 0 箭头在 `1..8` 行、帧 2 在 `11..19` 行 = 上/下箭头）⇒ **素材不缺，⛔ 撤掉"去 mpq 穷举取件"这条待办**。现候选根因 = `UI/CharacterPanel.cs:401-403` 在 `points == 0`（1 级必然 0）时 `SetPlusEnabled(false)`、`:427` 把 alpha 压到 **0.40**，而 **`0.40` 全仓唯一命中、无出处**（违反铁律 3）⇒ 归类从 BLOCKED 改为**表现类采证**，进 **W11**（看原版 1 级箭头是否本就灰；⛔ 不许自创灰度值）。④ **新发现（已派 `u52block`）**：`item_c.block` 未接入 `BonusBlock` ⇒ **装盾后格挡仍 0%**（`block_factor` 读表已通，缺的是盾牌基材那一环；须先查原版合成式与上限出处，⛔ 不许自创）。⑤ **工具级风险（上报用户）**：`tabletool.exe` 在 Windows 上写盘失败会把目标**截断成 0 字节且退出码仍 0**（已造成 `item_c`/`level_c` xlsx 一度为 0，经有界重试 + 逐表 `ALL_HEADERS_MATCH` 比对后已从 txt 完整重建）⇒ 须加"临时文件 + 原子重命名 + 失败非 0 退出"，属 `clover-tools` 仓库范围。⑥ 生成物 `Base/BaseTable.cs` +25 行（`ToFloat` 改 `NumberStyles.Float + InvariantCulture`）来自工具仓库**已提交**的 `d1b8411`、由"重打表"刷新所致，⛔ 非本工程手改；**⑦ 同日再闭环两条（与 ④ 同源，已登记 E60/E61）**：**旧存档三资源** = `SaveVersion 1→2` + 旧档迁移落地（报告 `.ai-tmp/test/report-u52-block.md` §10.6，实跑原文 `[Load] 存档版本 1 < 当前 2 ⇒ 旧档迁移：三资源按当前口径补满（生命=50 法力=15 耐力=84…）`）⇒ 用户那格「老档耐力卡 20/84」修掉（普查 74/74 档 `version==1` ⇒ 不 bump 永不迁移；"一律补满"被活档反例否决）；**装盾格挡恒 0%** = 判据改为「装备集里有没有官方盾类」（`toBlock<=0 ⇒ 0%` 旧口径取消，徒手仍 0%）。两条差异**同时**落在 `策划/差异登记.tsv` 与 `策划/验收表.md`「允许的差异」节；**⑧ E60 的迁移一度在真实读档链路上不可达（实机仍 `耐力 20/84`）** —— `charstat` 真机窗口采到后定位到 `文件:行`：`Module/Save/SaveModule.cs:343-350` 在 `fileVersion != SaveVersion` 时**先把 `data.version` 改写成当前版本**（Warn 之后），于是 `Module/Player/PlayerModule.cs:448` 的 `save.version < GameConst.SaveVersion` **恒 false** ⇒ 迁移分支**是死代码**，走 `else` 的 `Min(20,84)=20`（与实机逐值吻合）。**教训（核心）**：`playercheck §9c` / `itemcheck §13b` 当初是**直接构造 `PlayerModule` + v1 夹具** ⇒ **单元级全绿、链路级断**；该洞**只有真机窗口能发现**（⇒ "实机验证不可替代"）。**已派 `u52block` 修**（授权改 `Module/Save/SaveModule.cs` 一行级；硬约束：① 迁移必须可达 ② 回写磁盘必须写当前版本号 ③ ⛔ 不许用"else 一律补满"绕过，否则会静默治好 `SAArea1.json life=34` 那类中局受伤档），并要求补**链路级断言**（`SaveModule.Load(<v1 夹具>)` → `PlayerModule`：三资源补满 + 一条 Info + 档内 `version` 落盘为 2）＋反向断言（现行版本档/中局受伤档不许补满）。**另记**：该批 `play-log` 的 owner 字段是 runner 内历史常量（写作 `u52play`/`u52-charstat-arrow-play`，与实际采集片不符）——**登记不改**（账本是证据；判归属已改用"指纹 + 时刻"与命名空间）。 |
| U53 | 「你**检查所有弹框**，认真一点好吗？？？？？」＋「甚至连他妈的**关闭都没有**。。。吐血了」 | **审计已判死 3 条关闭缺陷**：① `UI/SkillTreePanel.cs`（:107 开在 `Popup` 层、:189-194 **无任何关闭控件**）+ 引擎在 Popup 层插的全屏遮罩（`clover-client-unity-engine/Runtime/Presentation/UI.cs:443-461`，`raycastTarget=true`）盖住 `Normal` 层 HUD ⇒ **纯鼠标玩家打开后关不掉**；② `UI/QuestLogPanel.cs`（:245 同链路）；③ `UI/InventoryPanel.cs`（:538-539）与 `UI/CharacterPanel.cs`（:252-253）关闭钮 = **`alpha 0` 命中区**（点得到、看不见）；另 `UI/MiniMapPanel.cs`（:129-135）无关闭控件。⚠️ **上一轮转述的「商店修理/关闭按钮底板是纯色占位」= 静态读码误报**，已更正（见下） | 全部 18 个弹框/面板（`Resources/UI/*.prefab` + `Scripts/UI/*.cs` + `ProjectBuilder` 清单 + 引擎通用件） | **已修（实机证据 2026-09-24，全部）** | 面板全集审计表 = `.ai-tmp/test/u3_popup_audit.tsv`（13 列 × 18 行）+ `u3_popup_roster.tsv`（四路差集**全空**，18↔18↔18）+ `report-popupaudit.md`；**18 个 prefab 全是 1 节点空壳** ⇒ 控件全运行时建 ⇒「有没有关闭」只能查代码；「可见数字追不到写者」= **0 条**。**误报更正**：`ShopPanel` 修理/关闭底板**就是原版帧**（L3 实测 `sprite=buysellbtn_2/_10 color=1,1,1,1`，`popupaudit` 只看到 `UiArt.SquareButton→ButtonBg` 的纯色**兜底**、漏看其后三行的 `ApplyBuySellButtonArt`）⇒ 该条**关闭为误报**，并新增机械闸门 `tools/probes/hosts/uicheck/ShopArtCheck.cs`（16/16，含两个退化样本）防复发。**真正待修的**：两颗钮的**中文标签压住原版图形**（原版该处纯图形自明）⇒ 裁定标签移出钮外（派 `shopart` R-1）。**BLOCKED**：原版关闭「X」图形在 Diablerie 里是 Unity sprite（guid `bd016b55…`）**非 DC6**、mpq 按名穷举 MISS、本工程 `.meta` 0 命中 ⇒ 需用户给 `Prefabs/**` 或那张 sprite。**实机判决（13:11:39~13:13:35 一次会话，排他自证齐全：`LOCK-PRE-TAKE-FREE` 两名都空 → 镜像 → 真锁 → 记账 → `EDITOR-STATUS-FIRST verdict=READY` → 两名释放 + `EDITOR-STOPPED`）**：① 背包 `CLOSE-RESULT InventoryPanel before=1 after=0` ⇒ **点关闭真的关**（`CLOSE-VISIBLE alpha=0.000` 是**设计**：可见的是它**下面的位图字模「X」**）；② `[R8] MUTEX inv_before=1 inv_after=0 skill=1 expect=1/0/1 by HudPanel.CloseScreenFamily` ⇒ 同族互斥成立；③ `[R8] POPUMMASK skillTreeOpen=1 **mask=NONE**`（技能树=Normal ⇒ 引擎**不插**全屏模态遮罩）+ **对照项** `INVENTORY-OPEN inv=1 **mask=PRESENT**`（背包仍 Popup ⇒ 遮罩确实会出）⇒「纯鼠标玩家原本被锁死」的机制被实机证实并已解除；④ 对照面板 `CharacterPanel alpha=1.000 sprite=btn_med_normal / RESULT 1→0`、`Settings 1→0`、`Pause 1→0`；新截图 `u53_r8_inventory_close_mark.png` / `u53_r8_skilltree_normal_layer.png`。**两条如实登记的边界**：① `CLOSE-MARK font=…` **读不出来** —— `D2Label` 走 `D2TextMirror` 字模图层、**没有 `UI.Text` 组件**（驱动探针类型盲区）⇒「位图字模没被降级成 TTF」目前**只有图证**；② 该轮 `WAIT-VERDICT R8-DONE elapsed=5s` 是**假命中**（控制台**跨会话累积**，等到了 11:23 遗留的 `MUTEX-END`）⇒ 已修（装驱动前再 `clear_console`）；主读数未受损（mode A 全落在 R8 之后）。**商店标签 R-1 + 钮居中 R2-a（`shopart`，13:22:09~13:23:03 一次合规会话，`LOCK-TAKEN pid=51036` 三段 + 镜像同写 + 账本在取锁后 + 同窗口账本只有本片一行）**：① **钮中心 y `−297`（原版 381 = 槽顶沿）→ `−329.4`（原版 399 = 内凹区中心）**；② **标签 rect 从 `50.4×50.4`（铺满钮 = 压图形）→ `93.6×28.8`（钮外正下方）**；③ L3 原始读数 `SHOPART-SLOT slot=2/3 actualCenter==expectCenter distCanvasPx=0 distOrigPx=0 insideInner=1` + `SHOPART-BTN sprite=buysellbtn_2/_10 color=1,1,1,1 overlapLocal=0 overlapScreen=0 labelFontSize=28`；④ 表现类 4× 放大读图 `u53d_repair_zoom4.png`（凹框四边在钮外可见、**锤子+铁砧完整无遮挡**）、`u53d_close_zoom4.png`（**禁止符 ⊘ 完整**）、标签均在钮外；`uicheck` **26/26**（含 **5 个退化样本按预期变红**）+ runner 自检 **20/20**。**残余（如实登记）**：a) 标签带（原版 414..429）**跨过槽下框线 414..417（3 原版px）** ⇒ 裁定**接受并登记**（原版该处无标签、中文标签属本项目新增；`overlapScreen=0` 已证**不遮挡钮图形**，⛔ 不为此再动几何）；b) `UiLayoutGame.ShopBottomSlotY`（旧 381）**已废弃**、由 `ShopPanel.SlotCenter` 取代（值保留，删除须与 `shopart` 同步）；c) 槽 0/1 **无钮**（E31 留空）⇒ 无标签，合理 |

**2026-09-24 · U52 数值层最后一格（老档「耐力 20/84」）已闭环到"离线全绿"（主 agent 亲验）**：根因 = `SaveModule.Load` 把 `data.version` 抬到当前 ⇒ `PlayerModule.LoadFrom` 的 `save.version < GameConst.SaveVersion` **恒假 ⇒ 迁移是死代码**（单元级夹具直接构造 `CharacterSave` ⇒ 当时是绿的，**"单元级绿 / 链路级红"**）。修法 = **读档只报不改（保留档内原版本号）+ 回写磁盘时才写当前版本**（`Module/Save/SaveModule.cs:343-376`；`GameConst.SaveVersion = 2`，磁盘上 74 档全是 v1）。⚠️ **契约变更必须连判据一起改**：`savecheck` 原判据「版本被抬到当前」在改契约后**当场红了**（`version=1`），已按新契约拆成两半 —— **③a 读=保留原值** ＋ **③a-2 写=抬到当前**（真调 `SaveModule.Save(CharacterSave)` 回写、再读落盘 JSON 比版本；用独立槽位，`Events.LoadDone` 事件账**先存后复原**，⛔ 不清成空）。**全量离线回归**：`run_all_hosts.ps1` ⇒ **`TOTAL_HOSTS=15 FAILED=0`**（`playercheck` **417/0**、`savecheck` **FAIL=0**、`uicheck`、`tableverify` 14/0 …），原始输出 `.ai-tmp/test/main-runallhosts-after-savefix.txt`。**⛔ 仍未闭环**：老档进游戏后 `耐力 84/84` 的**实机确认**（等 Unity 窗口）+ U52 的箭头取件。**另：同轮 U52 配表列也已闭环**（`class_c` 19→22 列 + 五步打表 + 读表切换点 + `tableverify 14/0`，见上表 U52 行与 `.ai-tmp/test/report-u52-classcols.md`）⇒ U52 现在只剩"Play 采证（上机）+ 箭头取件"两项。

---

## 2026-09-24 待 Unity 窗口（**一次采完清单**：窗口一可用就照此表逐项采，⛔ 别再各自重进 Play 做同一件事）

| # | 片 | 看什么（原始读数行） | 判定标准 | 备注 |
|---|---|---|---|---|
| W1 | `select` / `u44impl` | **只剩"像素那一半"**：同机位同 sprite「未悬停（材质 `…/Sprite-Lit-Default`）」vs「悬停后移开（`Sprite`）」各一图 | **两帧逐像素差异 = 0** | ✅ **数值那一半已闭环、不必等窗口**：L3 raw 早在盘（`u44hover_evidence.txt:6-28`）—— `P0-ROUNDTRIP=write 2.5/1.25 → read 2.5/1.25 ok=1`（非空判据）、`P1-BASE 1/1`、`P1-ON 3/1.01`、`P1-OFF 1/1`、`P3-HOVER-ON 3/1.01`、`P4-HOVER-OFF 1/1`、`P5-HOVER-AGAIN 3/1.01`（可复现） |
| W2 | `select` / `u44impl` | Town 悬停阿卡拉 | `plateVisible=1` + `plateText="阿卡拉"` + `barVisible=0`；回 BloodMoor 悬停活怪 ⇒ `barVisible=1` + `plateVisible=0` | ⚠️ `plateVisible=1 plateText="阿卡拉"` **在 `.ai-tmp` 全树无 raw log 命中**，只存在于二级载体 `report-u44-impl.md:180` ⇒ **按 L1 处理，补采后才可当判据** |
| W3 | `u44impl` | 击杀后：`Events.HoverTargetChanged` 载荷 + `EnemyBarView.BarVisible` | 载荷 `hasTarget=false`（或 `cursor != Attack`）且 `BarVisible=false`，**连采 3 拍一致** | 12:38 `barVisible=0` vs 13:23 `barVisible=1`（同驱动同时序）⇒ 离线不可判 |
| W4 | `u52resist` | `PROBE node=…` 行（`preferredWidth` / `cachedTextGeneratorForLayout.GetPreferredWidth` 在本面板是否恒 0） | 两条 uGUI 路均为 0 而 `rendererLineCount` 有值 ⇒ 钉死"唯一可信折行判据 = 渲染器自写的 `LineCount`" | 探针**已部署**在共享驱动 `（一次性判据脚本，2026-09-24 清理）` 的 FIT 块 ⇒ **任何**跑该驱动的会话顺带打出 |
| W5 | `u52resist` | `-100%` 强制实机态（值列最坏值） | `need 47 / availPx 52 / lineCount=1 single-line` | 故意未采（1 级无装备合法抗性 = 0，**不伪造存档**）；离线已穷举 `-100..75`（176 值）全单行 |
| W6 | `u52block` / `charstat` | 老档（`version=1`）进游戏后的三资源 | **`耐力 84/84`**（另生命 `50/50`、法力 `15/15`） | 离线已闭环（迁移链 + 判据两半 + `savecheck FAIL=0` + `run_all_hosts 15/15`）；缺的只是**实机确认** |
| W7 | `zorder` | **投射物之间的先后**（同格 / 反斜对角相邻） | ① **"投射物 vs 实体"与改前逐值一致** ⇒ **已被离线锁住**（值带 `[0.0001, 0.9]` 刻意压在实体档 `SortTieZ ≥ 1.0001` **之下** ⇒ 可见关系不变）；② 真正要看的是 **同格两投射物还会不会"交替/闪烁"**（原状 = 无决胜键 ⇒ 引擎按内部提交顺序交替） | **修法已落（离线可判、不需窗口）**：`ProjectileView.cs` 新增纯函数 `SortZFor(id)` = `1e-4 + (id%9000)×1e-4` + `WorldPosOf()` 作表现层唯一产地；`movecheck §12` **11 项全 OK**（`109/0`）；**变异自检真跑**（把 `SortZFor` 改成 `return 0f` ⇒ **5 项 FAIL / exit=1**，还原后 `restored_identical=True` 且 `109/0`）⇒ ⛔ **tie-break 依据 = `id` 升序**（**本项目新增的确定性规则**、原版无对应可见差异 ⇒ ⛔ 不许宣称"这是原版行为"，只作为"**消除交替**"） |
| W8 | `closefix` | `u27v10`（B 先 A 后）两档读数 | **口径先行**：主 = 同场景同行为 `sd`（绝对、**只在"未触顶帧"上算** = 剔 `abs(dt−1/target) ≤ 1%×T` 的帧）；辅 = **同一帧集**的 `CV`；另报 **≥100ms 单帧"次数 / 位置 / 是否同点重现"**（键 = `scen/spr@cad`，⛔ 不按帧号） | driver 已改、已过编译哨兵（`CSC-EXIT=0` 离线可复现）⇒ **已改未验证**；⚠️ **首次尝试（14:23:39）死在 `RECOMPILE`、未产出 TSV，留下僵尸锁（`d2u27-u27`，PID 53472 已死、锁龄 73min）** ⇒ 由 `jitter` 处置；⛔ 不据此改生产帧节奏 |
| W9 | `u44impl` | 中文名字牌黑底**绝对宽度**（`NameplateSizeFor` 实渲染 vs 文本实测） | 与 `ScreenLabel.cs` 口径（文本外接 + padding 6/6/0/4）**逐像素相等** | 离线宿主无 CJK 字模宽表 ⇒ 只能实机采 |
| W10 | **用户** | **变亮幅度与原版并排** | **HUMAN-ONLY**：需**用户给的原版实机图**（编辑器恢复也拿不到） | `HoverBrightness=3.0 / HoverContrast=1.01`，对照原版截图判"像不像" |
| W11 | `u52play`（+`charstat`） | 打开人物属性面板（`C`），看四维行右端凹槽 `Plus0..3` 在**属性点 = 0** 时的实态 | ① 箭头图形**屏上可见**（不是一块糊掉的暗方块）⇒ **D7 转"已修"**；② 若不可见 ⇒ 一并裁定 `CharacterPanel.cs:427` 的 **alpha `0.40`**（补原版灰态出处 **或** 取消置灰，⛔ **不许自创值**）；③ 同一次会话顺带复核 **D1~D6 版面** | 截图 + 运行时读数（`sprite / color.a / rect / interactable`）；预计 **4~6 分钟**，一次 Play 采齐 |
| W12 | `waypoint` | U32 **仅剩这一项真需要窗口（≈2 min）**：进营地 → 走到 `(31,26)` 点它 → **延迟 ≥2 帧再截面板图** | 图上必须同时看到 ① 外框 ② 两个目的地钮的**原版装饰底图** ③ 标签可读 ④「關閉」钮 | **为什么必须延迟**：`u32_u32p2_1_panel.png`（面板打开**当帧**截的）**看不到原版钮面装饰**，而更早批次 `uifix4_wp_panel.png` 能看到 ⇒ 推断是 `UiArt.Art` 走 `Game.Res.LoadAsset` **异步**、当帧仍是深色在途占位（`WaypointPanel.cs` Build ③ 段）；⛔ 不延迟就再跑一次仍是同一张在途图 = **白跑窗口**。**已排除的非窗口项**：R1 数值半（①-d 闸门当场关）+ 像素半（0 渲染写入方 + 素材 0 文件 ⇒ 锚点格**必然无贴图**，窗口里那条降级为**复核**）；R3「图未落盘」**已补**（`u32p2` 批次四张图四帧；R3 守卫实测 = 面板帧 99 → 落地帧 101 差 2 帧）|
| W13 | `waypoint` | U32 R2 落地黑窗 —— **只剩要你裁的裁决，不是窗口读数** | ① **像素半当场可关**：读 `u32_u32p2_2_landed.png`（落地帧 101）⇒ **不是黑屏**（可见旧区地形 + 「ENTERING Blood Moor」+ 中文加载行）⇒ "落地即整屏黑"在 `u32p2` **未复现**（台账引的"整屏黑"来自更早批次）；② **数值半是变量**：`BLACKWINDOW missAct>0 持续` = **0.428s**（`u32play` 10:40:38）vs **0.934s**（`u32p2` 11:49:35），**差 2.2×** ⇒ 台账那 0.428s 是**单次采样**（已标注）；且该 metric = "缺块数 > 0 的持续时长"、**≠"整屏黑"**（两件事） | ⛔ **节奏数字不动**（`ChunkRefreshInterval` 0.25s / 首铺预算属另一件事）⇒ 留给你/引擎方定 |

**已覆盖 / 不必单采（⛔ 别让两片各采一遍）**：`popupaudit` 清单的 **P-5**（`ShopPanel` 修理/关闭按钮底板）**就是 `shopart` 的判定对象**，13:22 合规会话**已采到**（`.ai-tmp/screenshots/shopgrid_evidence.txt:22/:24` = `SHOPART-BTN name=RepairAll sprite=buysellbtn_2 color=1,1,1,1` / `name=Close sprite=buysellbtn_10 color=1,1,1,1`）；离线版 = `uicheck` 段 `(u53-shopart)` 30 行、被判定源 `ShopPanel.cs` 两次采样逐值相同 ⇒ **P-5 标"已由 `shopart` 覆盖"**。**P-6**（`CharacterPanel` 数字正确性）归 `charstat`，且**已有离线判据**（`playercheck` 5 职业逐一对上）⇒ 只剩 W11 那次顺带核对。
**"只有用户能给"清单（⛔ 与"等编辑器"分开）**：**W10**（原版实机图，判变亮幅度像不像）、`popupaudit` 的 **P-7**（关闭「X」图形 = Unity sprite/非 DC6、mpq 按名穷举 MISS ⇒ 缺素材）、`waypoint` 的 **R1.2**（`d2exp.mpq` 或含 `data/global/objects/wp/*` 的归档）、**R1.4**（`MINIMAP/mapicons.DC6` 8 帧**权威语义映射**缺失 ⇒ `ResPaths.cs:324` 已登记 BLOCKED、统一用帧 0，⛔ **不许自指定**）。

🚩 **新发现（跨域，**定案到"看得见"、未定案到"为什么"**；`waypoint` 只报读数与出处、未改任何东西）**：换区时**加载行写着"出发地"** —— 一手证据（**crop 级、非降采样**）= `u32_crop_landed_center.png`（`u32play` 批次落地帧的裁切，11:29 生成，184 KB）同屏两条文案：上方红字横幅「**ENTERING BLOOD MOOR**」+ 中下方灰字加载行「**正在進入 Town…**」⇒ **目的地与"正在进入"的对象不一致**（用户可见）。**代码事实（三条，逐条给行号）**：① 全 client **唯一**一处 `ShowLoading` 调用 = `Module/Flow/AppFlow.cs:1470` `Game.UI.ShowLoading($"正在进入 {to}…")`，`to` = **目的地**（`:1459-1460` `var from=_area; _area=to;`）；② 引擎 `LoadingLayer.Show`（`clover-client-unity-engine/Runtime/Presentation/UIWidgets.cs:493-496`）**每次都刷新 `_label.text`** ⇒ **已排除"标签没刷新"**；③ 时序（`u32_evidence_u32p2.txt`）= `AREA-CHANGED area=0 frame=86` → `TRAVEL-REQ dest=1 from=0`（11:49:30.559）→ `AREA-CHANGED area=1 frame=101` + `LANDED area=1`。⇒ **进入 area=1 时加载行仍写着 area=0 的名字**；**排查入口** = `AppFlow.EnterArea` 在 travel 路径上是否有**早退**使 `:1470` 未执行（`:1452-1458` 的 `_dupExitCount` / `_switchingArea` 守卫），或横幅与加载行的更新时序。**归属** = `AppFlow.cs` / `LevelEntryTitle.cs` / `LoadingCheck` 判域（⛔ **不是 U32**）⇒ **待该片 owner 认领**；⚠️ `waypoint` 另在降采样图 `u32_u32p2_2_landed.png` 上读到**同一形状**（疑似两批都复现），但它**不把降采样观察当定案** ⇒ 认领者请照 crop 的做法**裁一次**。

**⚠️ 本节变更（2026-09-24 16:0x）**：
- **W7 = ✅ 已离线闭合，不必进窗口**（`zorder` 多帧证据）：真 A* **逐帧 60 帧 × 三形**，逐帧取两枚投射物的**实际 transform z** 算"谁在前" ⇒ **翻转 0 次 / 不可判别 0 帧 / #1 在前 60/60**；旧口径（两枚 z 都 = `WorldOf` 的 0）⇒ **每帧都不可判别 60/60**（= 原状：次序交给引擎提交顺序）⇒ **机械论证 = 三条不变量**，⛔ 不是"跑了 60 帧没看到"。⚠️ 它的**第一版判据写过头**（要求"键元组 `(order,zA,zB)` 逐帧恒定" ⇒ 位形 3 判红 **29/60**）—— 真因 = **`EntitySortOrder` 随格而变**（取值集合 `342,342 ｜ 338,338 ｜ 334,334`，两枚**同步**变 ⇒ 次序不变）；**"键恒定"恰恰是旧口径的病征** ⇒ 已改成"**每帧仍可判别**"。（**`SortZFor(id)` 的原版依据 = 无**：`参考工程_Diablerie` 全树 `.cs` = **0**；官方 `Missiles.txt` **171 列**逐列扫 `draw|layer|order|sort|prior|render|z` 只命中 `zoffset`/`Size` ⇒ **客户端数据里没有次序定义**）⇒ 口径照旧：**本项目新增的确定性规则，⛔ 不代表原版可见行为**。
- 🚩 **新登记（无出处主张，待改）**：`Module/View/ViewModule.cs:1376` 写着「**原版口径：原版同格实体的绘制次序是稳定的生成序号（不是每帧重排）**」—— 全仓 grep `生成序|创建序|生成序号|creation order|list order` **只此一处命中**（零第二处佐证），与上面"官方表无次序列 + 无参考 `.cs`"**冲突** ⇒ 应改成「**本项目口径 = 创建序（`EntityId` 升序纯函数），⛔ 不代表原版可见行为**」⇒ **派 `u44impl`**（单行注释、零逻辑）。
- 🧾 **归属补记（按"内容自证"归位 —— 这是"⛔ 不许凭印象认领"的**唯一例外**，且必须三条齐）**：`client/Assets/Scripts/UI/HudPanel.cs` 那处 **hunk `@@ -255,2 +255,8 @@`（注释-only，+6 行）归 `waypoint`** —— ① **文本自证**：hunk 里两次点名 `waypoint`；② **引文自证（最硬）**：它引的读数**逐字存在于该片证据**（`u32_evidence_u32play.txt` 的 `AREA-CHANGED area=0 frame=75 t=18.802`）；③ **时间窗**：`HudPanel.cs mtime 11:59:30` 落在该片 L3 窗口内、晚于其 11:49 批次（且与 `closefix` 报的 `ea677f5695bde23b / 76533 / 1341 / 11:59:30` **逐字节一致** ⇒ 同一版、非中间态）。

🚩 **新缺陷（有主，2026-09-24 16:0x；`u52resist` 新判据揭穿）**：**`CharacterPanel` 右上「经验 cur/next」在最坏组合下放不下** —— `Band2Right` 框 **118 art**，最坏组合（`exp`/`expNext` 各 **10 位**）**need = 152 art vs availPx = 99 ⇒ 余量 −53 art**（判据读数 `[FAIL]`；值域取自 `Experience.tsv`：等级 1..99、`maxExp=3837739017`）；退化样本(a) 框=最小可放−1 ⇒ 折行 90 个、(b) 加长 40 位 ⇒ need 282 ⇒ 折行。⚠️ **该条目前让 `uicheck` `FAILcount=1`** ⇒ **属"有主红"（归 `charstat` 定因/修），⛔ 不是谁把全量弄红**。
⇒ **处置（team-lead）**：**⛔ 不降级判据、不登记允许差异、不按"保留红等窗口"收** —— 先派 `charstat` 做两件**离线**事：① **判"最坏组合在 1..99 内是否可达"**（可达 ⇒ 真缺陷必修；不可达 ⇒ 是判据**值域取错**，应按实际可达组合收紧，⛔ 不许借"不可达"把判据删掉）；② 给**原版口径**（一手原版 prefab 里「经验 cur/next」怎么显示：字号/框宽/是否缩写）⇒ 有了出处才允许改（缩字号 / 改文案 / 加宽框，⛔ **不许自选**）。**修完该红自然消失**，⛔ 不用"降成 LOG-ONLY"来腾地方。
⚠️ **同轮该片还揭穿一处旧假绿**：`uicheck ⑧` 的 §⑥ 原读数把**已 ×K 的画布 px** 当**裸 art** 传给 `Row()`（内部再 ×K ⇒ `availPx` 放大 **K²≈3.24×**）；修后与该片 ⑨ 的判决**一致**（两条路不再漂）。

⛔ **窗口使用纪律**（沿用既有）：取锁前确认 `playMode=stopped`；`editor_stop` → 回读 `stopped` → 才放两位锁；跑完按上表逐项核销。

---

## 2026-09-24 中段 8 条裁决（片 `u3bverify` · **只读取证**；**本节取代上文 U33/U34/U40/U41/U42/U45/U46 与「道具没法拖动」那 8 行的状态列**）

> 起因：那 8 行的状态列停在 **09-23 16:02**（提交 `a34af30c`），而之后约 25 笔提交打在它们身上、台账没回写。
> 口径：**提交信息按 L1 处理**（只用于定位判据，⛔ 不作证据）；只有 **L2**（脚本采集产物 + 台账）或 **L3**（被测程序自写标记）才可写「已修有据」。
> 裁决表 = `.ai-tmp/test/u3b_verdict.tsv`（固定 10 列 × 8 行）；报告 = `.ai-tmp/test/report-u3b-verify.md`；
> 本片**未改任何生产文件**（`client/**` / `策划/**` / `tools/**` / skill 全部只读），未进 Play、未取锁。
> 复跑口径 = 逐宿主 `cd tools/probes/hosts/<name>; dotnet run -v q --nologo`，原始输出 `.ai-tmp/test/u3b-<host>.txt`。
> **结论分布**：**6 条「已修有据」**（U33/U34/U40/U42/U46/道具拖动）+ **2 条「需裁决」**（U41/U45：用户原话无指认对象 ⇒ 只有眼睛能判 ⇒ **需用户点位置**）。

| # | 现象（用户原话） | 根因 / 现状（定案） | 归口 | 状态 | 修复 / 证据（**带级别与产物路径**） |
|---|---|---|---|---|---|
| U33 | 鼠标在人附近移动没效果，必须要远 | 旧口径要求「鼠标格 == 怪格」才命中；`33f75bb5` 改为「鼠标世界点落在怪**贴图矩形**内 ⇒ 命中该怪」（含怪上半身格），`f2865570` 补齐 `HoverTargetChanged` 链（不再静默失效） | `Module/Input/HoverPicker.cs`、`Module/View` | **已修有据**（L2） | `playercheck` **407/0**：`[ OK ] ★ 新口径：贴图矩形覆盖到鼠标世界点 ⇒ 命中该怪（cursor=Attack / id 正确 / 格=悬停格）(hasTarget=True cursor=Attack id=1007 格=(13,16))` + 旧口径不变 + 矩形外不命中 + 无矩形退回旧口径（`.ai-tmp/test/u3b-playercheck.txt:992-996`）；`uicheck` **1050 行全 OK**：`离玩家 1 格内世界坐标 → 格号精确命中（不吸附到玩家格）` + `远离玩家 8 格处同样精确`（`.ai-tmp/test/u3b-uicheck.txt:866-867`）。⚠️ **残余 N1**：实机「悬停怪身」**尚无 L3 行**（盘上实机 `[Hover]` 只有 NPC `cursor=Interact id=0「阿卡拉」` `.ai-tmp/screenshots/x_evidence_do2.txt:2633`；`u44hover_evidence.txt:11` 明写 `P2 SKIPPED: no monster in this area (area=Town)`）⇒ 待一次实机冻结帧（**非 BLOCKED**） |
| U34 | 鼠标放在按钮上，按钮是花的。文字太小了 | 字号族 15 处 magic number（U50 已统一到 `UiLayoutGame.FontPx*`）；「花」= ① 按钮 label 全项目**近黑**（对比度 **2.79:1** < AA 4.5:1）② 中等按钮**悬停帧取到 ACT1 麻点图**（麻点比 **0.424**） | `UI/UiArt.cs`、`UI/UiLayoutGame.cs` | **已修有据**（L2 + L3 图） | `uicheck`：字色 **4.70:1**（对照改动前 2.79:1 ✗）、`按钮 label 对比度 ≥ 4.5:1 逐屏（10 个屏/路径）`、`⛔ 字色不许在调用点自造（0 处）`、字模整体 **×1.8**、悬停帧 **麻点比 0.054**（旧 ACT1 麻点图 0.424 必红）（`.ai-tmp/test/u3b-uicheck.txt:131/479-485/475-477/989/992`）；量法 `（一次性判据脚本，2026-09-24 清理） + .tsv`；**L3** 实机图 `.ai-tmp/screenshots/x_x9_cursor_hover.png`（sha256 `4F49B638457D355771AEEB9FD840958F2B3F85AA16A43BCB42587CCE99D9E02B`）。⚠️ 该图**副本**曾被别片收尾清理 ⇒ 引用只走主路径，外部引用锚点见 `.ai-tmp/test/u3b-refs-anchors.tsv` |
| U40 | 红色哥布林移动就是一坨，没有音效，没有打击动画 | 三类各自定案：① **音效**逐类素材与接线都缺（`e4b668af` 按原版 `MonSounds.txt` 接线 / `3e68bad5` 补 `HitDelay`·`FsCnt` / `30cb771a` 修「尸体分支 continue ⇒ 死亡音永不播」）② **受击动画只播 3 帧**（`hitStun 0.18s < 动画时长`，`e4b668af` 修；登记 `E28`）③ **移动**：AI 出手判定与结算层口径不一 ⇒ 怪停在格距 2.00 永不再靠近（`4099b5eb` 统一 `AttackDistance`；`3d35ee8f` 出手前线段自检） | `Module/Monster`、`Module/Audio`、`Module/View` | **已修有据**（L2 + L3） | `audiocheck` **98/0**：逐类 `Fallen(fa) 期望 Attack1=fallen_attack_1 / Hit=fallen_hit_1 / Death=fallen_death_1 / Footstep=light_walk_dirt_1 ⇒ 已挂载 monster_{hit,atk,die,step}_fa 四键全到位`（`.ai-tmp/test/u3b-audiocheck.txt:267-277`）；`animcheck` **87/0**：`fa 受击帧序列 0..6 = 全部 7 帧（@12fps=0.583s）`（旧口径只出 3/7）（`:130-131`）；`combatcheck`：`Melee 会靠近 (5.00 → 1.00)`（`:242-243`）；**L3** `SFX-START … clip=monster_hit_zm / monster_hit_fa`（`.ai-tmp/screenshots/msc2_evidence.txt:100/104/132`）+ `[AV2] FRAME … sprite=hit_s_0..hit_s_4 action=hit`（`av2b_evidence.txt:34-39`）。⚠️ **残余 N2**：「移动就是一坨」是**观感**（表现类）⇒ 待一次联络图 |
| U41 | 贴图问题。地图边界问题 | 边界族三条：① 相机无障碍夹制 ⇒ 贴边时**~31.3% 屏幕是地图外虚空**（`c45a2ef2` 改 `CameraBounds` 格空间夹制 / `0786a575` 拆 `camera(被夹)`·`focus(位移上限)`）② 野外出生点恒贴西边界 `(1,y)`（`c2ba0c0c` `EntryMarginCells=8`）③ 城镇营地外缺边界带（`b455112d` 用原版 `town_trees` 封 **688 格**）；另有血沼泽/换区落地黑（`1c15f432`/`9ff74d5b`） | `Module/Map`、`Module/Camera/CameraRig.cs` | **边界子项已修有据（L2）；「贴图问题」需裁决** | `mapcheck`：`✅ DEFECT-CLEARED 生产夹制不再放行该机位（ClampFocusGrid 把 (-19,-11) 移到 (-13.4,-13.8)）⇒ 修前 31.3% 屏是图外虚空，修后同口径 = 0%`（`.ai-tmp/test/u3b-mapcheck.txt:7224/7229`）+ `MapGenTown 边界环封闭 688 格 ⇒ 营地外可走区离四边界恒 ≥ 8 格`（`:18`）+ `Wilderness 873 格 / 周圈 31/31 块精确匹配`（`:97/3379-3384`）。⚠️ **「贴图问题」N3 = BLOCKED（只有用户能给）**：离线只能证「被布局引用的平色瓦片**未处置 = 0**」（`:5327`），**无法判"用户说的那张"** ⇒ **需用户点位置 + 同机位并排图** |
| U42 | 为什么没拦住 | 结算层 `LineClear` 拒绝时**只打日志**（method void / 契约冻结），**发起方从不检查** ⇒ 怪隔墙挥空（`3d35ee8f` 新增 `CombatModule.AttackLineClear` 唯一口径 + `MonsterAi` 四种 AI 出手前自检、不通走绕路；`4099b5eb` 统一 `AttackDistance` 消 5~8 格死区） | `Module/Combat/CombatModule.cs`、`Module/Monster/MonsterAi.cs` | **已修有据**（L2） | `combatcheck` §20：`★ 线段被阻断时 出手 0 次（被挡帧 88；修前每 1.10s 一次 ⇒ 必然 > 0）` + `★ 结算层一次都没因线段阻断拒绝（发起方已出手前自检）(被拒 0 次)` + `用例真的成立：窗口里有线段被阻断的决策帧（88/88）` + 对照 `同距离无墙：2.20s 内出手 2 次`（`.ai-tmp/test/u3b-combatcheck.txt:2302-2313`）+ `隔墙：怪没掉血 (4→4)` + `投射物在墙格消散 (hitTerrain=True cell=(11,8))`（`:1779-1790`） |
| U45 | 你看你这B贴图 | 已知三类具体缺陷的根因：① 地面物品**永不取贴图**（`ApplyFrame:1443` 对 `IsGroundItem` 直接 return；`067f8328` 修）② 金币 `itemId<=0` 早退 ⇒ **白方块**（`369910a1` 走原版 `invgld`）③ automap 地面 `seq=0` 兜底缺失 ⇒ 城镇/野外地面空白（`0e74f98a`） | `Module/View/ViewModule.cs`、`Module/Map` | **需裁决**（原话未指认具体图元） | `mapcheck §19`：`D2/** 共 18911 张 PNG；实心单色 = 197 张` + `✅ 被布局引用的平色瓦片全部已有处置（PaletteCycledFlatWallTiles 白名单）—— 实测被引用 1 个 / 未处置 0 个` + `未被引用 87 个（= 零可见影响）`（`.ai-tmp/test/u3b-mapcheck.txt:5319-5328`）。⚠️ **N3 同 U41 ⇒ BLOCKED（缺的只有用户能给）**：需用户指认「哪一张图/哪个位置」+ 同机位并排图 |
| U46 | tab 渲染地图不对，背景不用压暗，你去搜一下好吗？ | ① **中心错**：`MapModule` 没把玩家当前格填给 automap ⇒ `mapPlayer` 恒 = 出生点、`anchored` 不跟人（`73f71b74` 修）② **揭示太窄**：`MarkExplored` 只揭示「本格 + 8 邻域」⇒ 改按**半径 6 源头揭示**（`exploredCount 86→186`）③ **压暗**：`MiniMapPanel.BackdropAlpha=0` + 整屏素材亮度常量 = **纯白** | `UI/MiniMapPanel.cs`、`Module/Map` | **已修有据**（L3 + L2） | **L3** `.ai-tmp/test/amp_done_am3.txt.side.txt:11`：`READINGS=PANEL=open … exploredCount=186/2240 | overlayActive=1 | anchored=(172.80,28.80) | markers=8 | livePlayerGrid=(22,26) mapPlayer==livePlayer=1 | screen=1920x1080`（缺陷态对照 `amp_readings_am3.txt:17-18`：`mapPlayer=(spawn=32,28)` vs `livePlayer=(22,26)`）；**L2** `uicheck`：`原版亮度常量 = 纯白（不做任何压暗）`、`叠加层铺满画布 + 以玩家格为中心平移`、`MaxiMap 帧数=1260 / cel 帧尺寸=16×32`、`逐格图形 = 原版 cel`、`automap 贴图尺寸对 56×40 非退化 (tex=768x408)`（`.ai-tmp/test/u3b-uicheck.txt:349/641/642-649/896`） |
| — | **道具没法拖动！**（同批第 25 条） | **原根因（台账记的「卡在契约层」）已消**：`AppEventRouting` 收到 `MoveInInventoryRequest` 原先**只打 Warn**（落格没地方去）⇒ `IItemModule.MoveItem(from,to,out reason)` 补齐后**真的转发**，失败同一句话既写日志又弹 Toast（`33f75bb5` UI 侧拖影 + 目标格高亮 + 落格链路；`cb614e6c` 把拖拽驱动提升为判据资产） | `UI/InventoryPanel.cs`、`Module/Item/**`、`App/AppEventRouting.cs:147-162` | **已修有据**（L2） | `itemcheck` **exit=0**：`17a 空格放下 MoveItem(0→5) 成功、总件数不变、物品真的换格` + `17b 交换` + `17c 拒绝后两件都留在原格`（`.ai-tmp/test/u3b-itemcheck.txt:375/377/383`）；`uicheck`：`背包内拖放 ⇒ MoveInInventoryRequest 恰好 1 次，载荷 = 2 | (5<<16)`、`拖到面板外 ⇒ ItemDropRequest 恰好 1 次`、`PlanDrop 六态`（`.ai-tmp/test/u3b-uicheck.txt:330-331/872-879`）；源码 `App/AppEventRouting.cs:147-162` 已由「只打 Warn」改为真转发。⚠️ **残余 N4**：实机拖放手感（拖影跟随/吸附）属表现类，未采 |

**本节引出的未闭环（如实留档）**：
- **N1**（U33 实机怪身悬停 L3 行，非 BLOCKED ⇒ 可进 Play 采）、**N2**（U40 观感联络图）、**N4**（拖拽手感）→ 均需一次 Play，非阻塞。
- **N3**（U41/U45「贴图」的具体对象）= **BLOCKED：缺的只有用户能给** —— 需用户指认哪张图/哪个屏/哪个位置，判定权归人（同机位并排图）。
- **N5（独立缺陷，非这 8 条）**：`mapcheck` 现 1 项失败 = `❌ D2/** 下空目录 = 0`（现存 1 个 `/Shaders`）⇒ 已退给 `u44impl`（裁决：把着色器挪出 `D2/` 树，`D2/**` 语义是"原版素材"）。
- **外部引用锚点**：本节 8 行引用的他片共享产物已做一次性哈希锚点 `.ai-tmp/test/u3b-refs-anchors.tsv`（9 行；已抓到 1 条真实漂移 = 某图副本被别片收尾清理 ⇒ 引用已改存活主路径）。
