# 素材调研 —— 暗黑破坏神 II（参考游戏 A）

> **本文件是 skill「闸门 3：素材来源」的必交产物。** 没有它 = 素材闸门未通过。
> 规则：**级 ①（A 自己的素材）穷尽之前，禁止下载 / 禁止接入任何通用兜底素材。**
> 量化门槛：**≥ 4 轮**不同关键词（中英各半）× **≥ 3 类站点** × 换过**格式 / 打包方式**。

- 项目：`clover-project-diablo2`
- 参考游戏 A：**Diablo II / Diablo II: Lord of Destruction（暗黑破坏神2 / 毁灭之王）**
- 调研时间：2026-09-16
- 形态判定：**单机**（见 `策划案/暗黑破坏神2参考规格.md` §0）

---

## 0. 结论（一句话）

**级 ① 部分成功，且已拿到"关键那一半"；剩余部分需要你的游戏本体文件（级 ④）。**

| 素材类别 | 来源级别 | 状态 | 说明 |
|---|---|---|---|
| **UI / 菜单 / 面板 / 原版位图字体** | **① A 自己的素材** | ✅ **已拿到** | 来自社区 D2 复刻工程 `mofr/Diablerie`（其 README 明示"所有图像与音效均取自 Diablo 2"），含原版主菜单/职业选择屏/载入屏/控制面板/血蓝球/背包/人物属性面板/光标/字体 |
| **游戏数值（怪物/技能/物品/词缀/地图/经验/掉落）** | **① A 自己的素材** | ✅ **已拿到** | 官方 1.10f 数据表 92 个 txt（`MonStats.txt` / `skills.txt` / `Levels.txt` / `MagicPrefix.txt` / `TreasureClassEx.txt` …），从 `d2data.mpq`+`d2exp.mpq` 解出，社区原样存档 |
| **地图区域布局数据（AutoMap）** | **① A 自己的素材** | ✅ **已拿到** | `AutoMap.txt`（157KB，含罗格营地/血腥荒野的自动地图形状） |
| **解包工具链（MPQ / DC6 / DCC / DT1 / DS1）** | **① A 自己的素材（工具）** | ✅ **已拿到** | `mofr/Diablerie` 内含 C# StormLib（`MpqArchive.cs` / `MpqFileStream.cs` / `MpqFileSystem.cs`）与 D2 各格式读取器 |
| **角色精灵（5 职业 8 方向动画）** | **① 需要 A 的本体文件** | ⏳ **等用户（级 ④）** | 在 `d2char.mpq` 内（`.dcc`）。archive.org 直链在本机**不可达**（见 §3 第 5 轮），故需你提供本体 |
| **怪物精灵（堕落者/僵尸/尖刺鼠/堕落罗格…）** | **① 需要 A 的本体文件** | ⏳ **等用户（级 ④）** | 在 `d2data.mpq` 内（`.dcc`） |
| **地形瓦片（野外/洞穴/城镇）** | **① 需要 A 的本体文件** | ⏳ **等用户（级 ④）** | 在 `d2data.mpq` 内（`.dt1`） |
| **地图预设/迷宫（DS1）** | **① 需要 A 的本体文件** | ⏳ **等用户（级 ④）** | 在 `d2data.mpq` 内（`.ds1` / `.dt1`） |
| **音效 / BGM / 语音** | **① 需要 A 的本体文件** | ⏳ **等用户（级 ④）** | 在 `d2sfx.mpq` / `d2music.mpq` / `d2speech.mpq` 内（`.wav`） |

> ⛔ **未使用任何通用兜底素材**（KayKit / Quaternius / Kenney / 内置几何体一律未接入）。
> `_assets_tmp/` 里存在的 KayKit 包**与本项目无关**，本项目不引用。

---

## 1. 搜索矩阵（关键词 × 站点 × 格式）

### 1.1 关键词（已执行，中英各半）

| # | 关键词 | 结果 |
|---|---|---|
| 1 | `Diablo 2 sprite assets pack ripped tiles characters github` | 命中解码器与工具，无现成素材仓库 |
| 2 | `暗黑破坏神2 素材 提取 sprite 图集 MPQ 解包 github 复刻 开源` | 命中 MPQ/CASC 解包工具链（MPQEditor / CascView / bin2txt / D2RMM）、爱给网素材包 |
| 3 | `github diablo 2 dc6 dt1 dc6 sprites extracted assets repository dc6 decoder` | 命中 DC6 解码器若干 |
| 4 | `The Spriters Resource Diablo II sprite sheets download` | 命中 TSR D2 页面，但**站点对自动抓取返回 403** |
| 5 | `Diablo 2 Resurrected ripped models textures assets github download datamined` | 命中 D2R `.sprite` 转换器、D2R modding 工具站 |
| 6 | `暗黑破坏神2 素材包 UI 素材 图标 贴图 下载 爱给网 NGA 贴吧 网盘` | 命中**爱给网「《暗黑破坏神》完整游戏素材包」**（78 张 / 132.5MB），**需登录才能浏览/下载** → 归入级 ④ |
| 7 | `github diablo 2 dc6 converted png sprites pack assets repository download all monsters tiles` | 无现成素材仓库 |
| 8 | `OpenDiablo2 extract mpq command line tool github` | 命中 **`OpenDiablo2/utils/extract-mpq`**（命令行 MPQ 解包器） |
| 9 | `archive.org diablo 2 assets sprites tiles sounds pack download` | 命中 **archive.org 上暗黑2 完整版**（含 zh-TW 官方中文版，2.0GB） |
| 10 | `CC0 isometric ARPG assets pack monsters demons zombies skeleton animated free github clone` | **通用兜底素材**（Quaternius / Kenney 等）—— **按闸门 3 不采用** |

### 1.2 站点（≥ 3 类，均已访问）

| 类别 | 站点 | 是否可用 |
|---|---|---|
| 代码托管 | `github.com`（含 GitHub Search API，共 10 组查询） | ✅ 可访问、可 clone |
| 同源镜像 | `gitee.com` / `gitcode.csdn.net` | ✅ 仅有工具镜像，无素材 |
| 素材站 | `spriters-resource.com` | ⛔ **403 Forbidden**（反抓取），需人工浏览器打开 |
| 中文素材站 | `aigei.com`（爱给网）、`yswgame.com`、`diablofans.com.cn` | ⛔ **需登录 / VIP** → 归级 ④ |
| 论坛/社区 | `tieba.baidu.com`、`ngabbs.com`、`diablo2.com.cn`、`91d2.cn`、`d2rmodding.com` | ⚠️ 有工具与教程，素材多为网盘附件（需登录） |
| 作品存档 | `archive.org` | ⛔ **下载端点本机不可达**（HTTP 000，见 §3 第 5 轮） |
| 包索引 | `pkg.go.dev`、`poly.pizza`、`quaternius.com` | ✅ 可访问（后者为通用素材，不采用） |

### 1.3 格式 / 打包方式（均已覆盖）

`.dc6`（UI 与动画帧）· `.dcc`（角色/怪物压缩动画）· `.dt1`（等距地形瓦片）· `.ds1`（地图预设/迷宫）·
`.cof`（朝向动画配置）· `.pal`（调色板）· `.tbl`（本地化字符串）· `.mpq`（打包归档）· `.wav`（音频）·
`.txt`（官方数据表）· 以及 **DC6 → PNG 已转换产物**。

---

## 2. 逐轮记录（可复查）

| 轮 | 动作 | 站点 | 拿到什么 | 可用性判定 |
|---|---|---|---|---|
| 1 | 搜索 D2 素材包 / sprite sheet | 通用搜索 | 无直接素材仓库；大量解码器 | ❌ 素材未命中，工具命中 |
| 2 | 中文关键词搜素材包 | 百度系 / 爱给 / 贴吧 | 爱给网「暗黑破坏神完整游戏素材包」78 张 132.5MB；diablo2.com.cn 周边下载 | ⛔ **需登录/会员** → 级 ④ |
| 3 | 直接抓 TSR 暗黑2 精灵表页 | spriters-resource.com | — | ⛔ HTTP **403 Forbidden** |
| 4 | GitHub Search API 穷举 10 组关键词 | api.github.com | 见 §3 候选清单 | ✅ 命中**工具链 + 社区复刻工程的已转换素材 + 官方数值表** |
| 5 | 探测 archive.org 完整版本体 | archive.org | 元数据可取（含 **zh-TW 官方中文数字版 2.0GB**、4 张 ISO） | ⛔ **下载端点全部 http=000**（`archive.org/download/*`、`iaXXXXXX.us.archive.org/*`、http/https 皆不可达；无代理环境变量）→ **必须用户提供本体** |
| 6 | `git clone` 候选仓库并逐一开箱 | github.com | **`mofr/Diablerie` 11.35MB：原版 UI 贴图 49+ 张、原版位图字体、MPQ/D2 格式 C# 读取器、211 个 cs**；**`SheXMods/d2lod1.10txt` 2.96MB：官方 92 张数据表** | ✅ **级 ① 成立** |
| 7 | 记录来源与落地路径 | — | 见 §4 | ✅ |

---

## 3. 候选清单（含"为什么可用 / 不可用"）

### 3.1 ✅ 已采用（级 ①：A 自己的素材）

| # | 仓库 / 站点 | 内容 | 为什么可用 |
|---|---|---|---|
| 1 | `https://github.com/mofr/Diablerie` | Unity 上的暗黑2 复刻工程。**内含从 D2 本体解出的原版贴图**：`Assets/Images/ControlPanel/`（49 张：控制面板底图、血球 `healthbar.png`、蓝球 `manabar.png`、经验条、菜单按钮 4 帧、小面板按钮 16 帧）、`Images/Menu/`（主菜单、职业选择屏、载入屏、多人屏、按钮）、`Images/Panels/`（`inventory.png` 背包、`charstat.png` 人物属性、`buysellbtn`、`goldcoinbtn`）、`Images/Inventory/`（6 张装备栏底图）、`Images/Cursors/Cursor.png`、`Images/Skills/SkilliconAttack.png`、`Resources/Fonts/{font16,24,30,42}.png`、`StreamingAssets/data/local/font/*.DC6` | 其 README 明示 *"All images and sounds used are from Diablo 2"* ⇒ **就是 A 的素材**，只是已被社区转成 PNG。非商用项目可直接用（record 来源一行） |
| 2 | 同上 | `Assets/StormLib/{MpqArchive,MpqFileStream,MpqFileSystem}.cs` + `Assets/Scripts/`（211 个 cs，含 D2 各格式读取器）+ `Samples` 场景 | **解包工具链**：给我 `.mpq` 我就能解出 `.dc6/.dcc/.dt1/.ds1/.wav` 并转 PNG。**这是级 ④ 只缺"本体文件"的原因** |
| 3 | `https://github.com/SheXMods/d2lod1.10txt` | 暗黑2 **1.10f 官方全部数据表 92 个 txt**：`MonStats.txt`(413KB)、`MonStats2.txt`、`skills.txt`(173KB)、`SkillDesc.txt`、`SkillCalc.txt`、`Weapons.txt`、`Armor.txt`、`Misc.txt`、`ItemStatCost.txt`、`MagicPrefix.txt`、`MagicSuffix.txt`、`UniqueItems.txt`、`Sets.txt`、`Runes.txt`、`gems.txt`、`Levels.txt`、`LvlPrest.txt`、`LvlMaze.txt`、`LvlTypes.txt`、`LvlWarp.txt`、`AutoMap.txt`、`experience.txt`、`charstats.txt`、`TreasureClass.txt`、`TreasureClassEx.txt`、`Missiles.txt`、`MonAi.txt`、`AiParms.txt`、`MonUMod.txt`、`SuperUniques.txt`、`objects.txt`、`npc.txt`、`Sounds.txt`(505KB)、`MonSounds.txt` … | 从 `d2data.mpq` / `d2exp.mpq` / `patch_d2.mpq` 解出的**原版数值**。本项目所有配表以它为真源 ⇒ **核心逻辑的数值不再靠猜** |

### 3.2 ✅ 已确认可用（工具，级 ① 配套）

| 仓库 | 用途 |
|---|---|
| `OpenDiablo2/dc6`（Go） | DC6 解码/编码 |
| `OpenDiablo2/OpenDiablo2` → `utils/extract-mpq` | 命令行 MPQ 解包（`go install` 后指向 D2 目录运行） |
| `nebuladevs/diablo2-image-decoder`（Python） | `.dc6` → 带透明背景的 PNG |
| `nebuladevs/pcx-to-spritesheet`（Python） | 拼 sprite sheet + 去背 |
| `Donny-GUI/dc6-python`（Python） | DC6 读/写 |
| `AtsusaKai/D2RSpriteConverter` / `4KMong/D2RSpriteToolkit` | D2R `.sprite` → PNG（若用户装的是重制版） |
| `D2R-Gimli/D2_modding_tools` | DC6 Overview / DC6 Creator 等 |
| `kambala-decapitator/MpqSimpleIO`（C） | MPQ CLI |
| `jaenster/libd2`（Zig） | **确定性 D2 1.14d 引擎核心：地图生成 / 物品生成 / 运行时模拟** —— 随机地图与掉落算法的权威参考 |
| `AbyssEngine/AbyssEngine`（C，291★） | D2 引擎 clean-room 重实现（格式与流程参考） |
| `Lectem/Worldstone`（C++，82★） | D2 工具与引擎重实现 |
| `bethington/diablo-2-map-editor`（C） | DS1 地图编辑（地形格式参考） |

### 3.3 ⛔ 不可用（及原因）

| 来源 | 原因 | 归属 |
|---|---|---|
| `spriters-resource.com` 暗黑2精灵表 | HTTP **403**（反抓取），自动下载不可行 | 需人工浏览器 → 用户可选 |
| 爱给网《暗黑破坏神》完整游戏素材包（78张/132.5MB） | **必须登录**才能浏览压缩包 | 级 ④（用户） |
| archive.org 暗黑2 完整版（zh-TW 2.0GB / 4×ISO） | **下载端点本机不可达**（http=000，多端点、http/https 均失败，无代理） | 级 ④（用户） |
| `JeffVanGorp/Hero-Editor-Assets`（52★） | 内容是 `.d2i` / `.d2s`（Hero Editor 的存档与物品**数据**格式），**不含图形素材** | 不需要 |
| D2R 重制版 CASC `.texture` | 需用户本机安装 D2R + CascView | 级 ④（用户） |
| Quaternius / Kenney / KayKit 通用素材 | **是通用兜底素材（级 ③）**，闸门 3 禁止在级 ①② 未穷尽前接入 | ❌ 不采用 |

---

## 4. 落地路径与来源登记

| 落地 | 路径 |
|---|---|
| 调研缓存（gitignored） | `_assets_tmp/d2src/d2lod1.10txt/`（官方数据表 92 txt） |
| 同上 | `_assets_tmp/d2src/Diablerie/`（原版 UI 贴图 + 字体 + MPQ/格式读取器 + 211 cs） |
| 本项目引用（提交） | `策划/数值文档/`（由官方 txt 转成的源表）、`client/Assets/ThirdParty/Diablo2/`（原版贴图与字体） |

**来源行（记一行即可）**：
```
Diablo II / LoD 原始素材（贴图·字体·数据表）：
  - mofr/Diablerie            https://github.com/mofr/Diablerie            （原版 UI 贴图 / 字体 / MPQ 与 D2 格式读取器）
  - SheXMods/d2lod1.10txt     https://github.com/SheXMods/d2lod1.10txt     （官方 1.10f 数据表 92 txt）
  - 版权归 Blizzard North / Blizzard Entertainment；本项目非商用。
```

---

## 5. 级 ④：需要你提供的（其余全归我做）

**只缺一样东西：你电脑上的暗黑2 游戏本体文件。** 我不可能拿到它（archive.org 直链在本机不可达）。

| # | 要什么 | 为什么 | 从哪来 | 放到哪 |
|---|---|---|---|---|
| 1 | `d2data.mpq` | 怪物精灵（`.dcc`）、地形瓦片（`.dt1`）、地图预设（`.ds1`）、物品图标、场景对象 | 你的暗黑2 **1.13c/1.14d**（或 LoD 1.10+）安装目录 | `<项目根>/_assets_src/` |
| 2 | `d2char.mpq` | 5 个职业的角色精灵（8 方向行走/攻击/施法/受击/死亡动画） | 同上 | 同上 |
| 3 | `d2exp.mpq` | 毁灭之王扩展内容（部分怪物/物品/区域） | 同上 | 同上 |
| 4 | `d2sfx.mpq` | 全部音效（挥砍、命中、怪物叫声、UI 点击） | 同上 | 同上 |
| 5 | `d2music.mpq` + `d2xMusic.mpq` | BGM（罗格营地、血腥荒野） | 同上 | 同上 |
| 6 | `d2speech.mpq` + `d2xtalk.mpq` | NPC 语音（可选） | 同上 | 同上 |
| 7 | （可选）`patch_d2.mpq` | 若你的版本是 1.13c/1.14d | 同上 | 同上 |

**只放这一个目录就行，其余全归我做**：解包（StormLib / extract-mpq）、DC6·DCC·DT1·DS1 → PNG 转换、
建材质、建图集、接进 Unity 工程、生成 AnimatorController —— **全部我来**。
**解包工具不需要你装**（我已经拿到了 Diablerie 的 C# StormLib 与 OpenDiablo2 的解包器）。

> **在你把文件放进来之前**，我会继续做完**不依赖这些素材的部分**：
> 启动/菜单/创角链路、两张地图的关卡与随机地形生成、主线任务链、怪物 AI、战斗与伤害结算、
> 技能树、背包/装备/掉落、NPC 对话与商店、存档、音效触发点、HUD 与全部面板
> —— 这些用的都是**已到手的原版 UI 贴图 + 官方数值表**，以及引擎自己的能力。
> 届时**只换资源、不动逻辑**（资源引用已收敛到常量表）。

**如果放不进去或版本不对**：把安装目录整个拷到 `_assets_src/` 也行（我自己找 mpq）；
或者告诉我你只有重制版（D2R），我改用 CASC 工具链。
