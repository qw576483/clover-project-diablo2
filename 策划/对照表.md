# 对照表 —— 1:1 硬标准六维（skill §5）

> 由 `tools/probes/measure/build_parity_table.py` 生成（确定性、无时间戳 ⇒ 两次运行逐字节相同）。
> 每行三列：**原版值(出处) | 我们的值 | 差值**。
> 差值只允许两种取值：`0`，或 `策划/差异登记.tsv` 里真实存在的登记 id（如 `E42`）。
> ⚠ 模糊措辞一律不许；每个「原版值」的出处都必须在盘，本脚本每次运行逐条复核（任一漂移 ⇒ 不产出）。
> 六维来源 = 全局 skill `clover-engine` §5「1:1 硬标准六维」。

| 维度 | 原版值(出处) | 我们的值 | 差值 |
|---|---|---|---|
| 布局按原版像素 | 原版 `MainMenu.prefab` 的 `GameMenu/Buttons/*` 272×35（出处 `策划/自审对比/启动链路对照.md:20`） | `Single`/`Quit` 按钮 489.6×63 = 272×35 ×1.8（按高等比 + 水平居中；同出处） | 0 |
| 布局按原版像素 | 原版 `ClassSelectMenu/Canvas/SelectHeroClass` (0,267) 493×30（出处 `策划/自审对比/启动链路对照.md:35`） | `Title` 887.4×54 @(0,480.6)（= ×1.8；同出处） | 0 |
| 布局按原版像素 | 原版 `ControlPanel.prefab` Background 948×160（出处 `.ai-tmp/screenshots/w3_uigame_audit.tsv:2`） | `HudBgSize` = 1706.40×288.00（= 原版 ×K，两轴非等比 0.00%；同出处） | 0 |
| 布局按原版像素 | 原版 `FrontEnd/{cls}/{CLS}NU1.DC6` 帧 0 原生 118×198 / 58×183 / 86×183 / 82×179 / 89×166（出处 `策划/自审对比/启动链路对照.md:40`） | `PortraitAmazon` 212.4×356.4 = 118×198 ×1.8（共 5 个半身像；同出处） | 0 |
| 素材必须 A 原版 | 原版 `data/global/ui/Logo/logo.DC6` 帧 0（DIABLO II 火焰字标）原生 319×177（出处 `.ai-tmp/screenshots/w3_uiflow_audit.tsv:3`） | sprite=`logo_0`，实测 IHDR 319×177（= 原版 PNG 原样；同出处） | 0 |
| 素材必须 A 原版 | 原版 `MINIMAP/mapicons.DC6` 帧 0（16×16 ×8，白色模板）（出处 `.ai-tmp/screenshots/w3_uigame_audit.tsv:77`） | `MiniMap/mapicon_0.png` 实测 IHDR 16×16 → 28.80×28.80（= ×1.8；同出处） | 0 |
| 素材必须 A 原版 | 原版 `data/global/ui/MENU/EndGame.dc6` 页 0 四块 256×256 / 64×256 / 256×224 / 64×224 + `Pal.PL2`（出处 `策划/验收表.md:138`） | 工程 `endgame_{0..3}.png` 与独立 DC6 解帧 SHA256 全等 4/4（同出处） | 0 |
| 素材必须 A 原版 | 原版平色/模板帧：`Objects/warp` 81 帧（唯一色 #004430）/ `UI/MiniMap` 8 帧（白模板 #F4F4F4）（出处 `策划/差异登记.tsv:43` = E42） | 这些帧不参与落格、也不被任何帧号请求 ⇒ 对成品画面 0 影响 | E42 |
| 字体照原版 | 原版位图字模 `D2/Fonts/font16_chi.png` + `font16_chi_map.txt`（COLS 117 / CELL 13 13）（出处 `策划/自审对比/启动链路对照.md:49`） | 启动屏提示行 = 该字模合成模板：模板匹配 IoU 0.9743（同出处 :57）、墨迹色数 11（= 原版调色板 10）（:58） | 0 |
| 字体照原版 | 原版 `WideButton.prefab:68` `m_FontData.m_FontSize = 18`（Bold）（出处 `策划/自审对比/启动链路对照.md:26`） | 字模 `font16` 格子 16×18 → scale 2.025 ⇒ 32.4×36.45 画布px（= 原版字号 18 的位图档口径；同出处） | 0 |
| 字体照原版 | 原版 `Diablo_light.TTF @18px`：`SINGLE PLAYER` = 145 原版px（出处 `策划/自审对比/启动链路对照.md:70`） | `font16` 位图字模（18/16 换算后）`SINGLE PLAYER` = 129.4 原版px（同出处） | E21 |
| 色调不加滤镜 | 原版瓦片原色（`ACT1/Pal.PL2`），无滤镜（出处 `策划/自审对比/场景对照.md:69`） | 原版瓦片 PNG 原样、`SpriteRenderer.color = 白`、无色调乘数（同出处）；实机 `[Ui] [原版贴图] ... 色调=RGBA(1.000, 1.000, 1.000, 1.000)`（出处 `.ai-tmp/screenshots/t0-s2recap_evidence.txt:1057`） | 0 |
| 色调不加滤镜 | 原版精英 = 运行期 PL2 调色板变体（逐词缀调色板本机无出处）（出处 `策划/差异登记.tsv:41` = E40） | 本项目不加金色 tint（真素材到位后精英与普通怪同色） | E40 |
| 色调不加滤镜 | 原版河面 = 同 `river.dt1` 的 floor 水瓦片（wall 层纯色瓦片无运行期 PL2 循环）（出处 `策划/差异登记.tsv:33` = E32） | 不叠 `moor_river/028`（白名单恰 1 键；命中 49 格全在 x=47/54，仍为水=阻挡） | E32 |
| 交互反馈 | 原版 `WideButton.prefab:61` `m_Color=(0.098,0.098,0.098,1)` = `#191919`（出处 `策划/自审对比/启动链路对照.md:25`）；**该值的前提是「浅色石牌」** | 字模节点 `img.color = RGBA(0.95,0.87,0.60,1)`：本工程实际使用的原版按钮底图 `D2/UI/Menu/btn_med_normal.png` 内区实测平均 sRGB 亮度 **0.376**（量法 `tools/probes/measure/btn_plate_luma.py`，落盘 `btn_plate_luma.tsv`）⇒ `#191919` 只有 **2.79:1** < WCAG 2.1 AA 正文门槛 4.5:1（13px 中文密笔画糊成一块黑，实机 before 图 `.ai-tmp/screenshots/uifix4_z_before_btn1.png`）；现值 = **4.70:1** ✓（判据 = `tools/probes/hosts/uicheck` 的「逐屏按钮 label 对比度 ≥ 4.5:1」断言，⛔ 不再判字面常量） | E58 |
| 交互反馈 | 原版 `WideButtonBlank.dc6` 帧 0+1 常态 / 帧 2+3 按下（出处 `策划/自审对比/启动链路对照.md:24`） | `btn_wide_normal`（常态）/ `btn_wide_pressed`（按下，走 SpriteSwap）（同出处） | 0 |
| 交互反馈 | 原版 `CURSOR/Cursor.DC6` 5 态（攻击/交互/拾取/不可走 4 态本批素材没有）（出处 `.ai-tmp/screenshots/w3_uigame_audit.tsv:88`） | 5 态统一显示普通箭头，切到缺口形态逐态一条 Warn | E40 |
| 节奏 | 官方游戏循环 = 25 fps（R1 `gameserver.zig:675`）（出处 `策划/自审对比/数值对照.md:78`） | `MonsterTuning.LogicFps = 25`（换算基准）（同出处） | 0 |
| 节奏 | 官方 `CORPSE_TTL = 500` 帧 ≈ 20 s @25fps（R1 `gameserver.zig:3604-3606`）（出处 `策划/自审对比/数值对照.md:81`） | `CorpseLifetimeSeconds = 20`（改前 25；同出处） | 0 |
| 节奏 | 官方萨满复活冷却 = `aidel 15` 帧 ÷ 25 fps = 0.60 s（出处 `策划/自审对比/数值对照.md:82`） | `ShamanReviveCooldownSeconds = 0.60`（改前 4.0；同出处） | 0 |
| 节奏 | 官方：仇恨记忆/出手间隔/攻击动画时长/受击硬直/远程保持距离/逃跑触发 不是 txt 数值（在可执行 AI 脚本或 `.cof` 帧数里）（出处 `策划/差异登记.tsv:14` = E28） | `MonsterTuning` 的 17 条为本项目新增近似（其中 3 条已按官方改为 0.60 / 0.60 / 20） | E28 |

## 统计

| 维度 | 行数 |
|---|---|
| `布局按原版像素` | 4 |
| `素材必须 A 原版` | 4 |
| `字体照原版` | 3 |
| `色调不加滤镜` | 3 |
| `交互反馈` | 3 |
| `节奏` | 4 |

- 总行数 = **21**；差值 = `0` 的 **14** 行，差值 = 登记 id 的 **7** 行。
- 出现的登记 id：`E21` / `E28` / `E32` / `E40` / `E42` / `E58`。
  （`E58` = 2026-09-24 btn-label-fix 轮：交互反馈行的**按钮文字色**从原版 prefab 的 `#191919` 改为 `0.95/0.87/0.60`
   —— 原值在本工程实际使用的原版深板岩石牌上只有 2.79:1 < WCAG 2.1 AA 正文 4.5:1；详见 `策划/验收表.md` 的「允许的差异」E58 节。）

