# 资源欠缺清单

> #17 物品图标缺口：见文末（14 张资料片职业专属装备图不在素材里）。
> **agent-23 处置（Play 实测后）**：这 14 件的图标**无源可补**（逐名试探：`d2data.mpq` 的 597 条 items 全 MISS、`patch_d2.mpq` 1140 条里 `items/*.dc6` **0 条**、磁盘 348 张里也没有）
> ⇒ 已由 `Module/Item/ItemIconAvailability.cs` 把 `item_c.type ∈ h2h/orb/pelt/phlm/ashd/head` 的行**从生成/掉落/商店池剔除**
> （经典版原版本来就没有这些装备），于是**商店/背包/地面不再出现"取不到图的物品"**。买回来的旧存档若含这 14 件，仍显示暗色占位（`D2Icon.MissingIconColor`）。
> 本文件由 AI 维护。AI 不制作美术/音效/动画/模型，全部登记于此，替换后把状态改成 ✅。
> **最低可玩**：不替换任何资源，用纯色占位也能跑通（UI/字体已到位）。
> 更新时间：2026-09-16
> 维护者：agent-03（原版素材落位）；后续由引用资源的 agent 追加/更新行。

**素材来源（唯一来源，照 `策划/素材调研.md` §4）**

```
Diablo II / LoD 原始素材（贴图·字体·数据表）：
  - mofr/Diablerie            https://github.com/mofr/Diablerie            （原版 UI 贴图 / 字体 / MPQ 与 D2 格式读取器）
  - SheXMods/d2lod1.10txt     https://github.com/SheXMods/d2lod1.10txt     （官方 1.10f 数据表 92 txt）
  - 版权归 Blizzard North / Blizzard Entertainment；本项目非商用。
```

**是否使用通用兜底素材：否**（KayKit / Quaternius / Kenney / 内置几何体一律未引入；已用脚本扫描确认 0 命中）
**是否使用 A（暗黑2）自己的素材：是**（级 ①；UI + 位图字体已到位，其余类别在下方登记"等用户提供本体文件"）

## 已到位（无需替换，仅登记）

| # | 资源名 | 类型 | 规格 | 落位路径 | 来源 | 状态 |
|---|---|---|---|---|---|---|
| 0-1 | D2 原版 UI 贴图 67 张（控制面板/血蓝球/经验条/菜单屏/背包/属性/装备栏/光标/技能图标/按钮） | 图 | 见 `client/_dev/assetreport.txt` 实测尺寸 | `Assets/Resources/Clover/D2/UI/{Cursor,Panel,Menu,EquipSlot,SkillIcon}/` | `mofr/Diablerie`（级 ①） | ✅ 已到位 |
| 0-2 | D2 原版位图字体 4 套（font16/24/30/42，256 字形/套，拉丁+符号） | 位图字体 | 图集 512²/512²/512²/1024²；切分格子 16x18 / 24x28 / 31x32 / 41x43 | `Assets/Resources/Clover/D2/Fonts/` | 同上 | ✅ 已到位（中文缺，见 #11） |
| 0-3 | 原样副本（含 DC6 原始字体 + 逐字形步进表 + 源工程 Font 资产链） | 归档 | 89 文件，SHA256 与源一致 | `Assets/ThirdParty/Diablo2/**` | 同上 | ✅ 已到位 |

## 欠缺（需要你提供的：**只有一样——你电脑上的暗黑2 游戏本体文件**）

> 我不可能拿到游戏本体（`archive.org` 直链在本机不可达，见 `策划/素材调研.md` §3）。
> **你只要把 mpq 丢进 `<项目根>/_assets_src/` 就行**，解包 / DC6·DCC·DT1·DS1 → PNG 转换 / 接进 Unity
> 全部归我做（解包工具链已在手：Diablerie 的 C# StormLib 与 MPQ 读取器）。

| # | 资源名 | 类型 | 尺寸/规格要求 | 当前占位 | 需提供 | 替换方法（目标路径） | 影响范围（文件/接口） | 状态 |
|---|---|---|---|---|---|---|---|---|
| 1 | **角色精灵**：5 职业（野蛮人/亚马逊/法师/死灵法师/圣骑士）× 8 方向 × 走动/攻击/施法/受击/死亡 | 图（帧序列） | 按源 `.dcc` 解码的原始像素尺寸（到位后实测登记）；**轴心 = 脚底中心**（`conventions.md` §坐标）；PPU=64 | **无占位**（已到位；旧记「纯色矩形 + Warn」**已过期**，2026-09-21 更正） | —（已到位；保留此格只为出处） | 解包 `.dcc` → PNG 帧序列 → `Assets/Resources/Clover/D2/Chars/{class}/`（文件名建议 `{action}_{dir}_{frame}.png`） | `Module/Player`、`Module/View`、`Core/ResPaths.D2Chars`；`Assets/Editor/AssetImporter.cs`（新增目录要同步加规则） | ✅ **已到位**（`D2/Chars/**` 实测 **3456 张 PNG** = 5 职业 × 7 动作 × 8 方向，`Get-ChildItem -Recurse -Filter *.png` 计数；逐帧文件在盘、`manifest.json` 与 `SpriteFrameCounts.generated.cs` 逐格一致，见 `uicheck` W3GameCheck / `animcheck` §1~§3） |
| 2 | **怪物精灵**：堕落者 / 堕落萨满 / 尖刺鼠 / 僵尸 / 堕落罗格 / 血鹰 / 巨兽 / 幽灵 + 精英怪 | 图（帧序列） | 同上；死亡动画 + 尸体（`corpse_usable` 复用） | **无占位**（已到位；旧记「纯色矩形 + 日志」**已过期**，2026-09-21 更正） | —（已到位；保留此格只为出处） | 解包 `.dcc` → PNG → `Assets/Resources/Clover/D2/Monsters/{name}/` | `Module/Monster`、`Module/View`、`ResPaths.D2Monsters` | ✅ **已到位**（`D2/Monsters/**` 实测 **5184 张 PNG** = 8 种怪 + 5 NPC × 动作 × 8 方向，`Get-ChildItem -Recurse -Filter *.png` 计数；见 `uicheck` W3GameCheck / `animcheck` §1~§3） |
| 3 | **等距地形瓦片**：罗格营地（固定）/ 血腥荒野 / 邪恶洞穴（随机），含地面与随机变体 | 图（瓦片） | 菱形瓦片与 `GameConst.IsoTilePxW=128 / IsoTilePxH=64` 对齐（`docs/步骤文档.md` §3.5）；原版 `.dt1` 原始瓦片尺寸到位后实测登记 | 当前占位：**原版 dt1 瓦片**（`client/Assets/Resources/Clover/D2/Tiles/**` 10 组 + 各带 `manifest.json`；`ResPaths.D2Tiles`） | 用户提供 `d2data.mpq` | 解包 `.dt1` → PNG 瓦片 → `Assets/Resources/Clover/D2/Tiles/`；同时登记 DT1 的瓦片索引→图映射表 | `Module/Map`、`Core/ResPaths.D2Tiles` | ✅ 已到位（2026-09-23 片 W 复核：盘上已在位并已接线） |
| 4 | **场景对象**：建筑 / 树 / 岩石 / 栅栏 / 篝火 / 传送门 / 箱子 / 桶 / 火炬 | 图 | 同上（等距投影，轴心=底部中心） | 当前占位：**原版物体帧**（`client/Assets/Resources/Clover/D2/Objects/**` 16 组 + `manifest.json`；`ResPaths.D2Objects`） | 用户提供 `d2data.mpq` | 解包 `.dt1`/`.dcc` → PNG → `Assets/Resources/Clover/D2/Objects/` | `Module/Map`、`Core/ResPaths.D2Objects` | ✅ 已到位（2026-09-23 片 W 复核：盘上已在位并已接线） |
| 5 | **物品图标**：武器 / 防具 / 药水 / 卷轴 / 金币 / 符文 / 宝石 / 任务物品 | 图 | 尺寸 = `item_c` 的 `grid_w` × `grid_h` 个背包格；**单格像素请 UI agent 用 `inventory.png`(320x432) 实测网格后回写本行** | 纯色方块 + 物品名文字 | 用户提供 `d2data.mpq` | 解包物品 DC6 → PNG → `Assets/Resources/Clover/D2/Items/`（按 `item_c` 的 id 命名） | `Module/Item`、`UI/InventoryPanel`、`ResPaths.D2Items` | ✅ 348 张已到位；另缺 14 张见 #17 |
| 6 | **音效 SFX**：挥砍 / 命中 / 受击 / 死亡 / 怪物叫声 / 技能（火弹·冰弹）/ UI 点击 / 拾取 / 升级 | 音 | 实测全部 22050 Hz / 16-bit / 单声道（原版 `.wav` **原字节**，未重采样 / 未转码） | **无占位**（已到位；旧记「空实现（`Module/Audio` 打一条 `[SFX]` 日志）」**已过期** —— 全仓检索 `[SFX]` 的**唯一**命中就是本行这句话本身，`Module/Audio/**` 里没有这个占位） | —（已到位；保留此格只为出处） | 解包 `.wav` → `Assets/Resources/Clover/Sound/SFX/`（引擎约定路径，`Game.Sound.PlaySFX("名字")`） | `Module/Audio`、`ResPaths.D2Sfx` | ✅ **已到位**（24 个原版 `.wav`，键名 = 文件名，触发点 19 处全接线；**判据**：① `tools/probes/mpq/sfx-provenance.tsv`（由 `tools/probes/mpq/sfx_provenance.py` 用**真实 `d2sfx.mpq`** 生成）= 24/24 工程侧 clip 与包内同名 `.wav` **逐字节 sha256 相同**；② 映射表 `.ai-tmp/test/sfx-map.tsv` 每条都带 `Sounds.txt` **行号**（24/24 复算命中）；③ 离线宿主 `tools/probes/hosts/audiocheck` = **68 项通过 / 0 失败**；④ 实机 `.ai-tmp/screenshots/sfx_evidence_sfx1.txt`（1 次 Play，`VERDICT ok=1 checks_ok=8 checks_bad=0`）= 引擎 `AudioSource.clip.name` 回读 **13 个 clip 真出声**：`miss`（挥砍）/ `hit`（命中）/ `player_hurt`（受击）/ `item_pickup`（拾取）/ `ui_click`（UI 点击）/ `cast`（技能）/ `monster_attack`+`monster_die`（怪物）/ `portal`+`area_enter`+`town`+`bloodmoor`+`item_use`。⛔ **"好不好听 / 像不像原版"是人的判据** = HUMAN-ONLY，未下结论） |
| 7 | **BGM**：罗格营地 / 血腥荒野 / 邪恶洞穴（洞穴/地牢） | 音 | 实测 3 首全部 **22050 Hz / 16-bit / 立体声**（原版 `.wav` **原字节**，未重采样 / 未转码 / 未裁剪） | **无占位**（已到位；旧记「无（静音）」**已过期**，2026-09-22 片 `bgm-original-music` 更正 —— 实测 `client/Assets/Resources/Clover/Sound/BGM/` 下 3 个 `.wav` 都在盘且是真实音乐：`town.wav` 21 805 760 B / 247.23 s、`bloodmoor.wav` 42 270 644 B / 479.26 s、`denofevil.wav` 20 518 012 B / 232.63 s，RIFF/WAVE 头 + `fmt` 块合法，三首 sha256 互不相同） | —（已到位；保留此格只为出处） | 解包 `.wav` → `Assets/Resources/Clover/Sound/BGM/`（引擎约定路径，`Game.Sound.PlayBGM("名字")`） | `Module/Audio`、`ResPaths.D2Bgm` | ✅ **已到位**（3 个原版 `.wav`，键名 = 文件名，`SfxRegistry.BgmFiles` 登记 3 键；**判据**：① 映射表 `.ai-tmp/test/bgm-map.tsv` 每条都带 `Sounds.txt` **行号**（`music_town_1`@4675 / `music_wilderness`@4681 / `music_caves`@4659，3/3 复算命中）+ 调用点 `文件:行`（`SfxRegistry.cs:275/276/277` 的区域→键分派 + `AudioHook.cs:291/301/305/307/313/323` 的消费链）；② 离线宿主 `tools/probes/hosts/audiocheck` 的「BGM 溯源自检」四组断言；③ 实机 `.ai-tmp/screenshots/bgm_evidence_bgm1.txt`（1 次 Play，`[BGM] VERDICT ok=1 checks_ok=7 checks_bad=0`，console errors=0）= 引擎 `AudioSource.clip.name` 回读**三首都真出声且换区真换曲**：Town→`town`、BloodMoor→`bloodmoor`、DenOfEvil→`denofevil`（**与映射表逐字一致**）；④ 素材字节证据 = `tools/probes/mpq/bgm-provenance.tsv`（工程侧 3 个 clip 与真实 `D2music.mpq` 内同名 `.wav` **逐字节 sha256 相同**；生成器 `tools/probes/mpq/bgm_provenance.py`）。⛔ **"好不好听 / 像不像原版"是人的判据** = HUMAN-ONLY，未下结论） |
| 8 | **NPC 语音**（可选，非阻塞） | 音 | 无 | 无 | 用户提供 `d2speech.mpq` / `d2xtalk.mpq` | `Assets/Resources/Clover/Sound/Voice/`（引擎约定，`Game.Sound.PlayVoice`） | `Module/Npc` | ⏸ 可选 |
| 9 | **小地图图标与边框**（Tab 自动地图用；底图形状数据已到手 = 官方 `AutoMap.txt`） | 图 | 按原版尺寸（到位后实测） | 纯色线条 + 文字 | 用户提供 `d2data.mpq` | 解包 → `Assets/Resources/Clover/D2/UI/Panel/`（沿用同一导入规则） | `UI/MiniMapPanel` | ✅ 已到位（2026-09-22 片 automap-render-fix）：标记底图 = 原版 `D2/UI/MiniMap/mapicon_{0..7}.png` 8 帧 16×16（在盘、已被 `UI/MiniMapPanel` 作标记底图引用）；**"边框"这一项本身不成立** —— 原版自动地图是满屏叠加层、没有窗口框（`D2/UI/Banner/automap*` 一族只有标题/开关条）⇒ 边框素材需求作废，见 E23 的消除记录 |
| 10 | **中文文案来源**（NPC 对话 / 物品名 / 技能名 / 任务文本） | 数据 | — | 先用官方数据表 `string`/`tbl` 字段抽取；抽不到则由本项目自写中文（标注"本项目新增"） | 主 agent 定（`constraints.md` 未决） | 抽出的中文文案进 `策划/数值文档/*_c.txt` → 打表 | 全部 UI 文本、`Module/Npc`、`Module/Quest` | ⏸ 待主 agent 决 |
| 11 | **中文字形**：本批原版位图字体只有 **256 个字形（拉丁+符号）**，**不能渲染中文** | 字体 | 需要覆盖常用汉字的位图字体或 TTF；若走原版风格，需 D2 中文版的 font 表 | 当前占位：**原版中文位图字模**（`client/Assets/Resources/Clover/D2/Fonts/font{N}_chi.png` + `font{N}_chi_map.txt`，`UI/D2Text.cs` 使用） | 用户（本体里的中文版 `data/local/font`）或另指定中文字体 | 放入 `Assets/Resources/Clover/D2/Fonts/`（新增文件要在 `AssetImporter.cs` 的 FontGrids 表补规则）；或走 TTF 方案 | 全部 UI 文本；`UI/*`、`Assets/Editor/AssetImporter.cs` | ✅ 已到位（2026-09-23 片 W 复核：盘上已在位并已接线） |
| 12 | **素材解包/转换工具链落进工程**（MPQ + DC6/DCC/DT1/DS1 解码） | 工具（非美术） | 已有源码：`mofr/Diablerie` 的 StormLib C#（`MpqArchive.cs` 等）与 D2 格式读取器 | 工具在 `_assets_tmp`，**未落进工程** | 无需用户提供（有 mpq 即可解） | 需要时把解码器落到 `tools/`（不进 `Assets/`，避免把 211 个 cs 带进游戏程序集） | 素材导入流水线（#1~#5、#6~#8 的前置） | ⏸ 不阻塞当前工作 |

## agent-09（HUD 与游戏内面板）追加登记

> 本节由 agent-09 追加：这些位置**当前全部用纯色占位/`UiArt` 按钮**，画面对得上"原版布局"但**不是原版贴图**；
> 素材到位后只改 `Core/ResPaths.cs` 与对应面板的一行贴图路径，**不动逻辑**。

| # | 资源名 | 类型 | 尺寸/规格要求 | 当前占位 | 需提供 | 替换方法（目标路径） | 影响范围 | 状态 |
|---|---|---|---|---|---|---|---|---|
| 13 | **球/条的高光遮罩** `overlap.png`（原版 `HealthBulbOverlay`/`ManaBulbOverlay` 用它做玻璃反光） | 图（多帧条带） | 源 DC6 帧矩形（本批实测 256×128 含 2 块 x=0..82/84..164，**未切分**） | 当前占位：**原版 `overlap.png`**（`client/Assets/Resources/Clover/D2/UI/Panel/overlap.png`，`ResPaths.PanelOverlap`，`HudPanel.cs:423`） | 用户提供 `d2data.mpq` 里的对应 DC6，或在编辑器里确认帧矩形 | 切分后放 `Assets/Resources/Clover/D2/UI/Panel/overlap`（`AssetImporter.cs` 加 Multiple 规则） | `UI/HudPanel`（`BuildOrb`） | ✅ 已到位（2026-09-23 片 W 复核：盘上已在位并已接线） |
| 14 | **买卖/金币按钮** `buysellbtn.DC6.0.png`(1024×64) / `goldcoinbtn.dc6.0.png`(64×32) | 图（多帧条带） | 同上，帧矩形须来自源 DC6 | 当前占位：**原版 `buysellbtn.DC6.0`（22 帧）/ `goldcoinbtn.dc6.0`（2 帧）**（`ResPaths.cs:129/136/200/203`） | 同上 | 切分后替换 `ResPaths.PanelBuySellButton` / `PanelGoldCoinButton` 的使用点 | `UI/ShopPanel` | ✅ 已到位（2026-09-23 片 W 复核：盘上已在位并已接线） |
| 15 | **技能图标**（每技能一张，原版 `SkillIcon/*`；本批只有 `SkilliconAttack.png`） | 图 | 48×48（1:1） | 当前占位：**原版 `D2/UI/SkillIcon/**`（842 文件，5 职业 + Attack 基名）** | 用户提供 `d2data.mpq` | 解包 → `Assets/Resources/Clover/D2/UI/SkillIcon/`；`SkillTreePanel` 里按 `SkillDef.id` 取 | `UI/SkillTreePanel`、`UI/HudPanel`（技能栏两格） | ✅ 已到位（2026-09-23 片 W 复核：盘上已在位并已接线） |
| 16 | **技能面板底图**（原版技能树面板石框，本批素材内没有） | 图 | 按原版尺寸（到位后实测，勿拉伸） | 当前占位：**原版 `Panel/skltree_{a,b,n,p,s}_back_{0..3}.png`** | 用户提供 `d2data.mpq` | `Assets/Resources/Clover/D2/UI/Panel/skillpanel` | `UI/SkillTreePanel` | ✅ 已到位（2026-09-23 片 W 复核：盘上已在位并已接线） |
| 17 | **任务日志面板底图**（原版任务屏石框） | 图 | 同上 | 当前占位：**原版 `Panel/quest_back.png`**（`ResPaths.PanelQuestBack`） | 用户提供 `d2data.mpq` | `Assets/Resources/Clover/D2/UI/Panel/questlog` | `UI/QuestLogPanel` | ✅ 已到位（2026-09-23 片 W 复核：盘上已在位并已接线） |
| 18 | ~~**NPC 对话石框**（原版对话框底图）~~ | 图 | — | ~~深色底 + 金边（纯色）~~ | **已到位**（用户提供的官中包解出） | `Assets/Resources/Clover/D2/UI/Panel/dialog_back.png`（= `MENU/dialogbackground.DC6`，**210×158**，`tools/d2codec/export_d2ui.py` 的 panels 组；a51 复核：与 DC6 真身**逐像素全等**，md5 `7e961aabbb`）；标题条 `UI/Banner/npcspeech_0.png`（= `LOCAL/UI/chi/npcspeech.dc6`，95×34） | `UI/NpcDialogPanel` | ✅ 已到位（a51 更正：本行原写"待替换"是**过期**的，agent-18 起已在用原版石框） |
| 19 | **商店面板底图** | 图 | 同上 | 当前占位：**原版 `Panel/buysell_back.png` / `trade_back.png`**（`ResPaths.cs:273/276`） | 用户提供 `d2data.mpq` | `Assets/Resources/Clover/D2/UI/Panel/shop` | `UI/ShopPanel` | ✅ 已到位（2026-09-23 片 W 复核：盘上已在位并已接线） |
| 20 | **金币图标**（背包面板左下那个小金币） | 图 | ~15×15 | 当前占位：**原版 `Panel/goldbtn_*` / `goldcoinbtn.dc6.0`**（`InventoryPanel.cs:506`） | 用户提供 `d2data.mpq` | `Assets/Resources/Clover/D2/UI/Panel/goldicon` | `UI/InventoryPanel` | ✅ 已到位（2026-09-23 片 W 复核：盘上已在位并已接线） |
| 22 | **面板关闭按钮的「X」图形**（`InventoryPanel.prefab`/`CharstatPanel.prefab` 的 `CloseButton` 32×31 有 sprite，本批素材没有） | 图 | 32×31（×2.4 = 76.8×74.4） | 透明命中区（底图凹槽可见，点一下关面板） | 用户提供 `d2data.mpq` | `Assets/Resources/Clover/D2/UI/Panel/closebtn` | `UI/InventoryPanel` / `UI/CharacterPanel` | ⬜ 待替换 |

| 21 | **腰带格底图**（原版控制面板腰带格装饰，`ControlPanel.png` 已画有格子 ⇒ 非阻塞） | 图 | 与 `UI/HudPanel.BeltCellSize`(31×29) 对齐 | 用 `ControlPanel.png` 自带的格子 + 半透明纯色底 | 用户提供 `d2data.mpq` | — | `UI/HudPanel` | ⏸ 非阻塞 |

> 说明：**已经用上真原版贴图**的部分 = HUD 控制面板/双球/经验条/小面板与 7 个按钮/跑走按钮/
> 背包与属性面板底图/装备栏 6 张 `inv_*`/普通攻击图标/4 套位图字体（见上表"已到位"节）。


   等距瓦片/装备栏底图/图标这类对尺寸敏感的尤其要按表来。
2. **本批已到位的原版 UI 贴图**（67 张）与**位图字体**（4 套）**不是占位**，是 A 自己的素材，可直接用；
   每张图的实测尺寸、字体切分格子、九宫格边距见 `<项目根>/client/_dev/assetreport.txt`。
3. **替换不碰逻辑**：所有资源引用收敛在 `Core/ResPaths.cs`，换文件或改一行即可，不用重写模块。
   **新增目录**必须同步改 `Assets/Editor/AssetImporter.cs`（否则新素材按 Unity 默认导入 = 糊，且不报错）。
4. **状态图例**：⬜ 待替换 · ✅ 已完成 · ⏸ 暂不需要/待定。
5. **非商用**：以上素材（含已到位的原版贴图/字体）版权归 Blizzard North / Blizzard Entertainment，
   本项目非商用；将来若商用，按本清单的"来源"列统一替换或补授权即可。

---

## 追加（agent-07：怪物 / 战斗 / 技能 / 视图）—— **只追加，未改动上方任何行**

> 追加人：agent-07。以下内容**补充**第 #1 / #2 / #6 行的"具体要哪些文件、叫什么名字"，
> 上位行（#1 角色精灵 / #2 怪物精灵 / #6 音效）仍然是这些欠缺项的**主条目**。

### 1. 角色 / 怪物**帧序列**的确切路径与文件名（`Module/View/SpriteFrames.cs` 就会按这个去找）

```
{动作}_{方向}_{帧号}.png        例：walk_s_0 / attack_ne_3 / death_s_7
动作 = idle | walk | attack | cast | hit | death
方向 = s | sw | w | nw | n | ne | e | se      （与 Def.Dir8 同名小写，顺时针编号）
帧数（当前代码里的固定帧数表，`SpriteFrames.FrameCounts`；素材到手后按 .dcc 实测改一处）
   idle 4 / walk 8 / attack 8 / cast 6 / hit 2 / death 8
```

| 目录 | 内容 | 对应欠缺项 |
|---|---|---|
| `Assets/Resources/Clover/D2/Chars/{class}/` | 5 职业（class = amazon/sorceress/necromancer/paladin/barbarian），每个动作 × 8 方向 × 上表帧数 | #1 |
| `Assets/Resources/Clover/D2/Monsters/{code}/` | 8 种怪，`code` = `monster_c.sprite` 列**小写**：`fa`(堕落者) `fs`(堕落萨满) `si`(尖刺鼠) `zm`(僵尸) `cr`(堕落罗格) `bk`(血鹰) `ye`(巨兽) `wr`(幽灵) | #2 |

骨骼无关、**pivot 必须是脚底中心**（`conventions.md` §坐标：角色锚点 = 脚底中心），
导入设置沿用 #9（`Assets/Editor/AssetImporter.cs`：Point 过滤 / Sprite / PPU=64 / 无压缩）；
**新增这两个目录要同步改 `AssetImporter.cs`**，否则新图是糊的且不报错。
素材未到位时 `ViewModule` 用**纯色占位**（职业 5 色 / 怪物按 AI 4 色 + 精英镀金），画面里照样能跑能打。

### 2. 投射物贴图（`Module/Skill/Projectile.cs` 的速度/射程/半径**已取 `missile_c`**，只缺图）

| 欠缺 | 说明 | 需要的改动（**只改一处**） |
|---|---|---|
| 投射物动画（火弹/冰弹/箭矢…） | 官方文件名在 `missile_c.cel_file`（如 `Firebolt` / `Icebolt` / `Arrow`）；素材在 `d2data.mpq` 的 `.dcc` 里 | `Core/ResPaths.cs` 增一个 `D2Missiles` 常量（`Core/` 冻结，**需主 agent 改**）+ `Module/Skill/ProjectileView.cs` 的 `ResolveSprite` 加一行 `Game.Res.TryGet`；逻辑与调用点不动 |

当前用**纯色占位方块**按伤害类型上色（火=橙 / 冰=蓝 / 电=黄 / 毒=绿 / 物理=灰白）。

### 3. 音效键（`Module/Combat/SfxKeys.cs` 里已常量化，等音频素材按这些**文件名**到位）

```
hit  miss  player_hurt  player_die  player_revive
monster_die  monster_attack  monster_revive
cast  cast_fire  cast_cold  cast_lightning  cast_poison  level_up
```

对应引擎约定路径 `Assets/Resources/Clover/Sound/SFX/{键}.wav`（= 欠 #6）。
`IAudioModule` 未接入时这些调用**只留日志、不报错**（`Module/Combat/DamagePipeline.cs` 的可空音效钩子）。

---

## 追加（agent-11：音频模块）—— **只追加，未改动上方任何行**

> 追加人：agent-11。本节**细化**上方 #6（音效 SFX）/ #7（BGM）两行的"具体要哪些文件、叫什么名字"，
> **上位行（#6 / #7）仍是主条目**。
>
> **键名唯一来源** = `client/Assets/Scripts/Module/Audio/SfxRegistry.cs`
> （其中战斗/技能键的值直接取自 `Module/Combat/SfxKeys.cs`，不重复登记第二套键名）。
> **解包目标目录** = `Assets/Resources/Clover/Sound/SFX/` 与 `Assets/Resources/Clover/Sound/BGM/`；
> 文件名**必须**与下表逐字一致 —— 引擎按 `Sound/SFX/{键}` / `Sound/BGM/{键}` 取资源
> （`clover-client-unity-engine/Runtime/Presentation/Sound.cs:107/63`，`Resources` 按名取、扩展名无关）。
>
> **代码侧已就绪**：素材一放进目录**即自动出声，无需改任何代码**。
> 未到位时每个键只在 `Game.Logger` 里 **Warn 一次**然后静默降级（不刷屏、不崩），
> 由 `Module/Audio/AudioLog.cs` 的私有标志实现（**不用** `Core/Log.WarnOnce` —— 它依赖 Unity 原生时钟）。
> 离线自检宿主：`.ai-tmp/hosts/audiocheck/`（`dotnet run` 全部断言见该目录 README 式注释）。
>
> 来源：用户提供本体文件 `d2sfx.mpq`（+ `d2music.mpq` / `d2xMusic.mpq`）；解包工具链见 #12。

### A. 音效 SFX —— 需要 **24 个 `.wav`**，放 `Assets/Resources/Clover/Sound/SFX/`

| # | 键（= 文件名去扩展名） | 期望文件 | 触发点（谁在什么时候请求它） | 状态 |
|---|---|---|---|---|
| A-1 | `hit` | `hit.wav` | 命中（`Module/Combat/DamagePipeline.cs` 直连 `ctx.Audio.SfxAt`） | ⬜ 待替换 |
| A-2 | `miss` | `miss.wav` | 挥空 / 未命中（`DamagePipeline`） | ⬜ 待替换 |
| A-3 | `player_hurt` | `player_hurt.wav` | 玩家受击（`DamagePipeline`） | ⬜ 待替换 |
| A-4 | `player_die` | `player_die.wav` | 玩家死亡（`DamagePipeline`） | ⬜ 待替换 |
| A-5 | `player_revive` | `player_revive.wav` | 复活**完成**（`AudioHook` ← `Events.Revived`，由 `App/AppEventRouting.cs` 在复活成功后广播） | ⬜ 待替换 |
| A-6 | `monster_die` | `monster_die.wav` | 怪物死亡（`DamagePipeline`） | ⬜ 待替换 |
| A-7 | `monster_attack` | `monster_attack.wav` | 怪物挥击（`Module/Monster/MonsterModule.cs` 直连） | ⬜ 待替换 |
| A-8 | `monster_revive` | `monster_revive.wav` | 萨满复活同伴（`MonsterModule` 直连） | ⬜ 待替换 |
| A-9 | `cast` | `cast.wav` | 技能施放通用回落（`Module/Skill/SkillModule.cs` 直连） | ⬜ 待替换 |
| A-10 | `cast_fire` | `cast_fire.wav` | 火系技能（`SkillModule`，按 `DamageType` 选键） | ⬜ 待替换 |
| A-11 | `cast_cold` | `cast_cold.wav` | 冰系技能（同上） | ⬜ 待替换 |
| A-12 | `cast_lightning` | `cast_lightning.wav` | 电系技能（同上） | ⬜ 待替换 |
| A-13 | `cast_poison` | `cast_poison.wav` | 毒系技能（同上） | ⬜ 待替换 |
| A-14 | `level_up` | `level_up.wav` | 升级（`AudioHook` ← `Events.LevelUp`） | ⬜ 待替换 |
| A-15 | `footstep` | `footstep.wav` | 脚步（`AudioHook` ← `Events.PlayerGridChanged`，每 0.333s 一步、静止不发） | ⬜ 待替换 |
| A-16 | `item_pickup` | `item_pickup.wav` | 拾取物品（`AudioHook` ← `Events.ItemPicked`，非金币） | ⬜ 待替换 |
| A-17 | `gold_pickup` | `gold_pickup.wav` | 拾取金币（`AudioHook` ← `Events.ItemPicked`，`isGold=true`） | ⬜ 待替换 |
| A-18 | `item_use` | `item_use.wav` | 喝药 / 用卷轴（`AudioHook` ← `Events.ItemUsed`） | ⬜ 待替换 |
| A-19 | `ui_click` | `ui_click.wav` | UI 点击（`AudioHook` ← 面板开关 / 对话选项 / 买 / 卖按钮） | ⬜ 待替换 |
| A-20 | `dialog_open` | `dialog_open.wav` | NPC 对话开始（`AudioHook` ← `Events.DialogOpen`） | ⬜ 待替换 |
| A-21 | `shop_open` | `shop_open.wav` | 商店打开（`AudioHook` ← `Events.ShopOpen`） | ⬜ 待替换 |
| A-22 | `portal` | `portal.wav` | 传送 / 踩出入口（`AudioHook` ← `Events.ExitEntered`） | ⬜ 待替换 |
| A-23 | `area_enter` | `area_enter.wav` | 进图（`AudioHook` ← `Events.StageEntered`） | ⬜ 待替换 |
| A-24 | `quest_complete` | `quest_complete.wav` | 任务完成（`AudioHook` ← `Events.QuestCompleted`） | ⬜ 待替换 |

> 规格建议：22.05 / 44.1 kHz 单声道 `wav`（原版 `.wav` 直接可用；引擎 `ResourceSize.Estimate` 按 `samples*channels*2` 估体积）。
> `hit` / `miss` / `monster_*` / `cast_*` / `footstep` / `portal` 是**位置音**（走 `Game.Sound.PlaySFXAt`，3D 通道）；
> 其余是**2D 音**（走 `PlaySFX`）。若手上只有一段通用素材，可先同名复制到多个键上顶着（**只在素材到位前的临时手段**）。

### B. BGM —— 需要 **3 个音频文件**，放 `Assets/Resources/Clover/Sound/BGM/`

| # | 键 | 期望文件 | 触发点 | 状态 |
|---|---|---|---|---|
| B-1 | `town` | `town.wav` | 罗格营地（`AudioHook` ← `Events.AreaChanged`(Town) / 进图时的当前区域） | ✅ 已到位（21 805 760 B，原版 `d2music.mpq:data\global\music\act1\town1.wav`） |
| B-2 | `bloodmoor` | `bloodmoor.wav` | 血腥荒野（同上，AreaId.BloodMoor） | ✅ 已到位（42 270 644 B，原版 `d2music.mpq:data\global\music\act1\wild.wav`） |
| B-3 | `denofevil` | `denofevil.wav` | 邪恶洞穴（同上，AreaId.DenOfEvil） | ✅ 已到位（20 518 012 B，原版 `d2music.mpq:data\global\music\act1\caves.wav`） |

> `wav` / `ogg` 均可（引擎按名取、扩展名无关，`Resources.LoadAsync` 会自动匹配）。
> 循环点要干净（原版 BGM 是循环的；引擎 `PlayBGM` 会把 `AudioSource.loop` 置 true）。

### C. 与"未到位"有关的两条实现说明（不是欠缺项，供替换时参照）

1. **缺文件降级**：探测不到 → 该键**只 Warn 一次**（日志 tag = `Audio`）后静默；
   因为"素材缺失"是**每键一次**的有限告警，不会刷屏。素材到位后**无需改代码**。
2. **静音开关不落盘**（已知偏差，非阻塞）：`IAudioModule.SetMute` 只作用于本次会话 ——
   `Core/GameConst.cs` 里没有 `bgm_mute`/`sfx_mute` 的设置键，而 `Core/` 契约冻结（agent-11 不许改）。
   若要"静音也持久化"，需主 agent 在 `Core/GameConst.cs` 加常量。

---

## 追加（w5：光标 5 态 + 选项/暂停底板）

| # | 资源名 | 类型 | 尺寸/规格要求 | 当前处置 | 需提供 | 替换方法（目标路径） | 影响范围 | 状态 |
|---|---|---|---|---|---|---|---|---|
| 24 | **原版光标的另外 4 态**（攻击 / 交互 / 拾取 / 不可走；`Def.CursorKind` 共 5 态） | 图（DC6 帧） | 原版 `data/global/ui/CURSOR/Cursor.DC6` 的攻击/交互/拾取/不可走 4 帧（本批只有**普通箭头 1 帧 32×26**，即 `D2/UI/Cursor/Cursor.png`） | **4 态缺口**：5 态**统一显示那一帧箭头**，切到缺口形态时由 `UI/CursorView.cs::OnCursorChanged` **逐态打一条 Warn**（⛔ 按 skill §0「A 没有就不加」**不自画 4 个光标**）。承载机制已接通（`CursorChanged` 有真消费方 + 藏系统光标 + 跟随鼠标的 Top 画布 Image） | 用户提供 `d2data.mpq` 的 `CURSOR/Cursor.DC6`（或 4 张单帧 PNG） | 若是多帧：按 `AssetImporter.MultiFrameStrips` 口径切帧后改 `ResPaths.Cursor` 的取帧（`ResPaths.Frame(path, 帧号)`）+ `UI/CursorView.cs` 的 `SpriteFor` 一处；触发链 / 承载方式 / hot spot 都不用动 | `UI/CursorView.cs`、`UI/UiLayoutGame.cs`（`CursorArtPx` / `CursorSize` / `CursorHotspotPivot` / `CursorKindCount`）、`Core/ResPaths.cs` | ⬜ 待替换（机制已通） |
| 25 | **选项/暂停屏的「原版窗框」到底是哪一个** | 出处（不是文件） | — | **已用原版 `MENU/boxpieces.DC6`**（22 帧 14×15 拼装窗框，`D2/UI/Panel/boxframe_settings.png` 432×348 / `boxframe_pause.png` 288×180）—— 拼装偏移由**像素自证反推**（`tools/d2codec/assemble_boxpieces.py`），**不是**自创几何；但「原版这两屏用的就是它」**没有出处**（原版 options/暂停屏的 prefab 与实机截图都不在本机） | 原版选项/暂停屏的**实机截图**（一看就知道是哪个窗框、多大） | 若实为别的窗框（例如 `helpborder`）：只改 `UI/SettingsPanel.cs` / `UI/PausePanel.cs` 各一行 `ResPaths.PanelBoxFrame*` + `UiLayoutFlow.Settings/Pause.BoxSize`（尺寸随素材） | `UI/SettingsPanel.cs`、`UI/PausePanel.cs`、`UI/UiLayoutFlow.cs`、`Core/ResPaths.cs` | ⬜ 待出处（**不是占位物**：已是原版素材 + 逐像素自证口径） |

> 说明：`boxpieces` 的**装饰变体**（上/下边块各 6 个、左/右边块各 3 个，只差石纹与金色饰点）
> 中，本项目只用了每族的**第一个**（上 `#2` / 下 `#16` / 左 `#10` / 右 `#13`）——
> 「哪个变体放哪一格」在原版里**没有出处**（DC6 帧 offset 表不在本机），
> 取第一个 = **不挑**（不引入任何无出处的排布）。其余 14 帧因此仍未被引用（**已登记**，非遗漏）。

> **复核（2026-09-21 全量重取证轮）**：上表 **#24**（原版光标另外 4 态缺图）与 **#25**（选项/暂停窗框**出处**）
> 本轮**确认仍在**、行文与现行口径一致（承载机制已通、缺口=素材；窗框已是原版素材 + 可自证拼装）。
> 离线判据 = `tools/probes/hosts/uicheck` 的 W3GameCheck「光标 5 条断言」与「boxpieces 22 帧 / 偏移自证 / 接缝自证」两组。

---

## 追加（2026-09-21 全量重取证轮）—— **只追加，未改动上方任何行**

> 追加人：本轮执行者。以下 5 条是**全量重取证**（流程屏 / 游戏内 UI / 动画逐项审计 ＋ 两次冷启动实机 tour）里
> 逐项对账时**确认「拿不到出处 / 只能用已登记的近似」**的欠缺项。**#1 / #2 的过期状态已在上表就地更正**（⬜ → ✅）。
> 状态图例同前：⬜ 待替换 · ✅ 已完成 · ⏸ 暂不需要/待定 · **⛔ 待出处**（机制已通、只缺原版出处）。

| # | 资源名 | 类型 | 尺寸/规格要求 | 当前处置 | 需提供 | 替换方法（目标路径） | 影响范围 | 状态 |
|---|---|---|---|---|---|---|---|---|
| 26 | **原版自动地图（automap）图块 + `AutoMap.txt`** | 图 + 数据 | 原版 `ui/AUTOMAP/MaxiMap.dc6`（**1260 帧 16×32**，全 act 共用的地形图块表）+ 逐格 Cel 号的 `AutoMap.txt` | 小地图**没有**按原版 blit 图块；现行 = **暗底 + 逐格点阵**（`MiniMapPanel.SuperSample=4`、每格 1/4/9 纹理px、三色派生自原版 automap 横幅素材实测量）⇒ **已探索范围是「本格 + 8 邻域」近似**、配色为本项目选定 | 用户提供 `d2data.mpq` 的 `ui/AUTOMAP/**` 与 `data/global/excel/AutoMap.txt` | 解包图块 → `Assets/Resources/Clover/D2/UI/MiniMap/`（按 IHDR 加导入规则）；`AutoMap.txt` → 配表；`UI/MiniMapPanel` 的 `PaintSquare` 一处换成"按 Cel 号 blit 图块" | `UI/MiniMapPanel`、`Core/ResPaths`（新增 automap 常量） | ✅ 已到位（2026-09-22 片 automap-render-fix）：原版包已在盘（`原版资源/d2dc6/data/global/ui/AUTOMAP/MaxiMap.dc6` = 1260 帧 16×32；`原版资源/d2raw/data/global/excel/AutoMap.txt` = 2603 数据行 ×13 列；ACT1 `Pal.PL2`）⇒ 逐格 cel 表 + 21 个被引用帧的像素 + ACT1 调色板 768B 已由 `tools/probes/gen_automap.py` 生成为 `client/Assets/Scripts/Core/AutoMapCel.generated.cs`（⛔ 不整包复制 DC6），`UI/MiniMapPanel` 按原版 1/10 等距 blit 绘制（实机 = `.ai-tmp/screenshots/automap_contact_b1.index.tsv`）；残余差异见 E45 |
| 27 | **投射物动画 `.dcc`** | 图（帧序列） | 原版 `missile_c.cel_file` 指向的 `.dcc`（如 `Firebolt` / `Icebolt` / `Arrow`；文件名已登记在 `missile_c`） | **纯色占位方块**按伤害类型上色（火=橙 / 冰=蓝 / 电=黄 / 毒=绿 / 物理=灰白；`Module/Skill/ProjectileView.cs`） | 用户提供 `d2data.mpq` 的投射物 `.dcc` | 解包 → PNG 帧序列 → `Assets/Resources/Clover/D2/Missiles/{name}/`；`Core/ResPaths.cs` 增 `D2Missiles` 常量（`Core/` 冻结，**需主 agent 改**）+ `ProjectileView.ResolveSprite` 加一行 `Game.Res.TryGet` | `Module/Skill/ProjectileView`、`Core/ResPaths.cs` | ⬜ 待替换（逻辑与调用点不动） |
| 28 | **`AnimData.d2`（动画播放速度）** | 数据 | 原版 `data/global/AnimData.d2` —— 每个 `.cof` 动画模式的**逐帧播放速度**（帧率/延迟） | 帧率走**本项目基准帧率**（`SpriteAnimator` 的 `FpsForCycle`：有效帧率 = 帧数 × 格/秒；静态动作倍率复位为 1）；受击/出手/施法的**保持时长**由"该单位该动作真实帧数 ÷ 基准帧率"给出（**不引入无出处的时长常量**，见 `ViewAnimState.IsHitHolding` 注释） | 用户提供 `d2data.mpq` 的 `AnimData.d2` | 解包 → 配表（`anim_cof` 之类）→ 打表；`Module/View/SpriteFrames` 的帧率/时长改为按 `AnimData` 查表（现在是无出处常量） | `Module/View/SpriteFrames`、`Module/View/ViewModule`、配表 | ⬜ 待替换（现行是"帧数÷基准帧率"的**降级口径**） |
| 29 | **`boxpieces.DC6` 的帧 offset 表** | 数据（不是文件） | 原版 `MENU/boxpieces.DC6` 的**逐帧 offset 表**（决定每个 14×15 帧该压在哪一格） | **已用像素自证绕过**：生成器 `tools/d2codec/assemble_boxpieces.py` 反推 `pitch=12`、角块压 1..3/10..12、竖边块压 5..7 ⇒ 偏移 **左 −4 / 右 +5 / 下 +9 / 上 0**；拼出的整幅**逐像素接缝连续**（0 不一致）。⛔ 不是自创几何 | 原版选项/暂停屏的**实机截图**（一看就知道是哪个窗框、多大）—— 见 #25 | 拿到 offset 表 ⇒ 只改 `assemble_boxpieces.py` 的偏移常量并重生成两张整幅窗框；面板侧零改动 | `tools/d2codec/assemble_boxpieces.py`、`D2/UI/Panel/boxframe_{settings,pause}.png` | ⛔ 待出处（**不是占位物**：已是原版素材 + 逐像素自证口径；见 #25） |
| 30 | **原版「删除角色确认」弹窗的出处** | 出处（不是文件） | — | 现行 = 本项目自建 `UI/D2ConfirmPanel`（**原版窗框** `boxframe_pause` + **原版中等按钮三态**）；它**替掉了**引擎默认 uGUI 弹窗（深灰方块 + 纯蓝按钮），但「**原版这一屏的二次确认框长什么样**」**没有出处**（原版前端 prefab 与本机实机截图都没有） | 原版删除角色确认弹窗的**实机截图** | 若原版形制不同 ⇒ 只改 `UiLayoutFlow.Confirm` 的几何组 + `D2ConfirmPanel` 的素材行（窗框/按钮）；联动链不动 | `UI/D2ConfirmPanel`、`UI/UiLayoutFlow.cs`（`Confirm` 组） | ⛔ 待出处（**不是占位物**：已是原版素材 + 原版按钮三态） |

---

## 追加（agent-15：流程面板 1:1）

| # | 资源名 | 当前占位 |
|---|---|---|
| 22 | 选角屏 7 个职业半身像（原版 `ClassSelectMenu.prefab` 的 RawImage，原版本身也没贴图） | **原版 `D2/UI/FrontEnd/**`（592 文件，`NU1..3` 三态；`client/Assets/Scripts/UI/CharCreatePanel.cs:1338`）** ⇒ ✅ 已到位（2026-09-23 片 W 复核：盘上已在位并已接线） |
| 23 | 启动屏 Blizzard North logo（原版启动画面） | **原版 logo**（`client/Assets/Resources/Clover/D2/UI/Logo/`，验收表第 1 行 `sprite=logo_0`） ⇒ ✅ 已到位（2026-09-23 片 W 复核：盘上已在位并已接线） |

> 说明：上述两项目前**不是**"没做"，而是"原版素材本批不在手上"：
> 职业半身像在原版工程里就是**运行时喂图**的空 RawImage（`m_Texture={fileID:0}`），
> 素材到位后只改 `UI/CharCreatePanel.cs` 的职业按钮那一段（位置/尺寸已按原版热点矩形 ×2.4 写好）。

---

## 追加（T0 判据挖出的两个缺口修复轮）—— **只追加，未改动上方任何行**

> 追加人：本轮执行者。修复 = ① 死亡扣金币 10%（软核；无新素材需求）② 双武器组（原版 `W` 键切换）。
> 下面只登记 **② 的 HUD 指示图标缺素材**这一件事（① 不涉任何图）。

| # | 资源名 | 类型 | 尺寸/规格要求 | 当前处置 | 需提供 | 替换方法（目标路径） | 影响范围 | 状态 |
|---|---|---|---|---|---|---|---|---|
| 31 | **HUD 上的「武器组指示」素材**（原版控制面板里显示当前武器组 Ⅰ/Ⅱ 的那两个小格 + 高亮态） | 图（DC6 帧） | 原版 `data/global/ui/PANEL/ctrlpnl7.DC6`（= 控制面板底图 DC6 的**真名**；本清单旧写 `ControlPanel.dc6` 是错名，2026-09-22 `g1-naming-and-mpq-asset` 片按事实改正 —— `ControlPanel.dc6` 在 `D2data.mpq`+`Patch_D2.mpq` 里**不存在**，已解出的是 `ctrlpnl7.DC6`）里若含武器组格，则按该帧的原始矩形；本项目实测 `ControlPanel.png`（948×160，= 引擎/原版底图）**只画出 10 个格**：技能带 6 格（art x 320..542、pitch 38）+ 腰带 4 格（art x 599.5..692.5、pitch 31），**没有任何武器组格**；`D2/UI/**` 全目录按名检索 `weapon` / `swap` / `hand` **0 命中**（只剩 `EquipSlot/inv_weapons.png` = 背包槽底图，不是 HUD 指示） | **画不出就不画**：本轮**未**自画图标、**未**用纯色块顶。切换的可见反馈改由**有出处的两条**承担 —— ① `UI/InventoryPanel` 的右手装备槽（`rarm`，原版 `inv_weapons.png` 底图）显示**当前生效组**那把武器（切换即换图，用原版物品图 + 原版槽位）；② 人物面板/HUD 的攻击力随切换重算（词缀只算生效组） | 原版 HUD 上武器组指示的**素材帧**（或原版控制面板实机截图，用于确认它到底画在哪一格） | 拿到帧后：切帧落 `Assets/Resources/Clover/D2/UI/Panel/`（按 IHDR 加导入规则）→ `Core/ResPaths.cs` 加一个常量 → `UI/HudPanel.cs` 的 `Build()` 里加一个图标层（位置/尺寸取该帧实测值），逻辑与触发链（`Events.SwapWeaponRequest`）**不动** | `UI/HudPanel.cs`（新增一个指示层）、`Core/ResPaths.cs` | ⬜ 待替换（功能与状态机已通：`InputReader.SwapWeaponPressed` → `Events.SwapWeaponRequest` → `ItemModule.SwapWeaponGroup()` → 存档字段 `CharacterSave.activeWeaponIndex`；离线判据 = `tools/probes/hosts/playercheck` §17「双武器组」20 条断言 + §18「死亡扣金币」10 条断言 + `itemcheck` §12「双武器组」26 条断言 + §13「死亡扣金币」9 条断言，共 **65 条**，全绿） |

## 追加（d2fix 收口轮，2026-09-22）—— **只追加，未改动上方任何行**

> 追加人：收口片 `impl-gate-close`（用户本轮报「角色武器外观」）。**这条缺口此前从未登记过** —— `d2char.mpq` 的取回到 2026-09-22 才完成。

| # | 资源名 | 类型 | 尺寸/规格要求 | 当前处置 | 需提供 | 替换方法（目标路径） | 影响范围 | 状态 |
|---|---|---|---|---|---|---|---|---|
| 32 | **装备外观层（武器 / 盾 / 护甲按物品 code 分档的 DCC 图层）**：角色持械 / 着装的逐帧图层（`data/global/chars/<cls>/cof/<class><MODE><wc>.cof` 的 RH / LH / SH 三层；层图 = `data/global/chars/<cls>/<wc>/<MODE><wc><code>.dcc`） | 图（DCC 帧 + COF 索引） | 逐帧原生素材；**层名 = 该件物品自己的 `code`**（不是 `wclass` 大类） | 当前占位：**原版 `Chars/{cls}/equip/**`（按 item `code`，1411~4233 文件/职业，同 E47 已消除）** | 无需用户提供（素材已在本机）；需要的是**导出 + 选层口径**的落地 | 导出链 = `tools/d2codec/export_chars.py`（按 code 解 COF 三层）→ `client/Assets/Resources/Clover/D2/Chars/{class}/{wc}/**` → `Core/ResPaths.cs` 加路径常量 → `Module/View/SpriteFrames.cs` 把「按 wclass 选层」改成「按 item code 选层」 | `Module/View/SpriteFrames.cs`、`Module/View/ViewModule.cs`、`Core/ResPaths.cs`、`client/Assets/Resources/Clover/D2/Chars/**` | ✅ 已到位（2026-09-23 片 W 复核：盘上已在位并已接线） |

<!-- C3 片追加（只追加，不改既有行） -->
| C3-1 | **怪物移动（脚步）音效** | 音 | 短（<0.3s） | 当前**完全没有**：`AudioHook` 的脚步只订阅了玩家 `Events.PlayerGridChanged`，怪物侧 0 个触发点 ⇒ 听不到怪物走近 | 用户提供 `d2sfx.mpq`（`data\global\sfx\monster\<code>\walk*.wav`，逐怪一套） | `Assets/Resources/Clover/Sound/SFX/monster_walk.wav` | `Module/Audio/SfxRegistry`（需新增键）+ `Module/Monster/MonsterModule`（埋点） | ⌛ 待替换（素材未到位；键与触发点已定位） |
| C3-2 | **怪物受击音（逐类）** | 音 | 短 | 现在命中一律用 `hit` = 原版 `combat/impact/sword1.wav`（**挥砍撞击**，不是怪叫）⇒ 打什么怪都同一个声 | 用户提供 `d2sfx.mpq`（`data\global\sfx\monster\<code>\hit*.wav`） | `Assets/Resources/Clover/Sound/SFX/monster_hit.wav` | `Module/Combat/SfxKeys`（`Hit` 的取值处） | ⌛ 待替换 |
| C3-3 | **传送小站（Waypoint）** | 音/图 | — | 全仓**无传送点物件**（`策划/验收表.md` 已登记 S-40「功能未做，待裁决」）⇒ 无点击音、无面板音 | 用户提供 `d2sfx.mpq`（`cursor/windowopen` 等）+ `d2data.mpq`（waypoint 的 dc6/dt1） | — | 待主 agent 裁决归属（涉 `Module/Map` + `UI` 两片领地） | ⌛ 待裁决 |

---

## 2026-09-23 收口轮追加登记（gate-close2；⏔ **只追加**，本节之前的任何行一字未改）

> 前提核实：C3 片要求的 **C3-1 / C3-2 / C3-3** 三行（怪物脚步音 / 逐类受击音 / 传送小站物件音）**已在本文件里**（本轮实测：`C3-1` / `C3-2` / `C3-3` 各 1 命中）⇒ 不重复登记。

1. **字号缺口（片 V6 §4-②）**：`D2Label.Create` 仍有 **6 处**调用没给字号 ⇒ 按原版 px 1:1 画（比同屏其它 UI 文字小约 45%）。本轮已修 3 处（`EntityTooltip` 两条 + ①）；未修的 6 处里已点名的有 `client/Assets/Scripts/UI/GroundItemLabelView.cs:163`（地面物品名牌）与 `client/Assets/Scripts/UI/LevelEntryTitle.cs:206`（区域名标题）。⏔ 这**不是缺素材**，是代码口径缺口（需逐处给字号），登记以便收口。出处 = `.ai-tmp/test/report-V6.md` §4-②。
2. **武器外观按 code 逐件未导出（`ssd` 短剑等）**：当前只导了「每个 `wclass` 一件代表武器」的套（`1hs` 用 `ssd`、`stf` 用 `lst` …），同 `wclass` 的其它 code（`hax` / `axe` / `wnd` / `ywn` / `clb` / `mac` / `scm` / `flc` / `crs`）外观未导出 ⇒ 手里武器的“具体长相”不到位。出处 = `.ai-tmp/test/report-weapon-export.md` §4；口径登记 = `策划/差异登记.tsv` 的 **E56**。

---

## 2026-09-23 S3 片追加（**只追加**，本节之前的任何行一字未改）

> 判据脚本：`.ai-tmp/test/s3_probe_monster_sfx.py`（一次性探针，按名调 `SFileExtractFile` 解出原版 wav；
> 判据 = **产物落盘且 > 0 字节**，因为 `SFileHasFile` 在这份 DLL 上不可信，见 `tools/d2codec/storm.py` 文件头）
> 与 `tools/probes/mpq/sfx-provenance.py`（已入仓的溯源台账）。实机证据：`.ai-tmp/screenshots/s3_evidence.tsv`。

1. **修正 C3-2 的前提（"素材未到位"不成立）**：`原版资源/_mpq_incoming/d2sfx.mpq`（49.54 MB）**就在盘上**，
   且**确实含逐类怪物受击音**。按原版 `Sounds.txt` 的 `FileName` 列（`monster\fallen\gethit{1,2,3,6,7}.wav`，
   行 897~901；`monster\fallenshaman\gethit1.wav`，行 928）逐名解包实测 **8/8 全部解出**
   （gethit1=15176 B / gethit2=20560 / gethit3=18440 / gethit6=17584 / gethit7=14180 /
   fallenshaman/gethit1=16802；另 death1=42030、roar1=19530 作对照，与已入仓的 `sfx-provenance.tsv` 逐字节一致）。
   ⇒ **不需要用户再提供任何包**；C3-2 的"待替换"真实内容是**取件 + 接线**，不是等素材。
   注意文件名**不是** `hit*.wav`（旧登记的路径是错的）：原版库里叫 `gethit*.wav`。
2. **修正 C3-1 的前提（假设的路径在原版里不存在）**：`Sounds.txt` 里**没有** `monster\fallen\walk*.wav`
   这一族条目（897~923 行的 fallen 全部条目只有 attack/hit/death/neutral/warcry/flee 六族）。
   ⇒ "怪物脚步音"**不是"逐怪一套 walk*.wav"**，C3-1 里写的取件路径按现状**取不到任何东西**；
   要落地必须先定"原版怪物移动音的真正出处"（`Sounds.txt` 里没有 ⇒ 需另找依据），**不许先接线再补依据**。
3. **受击动画**：代码链路与素材**都在**（`MonsterModule.ApplyDamage` 设 `HitStunTimer=0.18` ⇒ `Sync()` 写
   `State.hitStun` ⇒ `ViewModule:457 ViewAnimState.SelectMonster` 出 `ViewAnim.Hit`；素材
   `Assets/Resources/Clover/D2/Monsters/fa/` 有 `hit_*` 帧，已机械枚举 13 个怪目录确认 fa 含 hit）。
   但 **S3 的实机探针未判成**：200 帧记录里怪物 sprite 只出现 `idle_*`/`attack_*`，**一次 `hit_*` 都没有**，
   而同一段里 `hp` 未下降（玩家 `HandlePrimaryClick` 的点击未在该窗口生效）⇒ **不能据此说"受击动画没播"**，
   本条**仍未判**（需要一次"伤害确实落在记录窗口内"的重测）。

---

## 2026-09-23 chunk-hole2 片追加登记（**只追加**，本节之前的任何行一字未改）

1. **换区 / 传送进区的头 1~2 秒，屏上仍有"整屏黑"的黑窗（残余，⛔ 未消除）**：那一小段时间里屏上只有角色 + HUD + 区域标题，地形还没铺出来；随后自愈（约 1~2 s）。
   来源 = **首次建块需要若干帧** + `ChunkRefreshInterval`（0.25 s，private const，反射可读）节流 + 块建造按 `MaxTileNodesPerFrame` 分帧 ⇒ 是**建块耗时**，**不是缺块**。
   ⛔ 它与"永久黑"是两件事：**永久黑已修**（`MapView.cs:1144` 早退条件加 `ChunkRangeCovered` 完整性判据，定义 `:1180`；`:1017` `SwapToBuilt` 后 `_nextChunkRefresh = 0f`）⇒ 修后读数 `MISSING=1 / pending=1`（缺块**已进待建队列**，会补上），修前是 `MISSING=4 / PendingChunks=0`（洞里没人补 ⇒ **永久黑**）。
   ⛔ 本条**不伪装成已消除**：原版换区同样有加载过程。登记为残余，等裁决。
   证据（两张相差 2 秒的实机图，由 **主 agent 读图**定案）：`.ai-tmp/screenshots/chunkhole2_tour_s2_07_bloodmoor_on_entry.png`（21:01:28，进区瞬间**整屏黑**）→ `.ai-tmp/screenshots/chunkhole2_01_entry.png`（21:01:30，**已完全正常**）；详版 = `.ai-tmp/test/report-chunkhole2.md` §4 / §9。
   ⛔ 要消掉这个黑窗就得动 `ChunkRefreshInterval` / 首铺预算这类**节奏数字** —— 那是另一件事，需单独裁决；本片**没有**为了报告好看去调任何一个数字。

---

## 2026-09-23 monster-audio 片追加登记（**只追加**，本节之前的任何行一字未改）

0. **先说结论（推翻"素材没到位 ⇒ 所以没声音"这个假设）**：**怪物音效素材源在本机是齐的** ——
   `原版资源/_mpq_incoming/d2sfx.mpq`（51,948,991 B）**已在盘**，`Sounds.txt` / `MonSounds.txt`
   （`原版资源/d2lod1.10txt-1.10f/data/global/excel/`，1.10f LOD）**已解析**。
   ⇒ 用户听到的"怪物没音效 / 打击没声音"**不是素材包缺失**，而是下面 1/2 两条**接线缺口**。
   ⛔ 因此本片**没有**用任何非暗黑2 的音效顶替，也**没有**用静音代替。

1. **【已修】怪物受击动画只渲染前 3/N 帧（E28 复判：仍成立 ⇒ 修的是播放逻辑，没删断言）**
   - 判据（离线，非叙述）：`tools/probes/hosts/animcheck` §8 逐帧驱动 —— 旧口径（只按
     `MonsterTuning.HitStunSeconds = 0.18s` 保持）8 类怪物**全部只出 0,1,2 三帧**；
     堕落者 `fa` 受击动作**真实 7 帧 @12fps = 0.583s** ⇒ 硬直一结束就被 `UpdateMonster`
     顶成 Idle/Walk，**后 4 帧从不上屏**。
   - 修法（只动受击编排，未新增任何时长常量）：`ViewModule.UpdateMonster` 的 `SelectMonster`
     第二个入参改 `state.hitStun || ViewAnimState.IsHitHolding(...)`（与玩家侧 `TickPlayer`
     同源口径：受击动作**播完**才回落）；并在 `TickOne` 补一条"受击播完主动回落 Idle"
     （防站着挨完打的怪因不再 `UpdateMonster` 而停在受击末帧）。
   - 修后 §8 新口径：8 类怪物受击帧序列 = `0..N-1` **全部上屏**（fa 7/fs 5/si 6/zm 5/cr 9/bk 5/ye 6/wr 8）。

2. **【缺·接线】怪物**自身受击音**没有独立音效键**（`MonSounds.HitSound` 一列**从未接线**）
   - 现状：玩家打中怪物 ⇒ 只有 `Combat.SfxKeys.Hit`（= `impact_blade_swing_1`，挥砍撞击音）；
     怪物**自己**的 `HitSound` 一次都不播。这是"打击没声音"最贴身的一条。
   - 期望键（出处 `MonSounds.txt` 行 / 目标文件出处 `Sounds.txt` 行）：
     | 怪物类别 | `MonSounds.HitSound` | `Sounds.txt` 目标文件 |
     |---|---|---|
     | Fallen(`fa`) | `fallen_hit_1` | `data\global\sfx\monster\fallen\gethit1.wav` |
     | FallenShaman(`fs`) | `fallenshaman_hit_1` | `data\global\sfx\monster\fallenshaman\gethit1.wav` |
     | QuillRat(`si`) | `spikefiend_hit_1` | `data\global\sfx\monster\spikefiend\gethit1.wav` |
     | Zombie(`zm`) | `zombie_hit_1` | `data\global\sfx\monster\zombie\gethit1.wav` |
     | Skeleton | `skeleton_hit_1` | `data\global\sfx\monster\skeleton\gethit1.wav` |
     ⚠ 文件名是 `gethit*.wav`，**不是** `hit*.wav`（沿用上一节已修正的口径）。
   - 取件包 = **`d2sfx.mpq`（已在盘）**；⛔ **不需要用户再提供任何包**。
   - 剩下 4 类（CorruptRogue `cr` / Brute `bk` / Wraith `ye` / BloodHawk `wr`）的逐类条目名
     本片**未逐条解析完** `MonSounds.txt` ⇒ **未定案**，下一棒接着补（不要凭印象写键名）。

3. **【缺·接线】逐类怪物的攻击音 / 死亡音当前是"一类音给 8 类用"**
   - 现状：只有 3 个通用键 `monster_attack` / `monster_die` / `monster_revive`，
     三者的素材源**全是堕落者 `fallen`**（`fallen_attack_1` / `fallen_death_1` / `fallenshaman_resurrect`）。
     ⇒ 打僵尸、打血鹰听到的都是沉沦魔的叫声/死声（原版是逐类一套）。
   - 期望（出处同上，全部已从 `MonSounds.txt` 解析到条目名，目标文件也已在 `Sounds.txt` 命中）：
     `fallenshaman_attack_1`→`monster\fallenshaman\roar1.wav`、
     `spikefiend_attack_1`→`monster\spikefiend\attack1.wav`、`spikefiend_death_1`→`...\spikefiend\death1.wav`、
     `zombie_attack_1`→`monster\zombie\attack1.wav`、`zombie_death_1`→`...\zombie\death1.wav`。
   - 取件包 = **`d2sfx.mpq`（已在盘）**。

4. **【未定案】怪物脚步音**：`MonSounds.txt` 的 `Footstep` 列对 `fallen`/`fallenshaman`/`zombie`/`skeleton`
   都填了 `light_walk_dirt_1`（= `ambient\footstep\LightDirt1.wav`，**已在盘、已入仓**），`FsCnt=2 / FsPrb=100`；
   `quillrat`/`rat` **没有** `Footstep`。⇒ "怪物脚步"要落的是**触发点**（按 `FsCnt`/`FsPrb` 逐格触发），
   不是再取素材；本片**没有**接线（会碰"脚步累计"这条前片定稿的口径）⇒ 交裁决。

5. **【未做】实机取证**：本片 ①②③④ 全部可离线判定 ⇒ 按派活书的 Play 锁**默认不进 Play**；
   期间 `unity command editor_status` 读数 `playMode=playing`（他片占用）⇒ **未取锁、未进 Play、未截图**。
   离线编译证据替代：两个宿主（`animcheck` / `audiocheck`）的 csproj **链真实业务源码**
   （`Module\*\*.cs`）⇒ `dotnet run` 成功即证明含 `ViewModule.cs` 在内的改动**编译通过**；
   Unity 侧 `recompile` **未跑**（⛔ 在别片 Play 中强行 recompile 会触发 domain reload 打断对方）。

---

## 2026-09-23 monster-audio 片第二轮（接线）：上面 2/3/4 三条**已消除**，只追加不改写

6. **【已消除】第 2 条（怪物自身受击音）+ 第 3 条（逐类攻击/死亡音）+ 第 4 条（脚步触发点）**
   —— 取件 → 常量 → 登记 → 触发，四步全做完：
   - **取件**：`原始包 d2sfx.mpq`（已在盘）按名解 **27/27 成功、0 失败**
     （清单 `.ai-tmp/test/wanted-monster-audio.txt`，脚本 `tools/probes/mpq/extract_wanted.py --base-archive d2sfx.mpq`；
     ⛔ 全程**按名**解包，未用 `SFileFindFirstFile` 枚举）。
   - **入仓**：30 个 `.wav` 落 `client/Assets/Resources/Clover/Sound/SFX/`，
     与 mpq 原字节 **sha256 54/54 逐字节一致**（`tools/probes/mpq/sfx-provenance.tsv`，`sfx_provenance.py` 重跑 RESULT=PASS）。
   - **键**：`Module/Combat/SfxKeys.cs` +30（`monster_{hit,atk,die,step}_{code}`）；
     `Module/Audio/SfxRegistry.cs` 的 `SfxFiles`/`Origins` 各 +30；解析表 `Module/Monster/MonsterSfx.cs`（新增）。
   - **触发**：`DamagePipeline`（命中 = `hit` + **怪物自身受击音**；击杀 = **逐类死亡音**）、
     `MonsterModule`（出手 = 逐类攻击音；**跨格 = 逐类脚步音**）。
   - **8 类代码 → 类别的唯一出处** = `MonStats.txt` 的 `Code`×`MonSound` 列
     （⛔ 实测反例：`bk` = **foulcrow 血鹰**、`ye` = **brute 野兽**，按显示名猜会整套错位）。
   - 已解析完（上一轮未定案的 4 类全在这里）：`cr=corruptrogue`（`corrupt_*`）、`ye=brute`（`yeti_*`）、
     `wr=wraith`（`wraith_*`）、`bk=foulcrow`（`hawk_*`，脚步档 = `hawk_wing_1` 振翅）。

7. **【已消除（第四轮）】脚步的 `FsCnt` 已按表建模**：`MonSounds.txt` 给有脚步的 6 类
   `FsCnt=2 / FsOff=0 / FsPrb=100` ⇒ **每 `1/FsCnt = 0.5` 格一步**（不再是"跨格一次"）。
   实现 = `MonsterModule.Tick` 里累计**本帧位移**（`Vector2.Distance(posBefore, m.Pos)`），
   累计 ≥ `MonsterSfx.StepPeriodTiles`（= `1/FsCnt`）就出一次并扣掉一个周期（⛔ 不靠"格号变了没"，
   否则慢速怪会把一步拖成一格）。`FsPrb=100` ⇒ 不掷随机；将来出现 `FsPrb<100` 的类 ⇒ **未建模**，需补掷点。
   尖刺鼠 `si` / 幽灵 `wr` 的 `FsCnt` 列为空 ⇒ 原版没有移动音 ⇒ **一次都不响**（⛔ 不拿别的类凑）。

8. **【已消除（第四轮）】受击音的 `HitDelay` 已按表建模**：`MonSounds.txt` 的 `HitDelay` 是**帧**
   （fallen/fs/zm/ye/cr/bk/wr = **2**，quillrat = **5**），换算口径 **÷ `MonsterTuning.LogicFps`(25)**
   （出处：`MonsterTuning.cs` 「官方出处是动画帧数…÷ 25」+ 既有 `OfficialAiDelayFrames / LogicFps` 写法）
   ⇒ 0.08s；尖刺鼠 0.20s。
   实现 = `MonsterModule.ApplyDamage` 把怪物自身受击音排进 `PendingHitSfx` + `PendingHitSfxTimer`，
   `Tick` 里到点起播；`DamagePipeline` 只同帧播**撞击音** `hit`（两音按原版错开）。
   ⛔ 击杀那一下不排受击音（播的是死亡音）。
   **【已消除（第五轮）】`DeaDelay`（死亡音延迟帧）已按表建模**：全类 = **1** 帧、quillrat = **4** 帧
   ⇒ 0.04s / 0.16s（同样 ÷ `MonsterTuning.LogicFps`(25)）。
   实现 = **复用**受击音那套 `PendingHitSfx` / `PendingHitSfxTimer`（⛔ 不新造排期），
   在 `MonsterModule.Die` 里排期；`MonsterModule.Tick` 的起播点**移到 `alive` 判断之前**
   （尸体分支下面直接 `continue`，放后面会导致尸体的死亡音**永远播不出来** = 静默失效）。
   `DamagePipeline` 击杀分支现在**一声都不播**（否则会变成"同帧 + 延迟"两声）。
   ⇒ **`HitDelay` / `DeaDelay` / `FsCnt` 三条原版时序全部落地，本片无残余。**

9. **【只登记·不做】`FsPrb`**：6 类有脚步的 `FsPrb` 全是 **100** ⇒ 当前不掷随机、不偏移（`FsOff` 全是 0）。
   将来若接入 `FsPrb<100` 的怪物类别 ⇒ 需要补掷点（随机源必须走调用方传入的 `CloverEngine.Rng`，
   ⛔ 不许在 AI/音效路径里自掷）。**按裁决：只登记，不实现。**

9. **判据改动两条（必须写明，⛔ 不是放宽）**：登记表从 24 键增到 54 键后，`audiocheck` 里两条
   **字面量**过时且必然判红，已改成**关系式**（都比原字面量更强）：
   - 「台账行数 == 24」→「台账行数 == `SfxRegistry.AllSfxKeys.Count`」（54）；
   - 「sfx-verify.log 含 `24/24`」→「含 `{台账行数}/{台账行数}`」（54/54）。
   另给 `IdentOf()` 补了 30 个键的常量名映射（否则新键判不出调用点）。
   ⇒ 改后 `audiocheck` **98 项 FAIL=0**、`animcheck` **87 项 FAIL=0**。

10. **Unity 编译**：`unity command recompile` → `recompile_status` = `completed`，
    `compilationFailed=false`，`errors=[]`（编辑器读数 `playMode=stopped` 后才落的码）。

---

## 2026-09-23 片 wp-art 追加登记（**只追加**；本节之前的任何行一字未改）

| 编号 | 缺什么 | 类别 | 现状 | 需要提供什么 | 期望落点 | 涉及文件 | 状态 |
|---|---|---|---|---|---|---|---|
| WP-ART-1 | **传送台本体动画**：`Objects.txt` Id=119 / `Name=Waypoint` / `Token=wp` 的**世界内**美术（原版 `data\global\objects\wp\*.dc6/.dcc`） | 图（DC6/DCC 多帧） | 锚点 (31,26) 上**只有原版地砖**：`Module/Map/MapGenTown.cs` 的 `Waypoint` 常量注释已写明"本体艺术本工程未解包"，全仓脚本 0 处引用任何传送台 sprite（grep `ObjectSprite`/`\bwarp\b` 均无传送台条目） | **缺归档**：本机 `原版资源/_mpq_incoming/` 只有 `D2data.mpq`(292,106,381 B)、`Patch_D2.mpq`、`d2char.mpq`、`d2music.mpq`、`d2sfx.mpq`；**`d2exp.mpq` 不在盘**（`原版资源/清单.md` 已记"d2exp.mpq 不在归档内"）。`D2data.mpq` 自带 `(listfile)` 是 stub（实测仅 4 条）⇒ 只能按名取件 | `Assets/Resources/Clover/D2/Objects/waypoint/**`（PNG）+ `Core/ResPaths.cs` 只加一个常量 | `Module/Map/MapGenTown.cs`（**只加美术**，锚点坐标/面板逻辑不动）、`Core/ResPaths.cs` | ⛔ **BLOCKED**（素材不在任何在盘归档里；详见 `原版资源/../.ai-tmp/test/report-wpart.md`） |

试过并**全部落空**的取件路径（逐条，供下一棒复核）：

1. `原版资源/d2dc6/**`（475 个 `.DC6`/`.dc6`）：子目录只有 `data/global/items/**`、`data/global/ui/**`、`data/LOCAL/{FONT,UI}/**` —— **没有任何 `objects/` 子目录**。
2. `原版资源/d2raw/**`（549 `.ds1` / 56 `.dt1`）：全部是 `data/global/{tiles,palette,excel}/**`；56 个 dt1 逐个核对，**没有 waypoint 的 dt1**。`ACT1/BARRACKS/warp.dt1` 已解包成 `D2/Objects/warp/`（81 张 + `manifest.json`），但 `dump_town_exit.py` 实测：它的 3 个标记格在营地**内部** `(12,18)/(14,18)/(16,25)`，是 `LvlPrest.txt` 的**关卡出入口（Warp 过渡块）**，与出城口无关、更不是传送台 ⇒ **不能用它顶替**（§0 铁律 1）。
3. `参考工程_Diablerie/`（92 个 txt + `d2lod1.10txt.zip`）：**只有官方 1.10f excel 表**（`objects.txt` 在其中），无任何美术文件、无 `.cs`。
4. `D2data.mpq`（`Patch_D2.mpq` 已作 patch 挂载）按名解包，工具 = `tools/probes/mpq/extract_wanted.py`，判据 = 产物落盘且 >0 字节；每轮都带对照项 `data\global\items\invwpl.DC6`（**每轮都命中** ⇒ 链路可信）：
   - 第 1 轮 14 条具名猜测（`wp\wpNU.dc6/.dcc`、`wp\wpOP.*`、`wp\wpON/S1/.dcc`、`wn\wnNU.*`、`objects\waypoint\waypoint.dcc` …）：**0 命中**；
   - 第 2 轮 `objects.txt` 全部 **520 个 token** × {`NU`} × {`.dc6`,`.dcc`} = 1040 条：**0 命中**；
   - 第 3 轮 520 token × 6 种命名模板（`{tok}\{tok}{mode}.{ext}`、`{tok}\{tok}.{ext}`、扁平 `{tok}{mode}.{ext}`；ext=`.dc6`/`.dcc`）= 3121 条：**0 命中**；
   - 第 4 轮 `objects\wp\` / `objects\wn\` 下 **wp|wn + 0..3 位 [a-z0-9] + `.dc6`/`.dcc`**（含扁平 `objects\wp*.dc6`）= **287,935 条**，耗时 62.6s：**仅对照项命中，0 命中**。
5. MPQ hash 表直读（`hashPos=290879949 / hashNum=65536 / blkNum=11116`）：本片的首轮实现**未通过自检**（已知存在的对照名被判为不存在）⇒ 该轮结论**已作废、不作判据**；判据只采用第 4 条（走 StormLib `SFileExtractFile`）。

⚠️ 给主 agent 的裁决点：要么**补 `d2exp.mpq`（或其它含 `data\global\objects\wp\*` 的归档）**，要么**接受"营地里没有传送台本体图"这一原版差异**（`MapGenTown.Waypoint` 的注释口径，交互功能不受影响）。⛔ 本片**没有**自创/借用任何别的图顶替。

---

## 2026-09-23 片 black-why2 追加登记（**只追加**；本节之前的任何行一字未改）

| 编号 | 缺什么 | 类别 | 现状 | 需要提供什么 | 期望落点 | 涉及文件 | 状态 |
|---|---|---|---|---|---|---|---|
| BW2-1 | **地图角格处「屏幕零虚空」**：玩家站在地图**角格**（如 (0,0) / (79,79) / (0,79) / (79,0)）时，屏幕仍会露出**地图外虚空**（实测图外格量 5.67~7.67 格，占屏约 30%） | 已知差异（几何结构性，⛔ 非缺陷） | 相机已改走**格空间**夹制 `CameraBounds.ClampFocusGrid`（`Module/Camera/**`），边格处已归零（实测黑窗现场 (1,20)：图外格量 5.58 → **0**）；但角格处**一分不退让**（5.67 → 5.67） | 不是素材/数据缺口 —— 需要的是**另一条改动**：把地图边界外一屏铺成崖壁/黑石（`Module/Map` 侧），相机层解决不了 | `Module/Map/**`（边界外填充），⛔ 不是 `Module/Camera/**` | `client/Assets/Scripts/Module/Camera/CameraBounds.cs`（现状已在此封顶）、`tools/probes/hosts/playercheck/Program.cs` §11.10（判据） | ✅ **已接受为已知差异**（主 agent 2026-09-23 裁决：主角可见优先，不拿"玩家出画"换"零虚空"） |

**为什么是结构性的（推导，⛔ 不是"没修好"）** —— 记 `u = gx − gy = X / Iso.HalfW`、`v = gx + gy = −Y / Iso.HalfH`，
半屏在 (u,v) 框里的半跨 `a = halfW / Iso.HalfW`、`b = halfH / Iso.HalfH`（本项目 a≈6.67、b≈7.5）。
可见格矩形 = `{|Δu| ≤ a, |Δv| ≤ b}`；它全部落在地图内 ⇔ `u+v ∈ [a+b, 2(W−1)−(a+b)]` **且** `v−u ∈ [a+b, 2(H−1)−(a+b)]`。
玩家在角格时两条约束**同时**贴边，相加得 `2v ≥ 2(a+b)` ⇒ 需要 `|Δv| ≈ a+b`；
而"焦点仍在视口内"只允许 `|Δv| ≤ b` ⇒ **`a + b > 2b` 时无解**（角格实测需把机位推 ~7 格 > 半屏 3.75 世界单位）。
⇒ 与 `CameraBounds.cs` 文件头「一条硬约束」段的独立推导**同结论**。

**处置**：`CameraBounds.ClampFocusGrid` 按「**主角可见优先**」封顶 —— 越界就让位给玩家可见，虚空推到最少即止。
判据 = `tools/probes/hosts/playercheck` §11.10（8 用例；`图外格量不差于修前` 全部 PASS，角落为 5.67 → 5.67）。

⚠️ 若要彻底消除角落虚空 ⇒ 改 `Module/Map`（边界外铺崖壁），⛔ 相机层做不到；本片未做，也未拿别的东西顶替。

---

## 2026-09-24 片 dialog-options2 追加登记（**只追加**；本节之前的任何行一字未改）

| 编号 | 缺什么 | 类别 | 现状 | 需要提供什么 | 期望落点 | 涉及文件 | 状态 |
|---|---|---|---|---|---|---|---|
| DO2-1 | **中等按钮的「悬停 / 高亮」底图**：原版 `data/global/ui/FrontEnd/MediumSelButtonBlank.dc6` 帧 0 必须配 `data/global/palette/menu1/Pal.PL2` 才解得出正确颜色 | 图（DC6 单帧） | 工程里那份 `D2/UI/Menu/btn_med_sel.png` 是**按 ACT1 调色板**解出来的**麻点图**（与 `tools/d2codec` 解码器输出**逐像素全等** maxdiff=0 ⇒ 不是写坏，是**调色板选错**）。麻点度量（相邻不透明像素的平均颜色跳变，越小越平滑）：`btn_med_sel` **53.40** vs `btn_med_normal` 26.03 / `btn_med_pressed` 24.08（≈2 倍）。实机后果 = 鼠标一悬停到中等按钮，底板花斑**把按钮文案糊掉**（实机图 `.ai-tmp/screenshots/btnlabel_recap_g1_dialog_options.png`：NPC 对话第 2 项「交易」被糊住；第 1 项「離開」清晰） | **`data/global/palette/menu1/Pal.PL2`**（在原版包 `D2data.mpq` 内）。本机取不到：`原版资源/d2raw/data/global/palette/` 只有 ACT1 / EndGame / loading / fechar 四套；且 `storm.dll` 缺席（`.ai-tmp/test/storm/storm.dll` 不存在）⇒ `tools/probes/mpq/extract_wanted.py` / `tools/d2codec/storm.py` 都无法按名取件 | `原版资源/d2raw/data/global/palette/menu1/Pal.PL2`；拿到后按 `tools/d2codec/export_d2ui.py` 的 `group_buttons` 重出 `Assets/Resources/Clover/D2/UI/Menu/btn_med_sel.png`（+ `btn_med_sel_pressed.png`） | `Core/ResPaths.cs`（常量已在）；`UI/UiArt.cs`（`ButtonSpritesFor` 的中等分支：拿到后把悬停帧改回 `ResPaths.BtnMedSel`）；`tools/d2codec/export_d2ui.py` | ✅ **已解决**（2026-09-24，片 dialog-options 追加）—— 原口径假设的 `menu1` **被证伪**；**正确调色板 = `fechar`**（该 DC6 属 FrontEnd 组）。资产已重出、接线已恢复、导出器已同步（见下方「DO2-1 解决记录」）。⚠️ 上面「现状/需要提供什么」两列**保留原样** = 「曾经 BLOCKED、后来怎么解开」的记录 |

### DO2-1 解决记录（2026-09-24，片 dialog-options 追加）

**定案：正确调色板 = `fechar`** —— **不是** 原假设的 `menu1`，**也不是** ACT1。
（该 DC6 在 `FrontEnd/` 下，属 **FrontEnd 组**；同目录的**共享**按钮才用 ACT1。）

判据（**可复跑**：`tools/probes/measure/probe_med_sel_palette.py`、`regen_med_sel_fechar.py`；麻点判据 = 孤立高饱和像素 / 高饱和像素）：

1. **解法自证**：用恢复回来的 `storm.dll`（`.ai-tmp/test/storm/storm.dll`，482304 B）从 `D2data.mpq(+Patch_D2.mpq)` 把 **15 套** `data/global/palette/*/Pal.PL2` 全解出来；解出的 `ACT1/Pal.PL2` 与 `原版资源/d2raw/.../ACT1/Pal.PL2` **逐字节同哈希**（`DE848A8D…`），且各套互不相同 ⇒ 不是"解错名返回同一个文件"的假阳性。
2. **同一判据下的唯一干净解**：`fechar` sel 帧0 = **0.054**、帧1 = **0.055**；对照 `menu1` **0.271（假设被证伪）**、menu4 0.103、sky 0.106、ACT1 **0.424**（旧麻点图来源）、其余 0.24~0.55（干净带 = 同族干净图量级 0.017~0.075）。
3. **横向佐证（决定性）**：同目录**共享**按钮只有 ACT1 干净 —— `WideButtonBlank` 0.017 / `MediumButtonBlank` 0.040 / `CancelButtonBlank` 0.045，改用 `fechar` 反而 0.104~0.177 起麻点；**只有 FrontEnd 专属的 Sel 变体反过来要 `fechar`** ⇒ 口径 =「同一目录里共享按钮 ACT1 / FrontEnd 专属按钮 fechar」，与本文件 `export_d2ui.py` 开头「`frontend` 组用 `PL2_FECHAR`」的既有惯例一致。

已落地：

| 项 | 内容 |
|---|---|
| 资产 | `client/Assets/Resources/Clover/D2/UI/Menu/btn_med_sel.png`（帧0，128×35，麻点 **0.054**，sha256 `06887764375D16E9…474C19`）；`btn_med_sel_pressed.png`（帧1，0.055，`0BCDECBC92B16451…7FEB0D`）。旧麻点图留档 `.ai-tmp/test/do-pal/old_btn_med_sel{,_pressed}.png`（`478BB689…`） |
| 接线 | `UI/UiArt.cs` 的 `ButtonSpritesFor` 中等分支 = `ResPaths.BtnMedSel`；原先的降级告警 `btnart.med.sel.blocked` **已删**（前提消失） |
| 导出器 | `tools/d2codec/export_d2ui.py::group_buttons` 的 `MediumSelButtonBlank` 两帧改用 **`PL2_FECHAR`** + 注释写明依据 ⇒ **防全量重导把麻点图覆盖回来** |
| 判据 | `uicheck` 的 Ⓐ-4/Ⓐ-4b：`ButtonSpritesFor` 返回的**每一条**路径都必须过麻点判据（含退化样本：旧麻点帧喂进同一判据必须变红） |

遗留（不是缺陷，仅登记）：本机 `原版资源/d2raw/data/global/palette/` 仍只落盘 4 套（ACT1/EndGame/loading/fechar），其余调色板**从 MPQ 现取现用**（`tools/probes/measure/*.py`），不另存。

## 2026-09-24 片 dialog-options2 追加登记（第二次；**只追加**）

| 编号 | 缺什么 | 类别 | 现状 | 需要提供什么 | 期望落点 | 涉及文件 | 状态 |
|---|---|---|---|---|---|---|---|
| DO2-2 | **（DO2-1 的更正/结案）** `data/global/palette/menu1/Pal.PL2` 已取回（`tools/probes/measure/extract_palette_from_mpq.py`，**443175 B / 256 项 / sha256 `FAC66F60622A36274C85E3940FB9C6EF9C2A27A0E31F90D66918D9957B125F63`**），但**实测它不是这批按钮的色表**：menu1 下常态帧 90.67、高亮帧 61.71（同族干净带 ≈ 24~26）⇒ **不采用**。真正的口径是「**按文件分调色板**」：`MediumButtonBlank/WideButtonBlank.dc6` → **ACT1**；`MediumSelButtonBlank.dc6` → **fechar**。证据：① 包里 **15 套** palette 穷举，**没有一套**能同时让两帧干净（`find_frontend_button_palette.py`）；② 两个 DC6 **连边框/雕槽的索引都不同**（23.7% / 21.1%，`probe_sel_button_structure.py`）；③ 色相锚点读图：fechar 的金边框+深蓝宝石与已知干净的常态帧同形同色（`probe_sel_palette_gem_anchor.py` + `.ai-tmp/screenshots/dialogoptions2_selpal_gems.png`） | — | ✅ **DO2-1 结案**：`btn_med_sel*.png` 已按 fechar 重出，麻点度量 **53.40 → 26.56** / 50.88 → **23.20**，落回同族干净带（常态 26.03 / 按下 24.08） | ⚠️ **新增守护**：⛔ 不许把同族的 `btn_med_normal/pressed`、`btn_wide_normal/pressed` 也用 `fechar` 重出 —— 实测会把它们从 26.03/24.08 打成 **105.16/99.46**（宽按钮 19.55/18.25 → 83.62/80.89）。已在 `tools/probes/measure/probe_med_button_palette.py` 文件头写明，并有宿主断言（`uicheck` Ⓐ-5d 量接线的图、Ⓐ-5e/5g 做退化自证） | `原版资源/d2raw/data/global/palette/menu1/Pal.PL2`（已落，留档备用）；`client/Assets/Resources/Clover/D2/UI/Menu/btn_med_sel*.png`；`tools/probes/measure/*` | ✅ **已结案**（原 `DO2-1` 的 BLOCKED 已消除） |
## s-ledger-docs 2026-09-24 更正（S-70 ③）

> S-70 ③ 记「本文件 13 行 ⬜ 实为 ✅」。按盘复核（2026-09-24）：**A 表 A-1 ~ A-24 共 24 行**的状态列仍写 `⬜ 待替换`，但这些键**已全部到位并接线** —— 出处 = 本文件 **#6 行**（`音效 SFX` ✅ 已到位：24 个原版 `.wav`、键名 = 文件名、触发点 19 处全接线）+ `tools/probes/mpq/sfx-provenance.tsv`（24/24 与真实 `d2sfx.mpq` 内同名 `.wav` 逐字节 sha256 相同）+ `tools/probes/hosts/audiocheck`（68 项通过 / 0 失败）+ 实机 `.ai-tmp/screenshots/sfx_evidence_sfx1.txt`（13 个 clip 真出声）。故 A 表那 24 行 `⬜ 待替换` **全部过期**（不止 13 行）。
>
> 本行按白名单「只追加」落盘（⛔ 未就地改 A 表任何既有行）；A 表 ⬜ → ✅ 的就地更正请由本文件维护片执行。
